# Jira 연동 작업 로그

## 2026-04-14 — 초기 구축

### 완료 작업

1. **todo/system/ 체계별 문서 작성** (7개 파일)
   - 01-datasource, 02-agent-pipeline, 03-api-collect, 04-monitoring
   - 05-api-provide, 06-procedure-mgmt, 07-security
   - 관점: 요구사항 → 서비스별 구현(개발) → 등록 현황(등록)
   - 파싱 규칙: `## 제목 [태그]` → Task, `- [x]/[ ]` → Sub-task

2. **Jira 초기 등록**
   - 기존 251건 삭제
   - Epic 7개 + Task 59개 + Sub-task 276개 생성
   - 완료 전환 234건 ([x] 항목 자동 완료)
   - 매핑 파일: `dev_plan/jira/jira-mapping.json`

3. **타임라인 날짜 설정**
   - Epic 5개 + Task 50개 시작일(customfield_10015) 설정
   - duedate는 기한 초과 경고 문제로 제거 → 시작일만 유지 (그라데이션)
   - 미개발 Epic(5-API제공, 6-프로시저)은 날짜 없음

4. **스크립트 개발** (`scripts/jira-sync.py`)
   - `--init`: 삭제 + 생성 + 매핑 저장
   - `--sync`: 매핑 기반 상태 업데이트 + 신규 생성
   - `--dry-run`: 미리보기

### 코드 검증 후 불일치 수정 (검토 완료)

| # | 항목 | 변경 | 이유 |
|---|------|------|------|
| 1 | DB 타입 | 5종 유지 + Tibero 비고 | Tibero는 Oracle로 대체 구현, 실사용 아님 |
| 2 | I3 JejuFacilityLoadStep | `[ ]` → `[x]` | 388줄 완성, 커밋 aad084f (4/13) |
| 3 | Entity 전환 Phase 5 | `[ ]` → `[x]` | Step 4개 JPA 전환 완료, 커밋 94e2127 (4/13) |
| 4 | 새올 Loader 표기 | "SaeolLoadStep" → "DefaultLoadStep 1:1 MERGE" | LINK_PLAN 커서는 SND(others)쪽, Loader는 단순 MERGE |
| 5 | API키 실제값 표시 | `[ ]` → `[x]` | 키 ID 표시가 정상 동작, 실제값 노출 불필요 |
| 6 | HikariCP 하드닝 | Sub-task 분리 | api-collector 미적용 → 별도 미완료 항목 추가 |

### 미반영 (todo 수정 + Jira sync 필요)

위 6건의 todo 수정 후 `python3 scripts/jira-sync.py --sync` 실행 필요

### 설계 결정 사항

- **duedate 미사용**: 기한 개념 없음, 작업기간(실적) 표시 목적
- **타임라인**: start date만 사용, 종료일 없이 그라데이션 허용
- **매핑 파일 구조**: `jira-mapping.json`에 이슈 키 + 날짜, todo 파일은 내용만
- **라벨 3종**: 개발 / 등록 / 확정필요
- **[참고] 태그**: Jira 미등록, Epic description에 codeBlock으로 삽입
- **동기화 방식**: 삭제+재생성 아닌 매핑 기반 상태 업데이트 (이슈 키 유지)

### Jira 정보

| 항목 | 값 |
|------|-----|
| 사이트 | https://k1m-gims.atlassian.net/ |
| 프로젝트 | 2026_gims (KAN) |
| 인증 | dev_plan/jira/jira_email.txt + jira_token.txt |
| 매핑 | dev_plan/jira/jira-mapping.json |
| 스크립트 | scripts/jira-sync.py |

### 이슈 타입/필드 정보

| 항목 | ID |
|------|-----|
| 에픽 | 10005 |
| 작업 (Task) | 10007 |
| Subtask | 10006 |
| Start date | customfield_10015 |
| 라벨 | labels |
| 완료 전환 | transition 41 |
| 해야할일 전환 | transition 11 |

### API 참고

- 검색: `GET /rest/api/3/search/jql` (구버전 /search 폐기됨)
- maxResults 최소 1 (0 불가)
- description: ADF(Atlassian Document Format) 사용 필수
