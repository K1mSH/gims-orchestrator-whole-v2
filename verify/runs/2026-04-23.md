# 2026-04-23 검증 실행 기록

## 기준 커밋
`998d362` — docs: verify 검증 세션 체계 신설

## 세션 맥락
- 검증 세션 체계(`verify/`) 신설 후 첫 실제 검증 사이클
- 대상: 공통 규약(invariant 카테고리 1~11) 위반 여부 grep 기반 전수 스캔
- 공식 체크리스트 도입 전 단계라 임시 grep 기반 ad-hoc 검증. 본 runs 문서에 스캔 범위/결과 직접 기록.

## 스캔 항목
- [x] invariant 11A — credentials 외부화 (Jasypt ENC 패턴 적용 여부)
- [x] invariant 11A — DB/서비스 URL 환경변수 치환 여부
- [x] invariant 11B — 임시값 (tmp/dummy/placeholder/fake/example.com)
- [x] invariant 11A — Windows 절대경로 하드코딩
- [x] invariant 3 — dev 포트 29xxx 하드코딩
- [x] 코드 내 TODO / FIXME / XXX / HACK

## 사용한 grep 명령 (재실행 가능)
```
# 임시값
grep -rE "(?i)\\btmp[_\\-]|\\bdummy\\b|\\bplaceholder\\b|\\bfake[_\\-]|example\\.com" --include="*.{yml,yaml,properties,java,ts,tsx,js}"

# localhost
grep -rn "localhost" --include="*.{yml,yaml,properties,java,ts,tsx,js}"

# dev 포트
grep -rnE "29000|29001|29004|29005|29006|29010" --include="*.{yml,yaml,properties,java,ts,tsx,js}"

# dev 계정
grep -rnE "\\bk1m\\b|password:\\s*1111|password=1111" --include="*.{yml,yaml,properties,java,ts,tsx,js}"

# TODO
grep -rnE "\\b(TODO|FIXME|XXX|HACK)\\b" --include="*.java"

# Windows 경로
grep -rnE "[DC]:[\\\\/]|[DC]:\\\\\\\\" --include="*.{yml,yaml,properties,java,ts,tsx,js}"

# URL 필드 전수
grep -rnE "^\\s*(url|jdbc-url):\\s" --include="application.yml"
grep -rnE "password:|username:" --include="application.yml"
```

## 결과 요약

| 항목 | 결과 | 비고 |
|------|:----:|------|
| 전 모듈 Jasypt ENC 적용 | **FAIL** | `gims-api-provider` 단일 예외 |
| localhost URL 환경변수 치환 | **PARTIAL FAIL** | `gims-api-provider`, `infolink-api-collector` 일부 |
| 임시값(tmp/dummy/fake) 제로 | PASS | Java/yml 깨끗, Frontend 70 건은 HTML `<input placeholder=...>` UI 속성 (무해) |
| Windows 경로 하드코딩 | PASS | 0 건 |
| dev 포트 29xxx 하드코딩 | PASS | 매치 모두 주석 |
| TODO / FIXME | PASS (경미) | 2 건 (ApiPrvNgw04, DatasourceService) |

## 발견 이슈
- **[VER-001](../issues/OPEN/VER-001-api-provider-externalization-missing.md)** — gims-api-provider 외부화 누락 (credentials + URL 평문). **실배포 블로커**.
- **[VER-002](../issues/OPEN/VER-002-api-collector-lookup-url-not-externalized.md)** — api-collector Lookup / API-Key URL 환경변수 치환 지점 미비. Mock URL 실배포 잔존 위험. (오후 2 차 스캔에서 Mock fallback 현황 전수 식별 — 본문 § H 참조)
- **[VER-003](../issues/OPEN/VER-003-frontend-spectab-host-hardcoded.md)** — Frontend `SpecTab.tsx` API host 코드 내 평문 하드코딩. 다른 호출부는 Next.js 프록시 경유 / `@Value` 로 외부화된 것과 대비되는 단일 예외.
- **[VER-004](../issues/OPEN/VER-004-bojo-int-target-execution-id-index-missing.md)** — bojo-int target 엔티티 20+ 전체 `@Index(execution_id)` 전무. `/target`, `/trace-source` 성능 저하.
- **[VER-005](../issues/OPEN/VER-005-provide-target-execution-id-index-missing.md)** — provide 16 엔티티 `@Index(execution_id)` 전무. 4/22 `@Comment` 만 추가되고 인덱스 빠짐.
- **[VER-006](../issues/OPEN/VER-006-bojo-int-entity-comment-missing.md)** — bojo-int 전 엔티티 `@Comment` 전무. 다른 Agent 는 적용, bojo-int 만 예외.
- **[VER-007](../issues/OPEN/VER-007-bojo-int-source-refs-uk-missing.md)** — bojo-int 엔티티 `source_refs` UK 전무. 다른 Agent(bojo/others/provide 32엔티티)는 `uk_*_source_refs` 일관 적용.
- **[VER-008](../issues/OPEN/VER-008-h2-console-enabled-production-risk.md)** — `spring.h2.console.enabled: true` 하드코딩(bojo, api-collector). 프로파일 분리 없어 실배포 시 콘솔 노출. **보안 위험**.
- **[VER-009](../issues/OPEN/VER-009-logback-configuration-missing.md)** — 전 모듈 `logback-spring.xml` 부재. Spring Boot 기본 로그만 사용 → 파일 저장 / 롤링 / 레벨 제어 불가. **운영 관측성 치명적 결함**.

## 파생 작업
- **[TASK-001](../tasks/OPEN/TASK-001-datasource-reference-check.md)** — Datasource 삭제 시 Agent 참조 무결성 검증 (DatasourceService.java:226 TODO 승격). 운영 안정성 관점 중요.

## 오후 추가 — config-replacement § G~I 확장 (2 차 스캔)

오전 1 차는 **yml / dev 계정 / Windows 경로** 위주로 스캔 후 VER-001 / VER-002 / TASK-001 발행.
오후 2 차는 **Java / Frontend / docker-compose** 로 범위 확장.

### 추가 스캔 대상
- [x] Java 내 localhost (13 매치)
- [x] Frontend `.ts/.tsx/.js` 내 localhost (5 매치)
- [x] `.properties` 파일 (매치 없음 — globals.properties 부재)
- [x] docker-compose (환경변수 `${SERVER_PORT}` 정상 사용)
- [x] Java 내 dev 포트 29xxx (주석 1 건만)

### 분류 결과
| 상태 | 건수 | 비고 |
|------|:--:|------|
| `@Value` 외부화 완료 (H1~H7) | 11 | Agent/Proxy 통신, Mock fallback 등 이미 환경변수 주입 가능 |
| CORS 평문 하드코딩 (G1~G2) | 2 | WebConfig.java — `@Value` 도입 필요 |
| Next.js 프록시 destination 평문 (G3~G5) | 3 | next.config.js — `process.env.*` 도입 필요 |
| Frontend 컴포넌트 내 평문 (G6) | 1 | SpecTab.tsx — **VER-003 발행** |

### config-replacement.md 갱신 내역
- **§ G 신규** — CORS + Next.js + SpecTab 6 항목 치환 대상 추가
- **§ H 신규** — `@Value` 외부화 완료 환경변수 9 개 키 레퍼런스 (실배포 주입 대상 목록)
- **§ I 리넘버** — 1 차 / 2 차 grep 스캔 체크박스 완료 + 재실행용 `rg` 명령 고정

### 오후 발견의 의미
- **"하드코딩 회피 규약"이 파편적** — MEMORY(`feedback_config_vs_registration`), invariant § 3 / § 11A / § 11B 에 흩어져 있음. 통합 invariant (`no-hardcoded-env-values.md` 같은 형태) 작성 후보.
- 실배포 블로커는 VER-001 / VER-002 위주. VER-003 은 단일 파일이라 우선순위 낮지만 **일관성 위반** 으로 발행.
- **H1~H7 의 `@Value("${key:default}")` 패턴이 프로젝트 표준** — 신규 코드는 반드시 이 패턴 또는 Next.js `process.env.*` 따라야 함.

## 양호 확인된 사항
- 전 모듈(8 개) 중 **7 개가 이미 Jasypt ENC + `${JASYPT_PASSWORD:sync-pipeline-secret-key-2024}` fallback 패턴** 적용 — 구조적 설계는 탄탄
- Jasypt 기본 암호(`sync-pipeline-secret-key-2024`)는 fallback, 실배포 시 `JASYPT_PASSWORD` 환경변수 주입으로 치환 가능 (`verify/deployment/credentials-rotation.md` 에 명시 대상)
- Java 코드 대부분(H1~H7)이 `@Value("${env.name:default}")` 패턴 이미 적용 — 실배포 전환 가능 (2 차 스캔 확인)
- `ApiPrvNgw04.java:78` 의 TODO (`0011 ~ 0125 항목 추가 필요`) 는 forward 세션이 provide A4/A6 이식 중인 컨텍스트라 **정상 진행 사항**으로 간주 (VER/TASK 승격 보류)
- Frontend `placeholder` 70 건은 모두 HTML `<input placeholder="...">` UI 힌트 속성. 실 값 아님. 향후 재스캔 시 제외 패턴 고려.

## 후속 작업 (다음 세션 대기 목록)
- [ ] VER-001 / VER-002 / VER-003 해결 확인 후 DONE 이동
- [ ] 통합 invariant 세부 파일 `no-hardcoded-env-values.md` 작성 (§ 3 + § 11A 교집합)
- [ ] 공식 체크리스트 `checklists/_invariants/externalization.md` 작성 시 본 스캔 절차 포함 (재실행 가능한 `rg` 명령 고정)
- [ ] `_invariants/` 세부 파일 작성 계속 (`header-routing.md`, `trace-source-branches.md` 등)
- [ ] `checklists/` 나머지 Agent 확장 (`bojo-rcv`, `bojo-loader`, `bojo-int`, `api-collector`, `orchestrator-api` 등)

## 파트 겹침 판단 (핸드오프용)
- **VER-001 (P9-api-provider)** — 다른 세션 provide A4/A6 작업(P7)과 파일 겹침 없음 → **병렬 안전**
- **VER-002 (P8-api-collector)** — 독립 모듈 → **병렬 안전**
- **VER-003 (P11-frontend)** — UI 전용 파일. backend 영향 없음 → **병렬 안전**
- **VER-004 (P6-bojo-int)** — 엔티티 20+ 수정. forward 세션 provide(P7) 작업과 독립 → **병렬 안전**
- **VER-005 (P7-provide)** — forward 진행 중인 A4/A6 엔티티 수정과 **같은 파트** → 순차 처리 권장 (A4/A6 이식 완료 후 일괄 `@Index` 추가)
- **VER-006 (P6-bojo-int)** — VER-004 와 같은 파트 + 엔티티 전체 → VER-004 와 **묶어서 처리** (한 세션에서 인덱스 + @Comment 동시 반영하면 효율 ↑)
- **TASK-001 (P10-orchestrator-backend)** — 광범위 영향 파트. P10 수정은 전 Agent 회귀 대상이지만 단일 메서드 내부 로직이라 실제 영향은 제한적. 다른 P10 작업과 동시 진행 시 주의.

## 오후 후반 — 추가 규약 스캔 (다중 라운드)

각 라운드마다 새 invariant 카테고리를 훑으며 위반 탐지.

### 라운드 3

앞선 "외부화" 축 외에 **추적 / 감사 / 주석 축**으로 확장 스캔.

### 스캔 항목 & 결과
| 규약 | 스캔 대상 | 결과 |
|------|---------|------|
| `@Index(execution_id)` (invariant 4) | 전 Agent 의 `entity/target` + `entity/iftable` | bojo/others/common ✅ / **bojo-int 전무(VER-004)** / **provide 전무(VER-005)** |
| `@Comment` (invariant 7) | 전 Agent 의 `entity/**` | bojo/others/provide/orchestrator/collector ✅ / **bojo-int 전무(VER-006)** |
| `X-Manage-Datasource-Id` 헤더 사용 | Orchestrator + common + Proxy | Orchestrator(ExecutionService.java:606) + common(ExecutionDataController 12 메서드) ✅ / **Proxy 내 사용 흔적 0건 — 다운스트림 전달 구현 확인 필요** |
| Link 테이블 제외 필터 (invariant 5) | common / Agent Step | SourceToTargetStep 에 주석으로만 언급. **실제 필터 구현 위치 확인 필요** |

### 라운드 4 — SyncLog 규약 / Retention / Proxy 구조 / 패키지
| 규약 | 결과 |
|------|------|
| SyncLog per-mapping (invariant 4) | ✅ `SourceToTargetStep.saveSyncLogMapping()` / JSON 배열 / `sourcePkColumn` 필드까지 구비 |
| Retention 4계층 방어 (invariant 6) | ✅ 4 계층 모두 코드 확인: Frontend `InfoTab.tsx:1022 min={1}` + `Math.max(1,...)` / Orchestrator `AgentService:486-495` / Agent `DataRetentionController:116-118` / Service `DataRetentionService:42-43` |
| Proxy 헤더 전달 구조 (invariant 3) | ✅ 의도대로 — common `ExecutionDataController` 가 Proxy 에 자동 포함되어 Proxy 가 직접 관리 DB 조회 응답 (`bojo` 는 excludeFilters 로 제외) |
| 패키지 구조 Controller 레벨 (invariant 8) | ✅ 모든 Agent 가 `HealthController + PipelineController` 일관 보유 |

### 라운드 5 — UK / ddl-auto / profile / TZ / H2
| 규약 | 결과 |
|------|------|
| `source_refs` UK (invariant 2) | **PARTIAL FAIL** — bojo(6) + others(10) + provide(16) = 32 엔티티 적용, **bojo-int 전무 → VER-007** |
| 쓰기 = JdbcTemplate batch (invariant 2) | ✅ Step 내부 `saveAll/repository.save` 매치 0건 (SyncLog 쓰기만 예외, 관리 DB 기록이라 허용) |
| `ddl-auto` 상태 (invariant 11A) | 정보 확인 — 전 모듈 `update` (Proxy 2개는 `none`). **실배포 시 `validate` 치환 대상** (`config-replacement § F4` 관리) |
| `spring.profiles.active` 설정 | 매치 0건 — 전 모듈 기본 profile. 실배포 시 `SPRING_PROFILES_ACTIVE=prod` 주입 필요 (`config-replacement § F1`) |
| 타임존 명시 (invariant 11A) | 코드/yml 내 명시 없음. `OrchestratorClient:71` MySQL JDBC URL 에 `serverTimezone=Asia/Seoul` 만 있음. JVM 레벨 TZ 는 기동 스크립트에서 주입 예정 (`config-replacement § F2`) |
| H2 콘솔 활성화 (보안) | **FAIL** — bojo / api-collector 에 `h2.console.enabled=true` 하드코딩. 프로파일 분리 없음 → **VER-008** |

### 라운드 8 — 스케줄 / 엔티티 생성 전략 / HTTPS / Agent YAML 구조
| 규약 | 결과 |
|------|------|
| `@Scheduled` cron 환경변수 치환 | ✅ `DataRetentionScheduler.java:32` = `${retention.cron:0 0 2 * * *}` / `AgentHealthScheduler.java:58` = `fixedDelay=30000` (합리적 기본값, 튜닝 가능성은 있음) |
| `@GeneratedValue IDENTITY` 일관성 | ✅ 전 엔티티 IDENTITY 사용. MEMORY `읽기=JPA/쓰기=JdbcTemplate batch` 규약 트레이드오프 내 수용됨 |
| HTTPS / SSL 설정 | ⚠️ 전 모듈 `server.ssl` 매치 0건. **인프라 결정** (Reverse Proxy 처리 vs Spring SSL 직접) — `environment-setup.md § 2.3` TODO 와 연결 |
| Agent YAML 의 `source-datasource-id` / `target-datasource-id` | ⚠️ 이 키 이름으로 매치 0건. 실제 Agent YAML 의 DB 지정 키 구조 확인 필요 (후속 — `config/agents/*.yml` 샘플 1 개 Read) |

### 라운드 7 — 실배포 직결 (프로파일 / Docker / 로그설정 / Transactional)
| 규약 | 결과 |
|------|------|
| Spring profile 분리 파일 (`application-dev.yml` / `application-prod.yml`) | **PARTIAL** — `infolink-api-collector/application-dev.yml` 단 1 건. 다른 9 모듈은 단일 yml (환경변수 치환으로 운영 예정). 프로파일 분리가 필수는 아니나 현재 환경 전환 전략 혼재 — 결정 필요 |
| 모듈별 Dockerfile | **PARTIAL** — `infolink-api-collector/Dockerfile` 단 1 건. 다른 모듈 부재. 실배포 전략(컨테이너 vs 수동 JAR)이 미확정 상태 — `environment-setup.md § 4.1` TODO 와 연결 |
| `logback-spring.xml` 로그 설정 | **FAIL** — 전 모듈 0 건. Spring Boot 기본 로그만. 파일 저장/롤링/레벨 제어 전부 부재. → **VER-009** |
| Service `@Transactional` | orchestrator / collector / api-provider / common = 적용 / **Agent(sync-agent-bojo/bojo-int/others/provide)는 common 의 ExecutionService 외 부재**. Step 단위 트랜잭션 관리가 수동인지 프레임워크 레벨인지 코드 흐름 확인 필요 (후속) |
| `readOnly=true` | orchestrator / collector 적용 8건. Agent 조회 경로에도 있는지 추가 확인 필요 |

### 라운드 6 — 공통 헬퍼 / 상태 추적 / 패키지 7레이어
| 규약 | 결과 |
|------|------|
| `runningAgentCodes` Set + finally (MEMORY `모듈 간 일관성 규칙`) | ✅ 4 Agent 모두 적용 — bojo/bojo-int/others/provide PipelineService 에 일관 |
| ConditionOperator / ExecutionCondition 공통 헬퍼 (invariant 6) | ✅ common 에 헬퍼 존재 (`ConditionOperator.java`, `ExecutionCondition.java`) — 사용처별 실제 호출 여부는 미확인 |
| Link 테이블 SyncLog 명시 제외 필터 | grep 결과 **별도 필터 코드 없음**. 추정: Link 테이블이 `table-mappings` 밖이라 자연스럽게 SyncLog 기록 경로 밖. **후속 코드 흐름 확인 필요** (DefaultLoadStep / LoaderStepHelper / LinkTableUpdateStep 읽기) |
| 패키지 7레이어 (controller/dto/entity/repository/service/scheduler/config) 통일 | **PARTIAL** — Agent 모듈별 편차. `scheduler/` 는 sync-agent-* 에 없음 (Orchestrator 전용). `dto/` 는 bojo/common 만. `repository/` 는 bojo/bojo-int(loader) 만. 엄격 7레이어 규약보다 "필요한 레이어만" 접근. MEMORY `모듈 간 일관성 규칙 (3/24 확정)` 의 "레이어 기반 통일" 해석 상 약한 불일치. 규약 완화 또는 모든 Agent 에 형식적 디렉토리 추가 결정 필요 |

### 후속 확인 필요 (다음 사이클)
- [ ] Link 테이블(link_ngwis / tm_gd970101 / tm_gd980002) 이 SyncLog 에 기록되지 않는 실제 경로 — DefaultLoadStep / LoaderStepHelper / LinkTableUpdateStep 코드 흐름 읽기로 확정
- [ ] trace-source 3단계 분기 실제 구현 (`buildSourceFilter` 코드 흐름)
- [ ] 패키지 7레이어 규약 범위 재정의 (엄격 vs 필요시만) — MEMORY 업데이트 대상 후보
- [ ] Jasypt 기본 암호(`sync-pipeline-secret-key-2024`) — JASYPT_PASSWORD 환경변수 주입 계획 / 실배포 암호 재생성 계획 확인
- [ ] 로그 언어 한글 규약 (invariant 8) — 영어 로그 잔존 grep
- [ ] `@Transactional` / `readOnly=true` 일관성
