# 2026-04-28 검증 실행 기록

## 기준 커밋
`bd162e5` — feat(api-provider): Type B 커스텀 핸들러 17종 + 6종 1차 구현 (B14/B15/B6/B17/B16-DJ/B16-KB)

## 세션 맥락
- 4-23 첫 검증 사이클 이후 5일 만의 두 번째 사이클
- 사용자 지시: **"C 쪽 먼저, 실행 없이 확인 가능한 것"** → 최근/현재 작업 범위(api-provider Type B 핸들러 + provide 엔티티 누적 추가분) read-only 스캔
- 추가 사용자 질의: **"환경변수에 기본값 두지 말라는 게 스캔 범위에 있나"** → invariant 11A 미정의 확인 → TASK-002 발행

## 4-23 이후 변경 범위 (998d362..HEAD)

### 커밋 13건 요약
- 4-23: provide refactor + Type A A2/A3/A4/A7 이식 + verify 체계 신설
- 4-24: API Gateway 슬래시 operationId, frontend Columns 탭 UX, dev_logs/스크립트
- 4-27: Type B 17종 계획서 작성 + 6종 1차 구현
- 작업 트리(unstaged): B5 SupplementaryGroundwaterHandler 신규 + GroundwaterQualityHandler.java 수정 + B5_setup.sql DDL

### 파일 변경 stat (api-provider + provide 엔티티만)
- gims-api-provider Java: +2500 / -20 (29 파일)
- sync-agent-provide entity: +920 / -88 (18 파일)

## 스캔 항목 & 결과

### C1. Type B 커스텀 핸들러 (P9-api-provider)

| 항목 | 결과 | 비고 |
|------|:----:|------|
| SQL Injection 안전 | PASS | 모든 핸들러 PreparedStatement(`?`) + setLong/setString |
| Exception 메시지 한글 | PASS | "필수 파라미터 누락:..", "..숫자여야 합니다" 등 일관 |
| Lombok 줄당 1개 | PASS | 7 핸들러 + Registry/Catalog 일관 |
| 격리 패턴 (saveHistory try/catch) | PASS | invariant 11C 부분 충족 (호출이력 실패가 응답 영향 X) |
| Korean Javadoc | PASS | 풍부, 레거시 SQL 보존 양호 |
| `CustomHandlerBootstrap` 참조 (실재 X) | **FAIL** | **VER-010** — 스테일 Javadoc |
| `"internal"` datasource ID 산재 | **FAIL** | **VER-011** — 7 핸들러 + metadata default = 8지점 |
| 외부 Oracle 직결 — 장애 격리/재시도 | **PARTIAL** | **VER-012** — HikariCP timeout/retry/CB 미설정 |
| 운영자 등록 인터페이스 (Catalog/Preview/Register) | PASS | 부팅 자동등록 X 패턴 정착 |
| 핸들러 debug 로그 한글 | INCONCLUSIVE | 변수만 출력 — 4-23 run 후속 "영어 로그 잔존 grep" 통합 처리 권장 |

### C2. provide 엔티티 누적 추가분 (P7-provide)

| 항목 | 결과 | 비고 |
|------|:----:|------|
| `@Index(execution_id)` | **FAIL** | **VER-005 미진척** — 0/18 그대로. 본문 16→18 갱신 |
| `source_refs` UK | PASS | 18/18 (4-23 fd077f1 refactor 로 완성, 신규 2건도 동일 패턴) |
| `@Comment` | PASS | 18/18 일관 |

### C3. 기존 OPEN 이슈 재검증 (4-23 이후 진행도)

| ID | 4-23 시점 | 4-28 재검증 | 진척 |
|----|:--:|:--:|:----:|
| VER-001 (api-provider 외부화) | OPEN | OPEN — application.yml:10-12, 37 평문 그대로 | 0% |
| VER-005 (provide @Index) | OPEN | OPEN — 18/18 미적용. 신규 2건 추가됨 (본문 갱신함) | 0% (대상 +2) |
| VER-007 (bojo-int source_refs UK) | OPEN | 미재검증 (이번 스캔 범위 외) | — |
| 그 외 VER-002~009 | OPEN | 미재검증 (이번 스캔 범위 외) | — |

## 사용한 grep / 도구

```bash
# Type B 핸들러 datasource ID 산재 확인
grep -rn 'DATASOURCE_ID = "internal"' gims-api-provider/src/main/java/com/gims/provider/custom/

# Stale Bootstrap 참조 확인
grep -rn 'CustomHandlerBootstrap' gims-api-provider/

# provide 엔티티 @Index 전수
grep -l '@Index' sync-agent-provide/src/main/java/com/sync/agent/provide/entity/target/

# provide 엔티티 source_refs UK
grep -l 'UniqueConstraint' sync-agent-provide/src/main/java/com/sync/agent/provide/entity/target/

# 임시값 (Type B 핸들러 한정)
grep -rnE "(?i)\b(tmp|dummy|placeholder|fake|TODO|FIXME)\b" gims-api-provider/src/main/java/com/gims/provider/custom/
# → 매치 0 ✅

# api-provider localhost
grep -rn 'localhost' gims-api-provider/
# → application.yml:10, 37 + ApiKeyValidationService.java:22 (외부화된 default)
```

## 발견 이슈

- **[VER-010](../issues/OPEN/VER-010-custom-handler-stale-javadoc.md)** — `CustomOperationHandler.java:14` Javadoc 의 `CustomHandlerBootstrap` 참조 (실재 X). 카탈로그 등록 패턴 전환 시 잔존.
- **[VER-011](../issues/OPEN/VER-011-custom-handler-datasource-id-hardcoded.md)** — `"internal"` datasource ID 8지점 산재. 운영자 register 인터페이스에 datasourceId 변경 미제공. 새 핸들러 추가 시 누적.
- **[VER-012](../issues/OPEN/VER-012-type-b-external-oracle-fault-isolation.md)** — Type B 외부 GIMS Oracle 다이렉트 의존. HikariCP timeout/retry/CB 미설정. invariant 11C 부분 충족 — 운영 방어 + 모니터링 보강 필요.
- **VER-005 본문 갱신** — 대상 엔티티 16 → 18 (A4/A7 추가 반영).

## 파생 작업

- **[TASK-002](../tasks/OPEN/TASK-002-env-var-default-policy.md)** — 환경변수 dev 값 default 제거 + invariant 11A 강화. 사용자 우려 제기 → **동일 세션 내 옵션 B 채택 결정** ("저런식의 default를 피해야한다는 말이긴해"). verifier 가 영향 범위 사전 grep 박음 (yml 9건 + Java 11건 = 20건). VER-001 권장 패턴 default 제거로 갱신함.

## 정책 결정 — TASK-002 옵션 B 채택 (2026-04-28 사용자 결정)

### 결정 내용
- **dev 값 default 금지** (`${KEY:dev-default}` 패턴 제거)
- 환경변수 미주입 시 즉시 기동 실패 ("fail fast")
- "안전 default" (production-safe 값) 허용 여부는 forward 의 invariant 강화 dev_plan 시 결정

### 4-23 run 의 양호 처리 사례 재분류 (위반으로 전환)

본 결정에 따라 4-23 run 의 다음 항목들이 **양호 → 위반** 으로 재분류:

> 4-23 run §양호 확인된 사항:
> - "전 모듈(8 개) 중 7 개가 이미 Jasypt ENC + `${JASYPT_PASSWORD:sync-pipeline-secret-key-2024}` fallback 패턴 적용"
> - "Java 코드 대부분(H1~H7)이 `@Value("${env.name:default}")` 패턴 이미 적용 — 실배포 전환 가능"
> - "Jasypt 기본 암호(`sync-pipeline-secret-key-2024`)는 fallback, 실배포 시 `JASYPT_PASSWORD` 환경변수 주입으로 치환 가능"

→ 모두 **dev 값 default 사용으로 위반**. TASK-002 일괄 처리 대상.

### 영향 범위 (verifier 사전 grep, 2026-04-28)

| 카테고리 | 건수 | 위치 |
|---------|:--:|------|
| yml `${JASYPT_PASSWORD:sync-pipeline-secret-key-2024}` | 9 | 9 모듈 application.yml 전수 |
| Java `@Value("${agent.orchestrator-url:http://localhost:8080}")` | 7 | bojo / bojo-int / others / provide / proxy-dmz(2) / proxy-internal |
| Java `@Value("${app.api-key-validation.url:...mock...}")` | 1 | gims-api-provider |
| Java `@Value("${orchestrator.url:http://localhost:8080}")` | 1 | infolink-api-collector |
| Java `@Value("${anyang.api.fac-url|data-url:http://localhost:8084/mock/...}")` | 2 | infolink-api-collector |
| **합계** | **20** | (전수 스캔 시 yml lowercase/dotted 키 추가 발견 가능) |

상세 위치는 TASK-002 본문 참조.

## 관찰 (이슈 X — 사용자 알림용)

- **`GroundwaterQualityHandler.java` unstaged 라인 23 주석 오타** — `SELECT DISTINCT` → `SELECT DISTINCTd` (`d` 우발적 키 입력 추정). 본 SQL(string concat) 에는 영향 없음. forward 진행 중인 파일이라 verifier 가 건드리지 않음.
- **Type B 진행 7/17** — 6종 커밋 (B14/B15/B6/B17/B16-DJ/B16-KB) + B5 unstaged. 미구현 10종 (B4/B7/B8/B9/B10/B11/B12-DJ/B12-KB/B13/B18) — forward 진행 영역.
- **새 DDL 8종** (`scripts/ddl/internal-oracle/provide-source/`) 컬럼/주석 일관성은 본 사이클 미스캔 — 다음 사이클 후보.

## 파트 겹침 판단 (핸드오프용)

- **VER-010** (P9, Javadoc 1줄 갱신) — 병렬 안전, 우선순위 낮음
- **VER-011** (P9, datasource ID 정책 + register 인터페이스 변경) — VER-012 와 같은 파트라 묶어서 처리 효율적
- **VER-012** (P9, ProviderDataSourceService HikariConfig + 모니터링) — 운영 방어 보강. forward 가 완료한 후 별도 운영 가이드 보강
- **TASK-002** (P0-meta, invariant 정책 결정) — 정책 결정 → 전수 스캔 → yml/Java 변경 (옵션 A/B 선택 시). 사용자 결정 우선

## 후속 작업 (다음 세션 대기 목록)

- [ ] **사용자 결정 대기**: TASK-002 옵션 A/B/C 선택
- [ ] VER-001 / VER-002 / VER-003 / VER-004 / VER-006 / VER-007 / VER-008 / VER-009 재검증 (4-23 이후 진행도 확인)
- [ ] 4-23 run 후속 항목 (Link 테이블 SyncLog 제외 코드 흐름 / buildSourceFilter 3단계 분기 / 영어 로그 grep / @Transactional 일관성)
- [ ] 새 DDL 8종 (`scripts/ddl/internal-oracle/provide-source/`) 컬럼/주석 일관성 스캔
- [ ] Type B 핸들러 미구현 10종 (B4/B7/B8/B9/B10/B11/B12-DJ/B12-KB/B13/B18) 추가 시 본 사이클 동일 항목 재스캔
- [ ] 통합 invariant 세부 파일 `no-hardcoded-env-values.md` 작성 (TASK-002 결정 후)

## 양호 확인 (이번 사이클)

- Type B 핸들러 SQL Injection 안전 (PreparedStatement 일관)
- Type B 한글 Exception 메시지 일관
- provide 18 엔티티 source_refs UK 100% 통일 (4-23 fd077f1 refactor 결과)
- ApiGatewayController 호출이력 격리 패턴 (saveHistory try/catch)
- 운영자 의도 반영 등록 (Catalog→Preview→Register) — 부팅 자동등록 회피 정착
