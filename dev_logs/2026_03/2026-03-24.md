# 2026-03-24 작업 일지

## 1. API키 참조 방식 리팩토링
- **목적**: `description.startsWith("🔑")` 이모지 기반 분기 → `isApiKeyRef` boolean 플래그로 변경
- **원인**: 등록 단계 인라인 테스트 시 description 미전달 → API키 resolve 안 됨 → 401
- **수정 파일**:
  - `ApiParam.java` — `isApiKeyRef` boolean 컬럼 추가
  - `ApiCallService.java` — 분기 조건 변경
  - `TestCallDto.java` — InlineParam에 `isApiKeyRef` 추가
  - `ApiTestService.java` — 인라인 빌드 시 `isApiKeyRef` 세팅
  - `ApiEndpointDto.java` — ParamRequest/ParamResponse에 추가
  - `ApiEndpointService.java` — 저장 시 반영
  - `types/api-collect.ts`, `collectorApi.ts`, `page.tsx`, `InfoTab.tsx` — 프론트 전체 교체

## 2. 실행 이력 페이징 + 날짜 검색
- **목적**: 이전/다음만 있던 페이징을 페이지 번호 방식으로 개선, 날짜 범위 검색 추가
- **수정 파일**:
  - `ApiExecutionHistoryRepository.java` — `startedAt BETWEEN` 쿼리 추가
  - `ApiHistoryController.java` — `startDate`/`endDate` 파라미터 추가
  - `collectorApi.ts` — 날짜 파라미터 전달
  - `HistoryTab.tsx` — 날짜 input + 페이지 번호 버튼(최대 5개) + 총 건수 표시

## 3. ScheduleExecutor 통일
- **목적**: Orchestrator ↔ API Collector 간 스케줄 실행기 패턴 통일
- **변경**:
  - `@PostConstruct` → `@EventListener(ContextRefreshedEvent)` (순환참조 방지)
  - `@Lazy` 생성자 → `@RequiredArgsConstructor`
  - `@PreDestroy destroy()` 삭제 (불필요)
  - 메서드명 통일: `registerSchedule`/`unregisterSchedule`/`getActiveScheduleCount`
  - 로그 한글화
- **수정 파일**:
  - `ScheduleExecutor.java` (orchestrator)
  - `ScheduleService.java` (orchestrator) — `cancelSchedule` → `unregisterSchedule`
  - `ApiScheduleExecutor.java` (api-collector) — 메서드명 통일
  - `ApiScheduleService.java` (api-collector)

## 4. 모듈 간 코드 일관성 정비
### 4-1. Internal Agent 누락 기능 추가 (#2,#3,#4)
- `PipelineService.java` (bojo-int) — `runningAgentCodes` Set + finally 블록 추가
- `HealthController.java` (bojo-int) — `loaderAgents`, `runningAgents` 반환 추가

### 4-2. 로그/예외 한글 통일 (#1,#5)
- `sync-agent-bojo` — PipelineController, PipelineService, SyncDataSourceService 한글화
- `sync-agent-bojo-int` — PipelineController, PipelineService, SyncDataSourceService 외 8개 파일 한글화
- `sync-orchestrator/backend` — AgentService, ExecutionService, ScheduleService 한글화

### 4-3. Agent PipelineController Map → DTO (#6)
- `PipelineDto.java` (common) — 응답 DTO 신규 생성
- `PipelineController.java` (bojo, bojo-int) — Map 응답 → DTO 교체

### 4-4. Lombok 스타일 통일 (#9)
- API Collector Entity 5개 + DTO 4개 — 한 줄 여러 개 → 줄당 1개

## 5. 테스트 결과
- 전체 빌드: common, bojo, bojo-int, orchestrator, api-collector, frontend 모두 성공
- 스케줄 실행: `@EventListener` 초기화 정상, cron 트리거 → E2E 파이프라인 SUCCESS
- PipelineController DTO: Orchestrator → Agent 연동 정상

## 6. 메모리 정리
- `MEMORY.md`에서 "미완료 작업 — Loader 지역별 모드" 제거 (where 기능으로 대체됨)
