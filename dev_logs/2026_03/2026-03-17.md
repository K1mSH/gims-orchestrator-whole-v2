# 2026-03-17 작업 일지

## 작업 내용

### 1. 보안: 자격증명 암호문 통신으로 전환 (Part A)
Orchestrator connection-info API가 평문 자격증명을 응답하던 문제를 수정.
이제 **암호문(ENC) 그대로 전달** → 수신측이 `PasswordEncryptor`로 각자 복호화.

**CredentialEncryptor 삭제, PasswordEncryptor(sync-agent-common)로 통합**
- Orchestrator: CredentialEncryptor → PasswordEncryptor 교체, Bean 등록, JAR 의존성 추가
- Proxy DMZ/Internal: PasswordEncryptor 추가, 복호화 로직 적용
- Agent bojo/bojo-int: SyncDataSourceService에 PasswordEncryptor 추가

**수정 파일 (6개 모듈)**
- `sync-orchestrator/backend/build.gradle` — common JAR 의존성 추가
- `sync-orchestrator/backend/libs/sync-agent-common-*.jar` — 신규 복사
- `sync-orchestrator/backend/.../config/WebConfig.java` — PasswordEncryptor Bean
- `sync-orchestrator/backend/.../common/CredentialEncryptor.java` — 삭제
- `sync-orchestrator/backend/.../datasource/DatasourceService.java` — 암호문 그대로 응답
- `sync-orchestrator/backend/.../agent/AgentService.java` — 암호문 전달
- `sync-orchestrator/backend/.../execution/ExecutionService.java` — import 변경
- `sync-proxy-dmz/.../ProxyDataSourceService.java` — 복호화 추가
- `sync-proxy-internal/.../ProxyDataSourceService.java` — 복호화 추가
- `sync-agent-bojo/.../SyncDataSourceService.java` — 복호화 추가
- `sync-agent-bojo-int/.../SyncDataSourceService.java` — 복호화 추가

### 2. api-collector 외부 Datasource 연동 (Part B)
API 수집 결과를 Orchestrator 등록 외부 DB에 적재할 수 있도록 연동.

**백엔드**
- `OrchestratorClient.java` — 신규: Orchestrator connection-info 조회 + 복호화
- `DynamicDataSourceService.java` — 신규: HikariCP 동적 DataSource 관리 (캐싱)
- `ApiExecutionService.java` — targetDatasourceId 기반 DataSource 분기
- `MetadataController.java` — 삭제 (프론트에서 Orchestrator API 직접 호출)
- `application.yml` — orchestrator URL 설정 추가, 자격증명 ENC() 암호화

**프론트엔드 (MappingTab)**
- `metadataApi` 제거 → `datasourceApi` (Orchestrator API) 사용
- Datasource 드롭다운 → 테이블 드롭다운 → 컬럼 드롭다운 연동

### 3. 프론트엔드 UI 개선

**등록 페이지 (`/api-collect`) 전면 개편**
- 섹션별 구분: 요청 / 헤더 / 인증 / 기타 / 파라미터
- Content-Type 드롭다운, 커스텀 헤더 key-value 행 입력
- 인증 유형별 조건부 필드 (BASIC → JSON, BEARER → 토큰)
- **테스트 선행 필수**: 테스트 성공 전까지 등록 버튼 비활성
- 테스트 결과 카드: 요청 정보(method/URL/파라미터/헤더) + 응답(상태/트리)

**상세 InfoTab 동일 구조 적용**
- 섹션별 레이아웃, Content-Type, 헤더 key-value, 인증

### 4. 인라인 테스트 API (신규)
DB 저장 없이 등록 전 테스트할 수 있는 API 추가.

- `POST /api/endpoints/test-inline` — URL, method, headers, auth, params를 body로 받아 호출
- `TestCallDto.InlineRequest`, `TestCallDto.InlineParam` DTO 추가
- `ApiTestService.testCallInline()` — 임시 ApiEndpoint/ApiParam 객체로 호출
- 프론트: `testApi.callInline()` — 등록 전 폼 데이터로 직접 테스트

### 5. Jasypt 암/복호화 도구
- `scripts/encrypt.sh` + `scripts/encrypt.java`
- 단건: `./encrypt.sh encrypt "평문"` / `./encrypt.sh decrypt "ENC(...)"`
- 대화형: `./encrypt.sh` → `e 값` / `d 값` / `q`

### 6. api-collector application.yml 암호화
- datasource url/username/password → ENC() 변환
- orchestrator url → ENC() 변환
- jasypt key: `${JASYPT_PASSWORD:sync-pipeline-secret-key-2024}` (전 모듈 통일)

## 빌드 결과
- sync-orchestrator/backend: OK
- sync-proxy-dmz: OK
- sync-proxy-internal: OK
- sync-agent-bojo: OK
- sync-agent-bojo-int: OK
- infolink-api-collector: OK
- frontend tsc --noEmit: OK

## 계획 문서
- `dev_plan/2026_03/17/api-collector-external-datasource.md`

## 미완료 테스트 (전부 미검증)

### A. 암호문 통신 (Part A)
- [ ] Orchestrator 재시작 후 정상 기동 (PasswordEncryptor Bean 로딩)
- [ ] `GET /api/datasources/{id}/connection-info` 응답이 ENC() 형태인지 확인
- [ ] Proxy DMZ가 connection-info 받아서 복호화 → DataSource 정상 생성
- [ ] Proxy Internal 동일 확인
- [ ] Agent bojo가 connection-info 받아서 복호화 → 외부 DB 연결 성공
- [ ] Agent bojo-int 동일 확인
- [ ] 기존 파이프라인(RCV→Loader→SND) 정상 동작 (암호화 변경이 기존 흐름 깨뜨리지 않는지)

### B. api-collector 외부 Datasource 연동 (Part B)
- [ ] api-collector → Orchestrator connection-info 호출 성공
- [ ] DynamicDataSourceService: 외부 DB DataSource 생성 + 캐싱 동작
- [ ] ApiExecutionService: targetDatasourceId 있을 때 외부 DB에 INSERT
- [ ] ApiExecutionService: targetDatasourceId 없을 때 자체 DB fallback

### C. 프론트엔드 등록 페이지 UI
- [ ] Content-Type 드롭다운 동작
- [ ] 헤더 key-value 행 추가/삭제/입력
- [ ] 인증 유형 전환 시 조건부 필드 표시 (NONE→BASIC→BEARER)
- [ ] 파라미터 추가/삭제, STATIC↔DYNAMIC 전환
- [ ] 테스트 호출 → 요청정보(method/URL/파라미터/헤더) + 응답 트리 표시
- [ ] 테스트 실패 시 등록 버튼 비활성 유지
- [ ] 테스트 성공 후 등록 버튼 활성화 → 등록 → "상세(매핑)→" 이동

### D. 프론트엔드 상세 페이지 (InfoTab)
- [ ] 기존 데이터 로드 시 헤더 key-value 파싱 표시
- [ ] 헤더 수정 → 저장 → JSON 변환 정상
- [ ] Content-Type 변경 저장

### E. 프론트엔드 상세 페이지 (MappingTab)
- [ ] Datasource 드롭다운에 Orchestrator 등록 DB 목록 표시
- [ ] Datasource 선택 → 테이블 목록 로드
- [ ] 테이블 선택 → 컬럼 목록 로드 (columnName/dataType/isPrimaryKey)
- [ ] 매핑 저장 → 수동 실행 → 외부 DB에 데이터 적재 확인

### F. 인라인 테스트 API
- [ ] `POST /api/endpoints/test-inline` — 파라미터 없는 단순 GET
- [ ] 인라인 테스트 — QUERY 파라미터 + 인증(BEARER) 포함
- [ ] 인라인 테스트 — POST + BODY 파라미터 + Content-Type
- [ ] 인라인 테스트 — 커스텀 헤더 전달 확인
- [ ] 인라인 테스트 — 잘못된 URL 시 FAILED 응답

### G. E2E 통합 (전체 플로우)
- [ ] 실제 공공 API로: 등록 페이지 테스트 → 등록 → 데이터 루트 선택 → 외부 DB 매핑 → 수동 실행 → 적재 확인
- [ ] Schedule 등록 → cron 실행 확인

### H. Docker 배포
- [ ] api-collector Dockerfile 작성 (또는 기존 확인)
- [ ] api-collector 이미지 빌드 + 컨테이너 기동
- [ ] Docker 환경에서 JASYPT_PASSWORD 환경변수 주입 확인
- [ ] Docker 컨테이너 → Orchestrator(8080) 통신 정상
- [ ] Docker 컨테이너 → PostgreSQL(29001) 통신 정상
- [ ] docker-compose에 api-collector 서비스 추가 (기존 compose 있을 경우)
- [ ] Orchestrator/Proxy/Agent 포함 전체 서비스 docker-compose up 정상 기동

## TODO (다음 작업)
- 위 테스트 항목 순서대로 검증
- 실패 시 수정 후 재테스트
- dev_log에 테스트 결과 기록
