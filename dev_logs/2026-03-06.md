# 2026-03-06 작업 일지

## 완료 작업 1: LINK 타입 모니터링/표출에서 제외

### 목적
LINK 타입(link_ngwis, tm_gd980002)은 데이터 처리 로직에서는 유지하되,
프론트엔드 표출 및 로그/모니터링 집계에서는 제외.

### 수정 파일
1. **`sync-agent-common/.../repository/SyncLogRepository.java`**
   - `sumCountsByExecutionIdExcludeLink()` 쿼리 추가 (LINK 타입 제외 집계)

2. **`sync-agent-common/.../controller/ExecutionDataController.java`**
   - `/summary`: LINK 제외 집계 쿼리 사용
   - `/tables`: `tableType == "LINK"` 항목 skip (continue)

3. **프론트엔드**: 기존에 이미 LINK 필터링 적용됨 (확인만)

### 빌드/배포
- sync-agent-common 빌드 → 4개 모듈에 JAR 복사 (bojo, bojo-int, proxy-dmz, proxy-internal)
- proxy-dmz(8083), proxy-internal(8093) 재빌드/재시작

---

## 완료 작업 2: InternalLoadStep RESYNC 지원

### 목적
시간범위 실행 시 Internal RCV가 link_status='RESYNC'로 IF_RSV에 기록하는데,
InternalLoadStep이 'PENDING'만 읽어서 Loader가 0건 처리되는 문제 수정.

### 수정 파일
- **`sync-agent-bojo-int/.../loader/step/InternalLoadStep.java`** (line 142)
  - `WHERE link_status = 'PENDING'` → `WHERE link_status IN ('PENDING', 'RESYNC')`

### 빌드/검증
- sync-agent-bojo-int 빌드 → Internal Agent(8092) 재시작
- Internal Loader 실행: **4555건 read/write 성공**

---

## 완료 작업 3: Retention 비활성화

### 목적
retentionDays=1 설정으로 pm_gd970201 데이터가 삭제되는 문제 방지.

### 변경
- `PUT /api/agents/20/retention` → `enabled: false`
- 코드 변경 없음 (DB 설정 변경)

---

## 완료 작업 4: 추적 기능 E2E 테스트

### 전체 파이프라인 데이터 투입
| 단계 | Agent | 건수 | 상태 |
|------|-------|------|------|
| DMZ RCV | dmz-bojo-rcv-daejeon | jewon 402 + obsvdata 4957 | SUCCESS |
| DMZ Loader | dmz-bojo-loader | 5359 | SUCCESS |
| DMZ SND | dmz-bojo-snd | 8963 | SUCCESS |
| Internal RCV | internal-bojo-rcv | 12567 (시간범위) | SUCCESS |
| Internal Loader | internal-bojo-loader | 4555 | SUCCESS |

### 추적 테스트 결과
| 테스트 | 경로 | 결과 |
|--------|------|------|
| DMZ RCV Forward | sec_jewon_view(401) → if_rsv_sec_jewon | SYNCED (1건) |
| DMZ RCV Reverse | if_rsv_sec_jewon → sec_jewon_view(401) | FOUND (1건) |
| Internal Loader Forward | if_rsv_sec_obsvdata(1) → pm_gd970201 | SYNCED (5건) |
| Internal Loader Reverse | pm_gd970201 → if_rsv_sec_obsvdata(19846) | FOUND (1건) |
| Orchestrator Proxy Forward | 8080 → 8093 프록시 | SYNCED |
| Orchestrator Proxy Reverse | 8080 → 8093 프록시 | FOUND |

### 결론
추적 기능 정상 동작 확인. 문제의 원인은:
1. retention이 테스트 데이터를 삭제함
2. InternalLoadStep이 RESYNC 상태를 읽지 못함
코드 자체에는 추적 로직 문제 없었음.

---

## 작업 5: Forward Trace 로직 source_refs 기반 재설계

### 목적
Internal Loader forward trace 시 다른 source PK 레코드가 섞이는 문제 해결.
이름 기반 테이블 매칭 → source_refs 기반 3단계 로직으로 교체.

### 수정 파일
- **`sync-agent-common/.../controller/ExecutionDataController.java`**
  - `traceBySourcePk()` 본문 교체 (이름 기반 4단계 → source_refs 기반 3단계)
  - `traceByBusinessKey()` 삭제
  - `extractAgentCode()` 삭제

### 신규 로직
- Step 1: source_refs에 PK 임베딩 검색 (Pattern A 정밀, Pattern B 범용)
- Step 2: source_refs 값 일치 (Loader 복사 패턴)
- Step 3: 직접 PK fallback

### 빌드/배포
- sync-agent-common 빌드 성공, JAR 4개 모듈 복사 완료
- 서비스 재시작 완료 (8080, 8082, 8092, 3000)

### 현재 상태
- ⚠️ 추적 기능 아직 문제 있음 — 추가 디버깅 필요
- 계획 문서: `dev_plan/2026-03-06/forward-trace-source-refs-redesign.md`

### TODO (다음 작업)
- forward trace curl 테스트로 어떤 Step에서 실패하는지 확인
- 5개 파이프라인 단계별 검증
- Internal Loader source 추적 혼합결과 해소 확인
