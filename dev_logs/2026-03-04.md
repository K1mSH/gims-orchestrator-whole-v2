# 2026-03-04 작업 일지

## 작업 목표
내부망 Loader (internal-bojo-loader) 실행 상세의 데이터 추적 기능 완성 및 테이블 처리 현황 개선

## 완료 작업

### 1. Source PK 매칭 수정 (내부 RCV)
- **문제**: `if_snd_sec_obsvdata`(SOURCE)와 `if_rsv_sec_obsvdata`(IF)의 이름 매칭 실패
- **수정**: `ExecutionDataController.getSourcePksFromIfTable()`에 `stripIfPrefix()` 메서드 추가
  - IF 접두사(`if_rsv_`, `if_snd_`, `if_loader_`) 제거 후 코어 테이블명으로 매칭

### 2. LINK 테이블 SyncLog 기록 + 처리현황 표시
- `LinkTableUpdateStep` (bojo): SyncLog에 tableType="LINK"로 link_ngwis 건수 기록
- `InternalLoadStep` (bojo-int): SyncLog에 tableType="LINK"로 tm_gd980002 건수 기록
- Frontend: LINK 타입 보라색 표시, [상세] 버튼 비활성화

### 3. 총건수 SyncLog 기반 집계로 변경
- **문제**: Execution.totalWriteCount가 EAV 확장된 수(29,742)로 테이블 처리현황 합계(10,718)와 불일치
- **수정**: Frontend에서 SyncLog 기반 집계 사용 (SOURCE=읽기, 나머지=쓰기)

### 4. pm_gd970201 추적 지원 (execution_id + source_refs)
- DDL: pm_gd970201에 `execution_id`, `source_refs` 컬럼 추가
- `GimsTargetRepository.batchInsertObsvdata()`: INSERT SQL에 6개 파라미터로 확장
- `InternalLoadStep`: EAV 확장 시 executionId, sourceRef(`["I:internal:if_rsv_sec_obsvdata:{id}"]`) 전달

### 5. 정방향 추적 폴백 (SOURCE → TARGET)
- **문제**: `/trace` 엔드포인트에서 `if_rsv_sec_obsvdata` → `pm_gd970201` 이름 매칭 실패
- **수정**: 이름 매칭 실패 시 SyncLog TARGET 테이블에서 `source_refs LIKE '%:sourceTable:pkValue%'` 쿼리로 직접 검색
- 결과가 있으면 테이블 해석 + 데이터 조회를 한번에 반환

### 6. 역추적 수정 2건
- **테이블명 해석**: `parseSourceRefsTableName()` 추가 — source_refs의 `I:dsId:tableName:pk` 형식에서 테이블명 직접 추출
- **행 침범 방지**: Frontend rowPk를 source_refs pk → 행 자체 PK(첫 번째 컬럼)로 변경
  - N:1 관계(3개 pm_gd970201 행 → 1개 IF 행)에서 동시 확장 방지

## 수정 파일 목록

### sync-agent-common
- `controller/ExecutionDataController.java` — stripIfPrefix(), source_refs 폴백, parseSourceRefsTableName()

### sync-agent-bojo
- `rcv/step/LinkTableUpdateStep.java` — LINK SyncLog 기록
- `config/RcvPipelineConfig.java` — LinkTableUpdateStep 생성자에 syncLogRepository 추가

### sync-agent-bojo-int
- `loader/step/InternalLoadStep.java` — LINK SyncLog 기록, expandedRows에 executionId/sourceRef 추가
- `loader/repository/GimsTargetRepository.java` — INSERT SQL에 execution_id, source_refs 추가

### sync-orchestrator/frontend
- `app/executions/[id]/page.tsx` — LINK 타입 표시, rowPk 유니크화, 총건수 SyncLog 집계
- `app/globals.css` — purple CSS 변수 추가
- `types/index.ts` — TableStats에 'LINK' 타입 추가

### 기타
- `scripts/gims-target-ddl.sql` — pm_gd970201 DDL 반영

## API 테스트 결과
- 정방향: `if_rsv_sec_obsvdata:18942` → `pm_gd970201` 3건(gwdep, gwtemp, ec) 정상 반환
- 역추적: `pm_gd970201` → source_refs 파싱 → `if_rsv_sec_obsvdata:18942` 정상 반환

## 다음 작업 TODO
- [ ] LINK type 표시 롤백 (처리현황에서 제거 검토)
- [ ] 프론트엔드에서 실제 브라우저 동작 확인 (행 침범 해결 여부)
