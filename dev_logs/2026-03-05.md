# 2026-03-05 작업 일지

## 완료 작업: 테이블 이름 옆에 한글 설명(alias) 자동 표시

### A. 테이블 등록 시 remarks → alias 자동 매핑
- **파일**: `sync-orchestrator/frontend/app/datasources/page.tsx`
- DB COMMENT(remarks)를 tableAlias/columnAlias로 자동 전달되도록 수정

### B. 모든 화면에서 테이블명 옆에 (alias) 표시
- **`frontend/components/agent/InfoTab.tsx`** — 소스/타겟 체크박스 2곳 + 목록 2곳 (총 4곳)
- **`frontend/app/agents/page.tsx`** — 소스/타겟 체크박스 2곳
- **`frontend/app/executions/[id]/page.tsx`** — 테이블 통계 + 선택 테이블 헤더 2곳 (alias-map API 사용)
- **`frontend/app/datasources/page.tsx`** — 등록된 테이블 목록 1곳

### C. 백엔드 API 추가
- **`backend/.../datasource/DatasourceController.java`** — `GET /api/datasources/table-alias-map` 엔드포인트 추가
- **`backend/.../datasource/DatasourceService.java`** — `getTableAliasMap()` 메서드 추가
- **`frontend/lib/api.ts`** — `datasourceApi.getTableAliasMap()` 함수 추가

### D. 기존 데이터 보정
- DB에 SQL UPDATE로 33개 테이블의 `table_alias` 컬럼 채워넣기 완료

### 빌드/검증
- 백엔드 빌드 통과
- 프론트엔드 타입체크(`npx tsc --noEmit`) 통과
- `GET /api/datasources/table-alias-map` API 정상 응답 확인

---

## 미완료: 추적(Trace) 기능 확인

### 상황
- 실행 상세 페이지에서 테이블별 처리현황 → 데이터가 안 뜨는 현상 발견
- **원인**: retention 삭제 로직이 최근 데이터(pm_gd970201 등)를 삭제한 것으로 확인
- **코드 문제 아님** — 데이터 문제

### 수행한 작업
- ExecutionDataController.java, ExecutionService.java를 잘못 수정했다가 **되돌림 완료** (git checkout)
- sync-agent-common 리빌드 + 4개 모듈에 JAR 복사 완료
- 서비스 재시작(8080, 8083, 8093) 완료

### 다음 작업 (TODO)
1. 기존 문서(ARCHITECTURE.md 등) 정독하여 실행 → 콜백 → 이력 기록 흐름 재확인
2. 새 실행 데이터 넣기 (전체 파이프라인: DMZ RCV → Loader → SND → Internal RCV → Loader)
3. 실행 후 추적 기능이 정상 동작하는지 검증
4. 문제가 있으면 그때 원인 분석 후 코드 수정
