# 2026-05-07 작업 일지

## 요약 (보고용)

### 파트별 진행도

| 파트 | 상태 | 비고 |
|------|:----:|------|
| **서비스 이름 정리 결정** (sync-/gims- → infolink-, frontend 별 모듈 분리, -int → -internal, -dmz suffix 명시, com.* 패키지 통일) | **완료** | `dev_plan/2026_05/07/service-naming.md` 매핑표 12 모듈 + 결정 원칙 4가지 |
| **Phase 1 — 디렉토리/이름 외피** | **완료** | 11 디렉토리 git mv + 8 settings.gradle + 9 application.yml + 9 build.gradle libs/ + frontend package.json + sync-orchestrator/ 우산 해체 |
| **Phase 2 — Java 패키지 root** | **완료** | 12 디렉토리 이동 (com/sync, com/gims → com/infolink, bojoint→bojo 통합) + 모든 .java sed + 11 build.gradle group + AutoConfiguration.imports + auth yml/build.gradle FQCN. 11 모듈 BUILD SUCCESSFUL |
| **Phase 3 — 통합 기동 + smoke test** | **완료** | 7 서비스 health 7/7 + JWT 로그인 + backend/provider/collector + B4 핸들러 회귀 6/6 PASS |
| **Phase 4 — 문서 일괄 갱신** | **완료** | CLAUDE.md (root) + docs/ + verify/ + test_plan/ + todo/ + nexus-dependencies.md + memory/ + claude-memory/ 일괄 sed (dev_logs/dev_plan 과거 보존) |
| **Frontend type 에러 4건 정리** | **완료** | 5/6 부터 잔존하던 type 에러 — `DiscoverResponse.agentInfo`, `TraceResult.traceStatus = NOT_TRACKABLE` 추가 |

---

## 오전 — 서비스 이름 정리 결정 + Phase 1 + Phase 2

### 1. 결정 (사용자 5/7)

| 영역 | 결정 |
|---|---|
| Prefix 통일 | `sync-` / `gims-` 혼재 → `infolink-` 단일 |
| Frontend 분리 | `sync-orchestrator/{backend,frontend}` 우산 해체 → `infolink-orchestrator-backend/` + `infolink-orchestrator-frontend/` 형제 |
| `-int` → `-internal` | `bojo-int` → `bojo-internal`. proxy-internal 과 대칭 |
| DMZ suffix 명시 | bojo/others/provide → `-dmz` 추가 (proxy-dmz 와 대칭) |
| Auth 단순화 | `sync-orchestrator-auth` → `infolink-auth` (orchestrator 산하 X) |
| Java 패키지 root | `com.sync.*` / `com.gims.*` → `com.infolink.*` 통일 |
| bojo-internal 패키지 | `com.infolink.agent.bojo` (DMZ와 통합 — JAR 별 빌드라 충돌 없음) |
| 작업 브랜치 | `refactor/rename-modules` 분리 |

### 2. Phase 1 — 디렉토리/이름 외피 (자바 패키지 미변경)

#### 디렉토리 매핑 (11개 rename + 1개 그대로)
| 기존 | → | 새 |
|---|:-:|---|
| sync-orchestrator/backend | → | infolink-orchestrator-backend |
| sync-orchestrator/frontend | → | infolink-orchestrator-frontend |
| sync-orchestrator-auth | → | infolink-auth |
| sync-agent-common | → | infolink-agent-common |
| sync-agent-bojo | → | infolink-agent-bojo-dmz |
| sync-agent-bojo-int | → | infolink-agent-bojo-internal |
| sync-agent-others | → | infolink-agent-others-dmz |
| sync-agent-provide | → | infolink-agent-provide-dmz |
| sync-proxy-dmz | → | infolink-proxy-dmz |
| sync-proxy-internal | → | infolink-proxy-internal |
| gims-api-provider | → | infolink-api-provider |
| infolink-api-collector | → | (그대로) |

#### 부속 갱신
- 8 `settings.gradle` `rootProject.name`
- 9 `application.yml` `spring.application.name`
- 9 `build.gradle` `implementation files('libs/...jar')` 경로
- `infolink-agent-common/build.gradle` publishing `groupId='com.infolink'` + `artifactId='infolink-agent-common'`
- `infolink-orchestrator-frontend/package.json` + `package-lock.json` `name`
- 옛 우산 `sync-orchestrator/` 디렉토리 해체 (.idea untracked 만 남음)

#### 검증
- yml/gradle 안 잔존 sync-/gims- 패턴 0건 grep
- 디렉토리 12개 정합 ls
- 빌드는 패키지 미변경 상태라 일부러 안 함 (Phase 2 끝에 검증)

### 3. Phase 2 — Java 패키지 root (com.sync./com.gims. → com.infolink.)

#### 패키지 root 매핑
| 현재 root | 사용 모듈 | → | 새 root |
|---|---|:-:|---|
| `com.gims.auth` | infolink-auth | → | `com.infolink.auth` |
| `com.gims.provider` | infolink-api-provider | → | `com.infolink.provider` |
| `com.infolink.collector` | infolink-api-collector | → | (그대로) |
| `com.sync.agent.{bojo, bojoint, others, provide, common}` | 5 agent 모듈 | → | `com.infolink.agent.{bojo, others, provide, common}` (bojoint→bojo 통합) |
| `com.sync.orchestrator` | infolink-orchestrator-backend | → | `com.infolink.orchestrator` |
| `com.sync.proxy` | infolink-proxy-dmz, internal | → | `com.infolink.proxy` |

#### 작업 (sed 일괄)
1. **디렉토리 이동** 13건 git mv:
   - bojo-internal: `com/sync/agent/bojoint` → `com/sync/agent/bojo` → `com/infolink/agent/bojo`
   - 나머지 모듈: `com/sync` 또는 `com/gims` → `com/infolink` (test/ 있는 모듈 = common, auth)
2. **.java 파일 sed**:
   - `com.sync.agent.bojoint` → `com.infolink.agent.bojo` (먼저 — bojo-internal 만 영향)
   - `com.sync.` → `com.infolink.`
   - `com.gims.` → `com.infolink.`
3. **build.gradle group** 일괄 sed: `'com.sync'`/`'com.gims'` → `'com.infolink'` (10 모듈)
4. **AutoConfiguration.imports** (infolink-agent-common): FQCN `com.infolink.agent.common.auth.AuthVerifierAutoConfiguration`
5. **auth/build.gradle** mainClass + **auth/api-provider yml** logging level FQCN
6. **agent-common JAR 재빌드** + 9 검증자 모듈 libs/ 옛 sync-agent-common-*.jar 삭제 + 새 `infolink-agent-common-1.0.0-SNAPSHOT.jar` 복사

#### 검증
- 모든 .java 잔존 com.sync./com.gims. 0건 grep
- `infolink-agent-common` 빌드: BUILD SUCCESSFUL in 17s
- **10 검증자 모듈 BUILD SUCCESSFUL** (auth 6s / backend 7s / collector 7s / provider 5s / bojo-dmz 5s / bojo-internal 9s / others-dmz 5s / provide-dmz 6s / proxy-dmz 3s / proxy-internal 4s)

### 4. Frontend type 에러 4건 정리 (5/6 잔존)

| 에러 | fix |
|---|---|
| `agents/page.tsx:326` `DiscoverResponse.agentInfo` 누락 | `types/index.ts` 의 `DiscoverResponse` 에 `agentInfo?: Array<{ agentCode: string; [key: string]: unknown }>` 추가 |
| `executions/[id]/page.tsx:817~822` `traceStatus === 'NOT_TRACKABLE'` 비교 type union 미스매치 | `lib/api.ts` 의 `TraceResult.traceStatus` union 에 `'NOT_TRACKABLE'` 추가 |

`npx tsc --noEmit` → 에러 0 PASS.

---

## 오후 — Phase 3 (통합 기동 + smoke test) + Phase 4 (문서 갱신)

### Phase 3 — 통합 기동 + smoke test

7 서비스 + frontend 백그라운드 기동 후 90초 대기 → health/smoke test.

#### Health 7/7 + Frontend (8/8)
| 포트 | 서비스 | 결과 |
|:-:|---|:--:|
| 8096 | infolink-auth | ✅ 200 |
| 8080 | infolink-orchestrator-backend | ✅ 200 |
| 8083 | infolink-proxy-dmz | ✅ 200 |
| 8093 | infolink-proxy-internal | ✅ 200 |
| 8084 | infolink-api-collector | ✅ 200 |
| 8095 | infolink-api-provider | ✅ 200 |
| 8082 | infolink-agent-bojo-dmz | ✅ 200 |
| 3000 | infolink-orchestrator-frontend | ✅ 307 (middleware → /login redirect 정상) |

#### Smoke test 6/6 PASS
| # | 검증 | 결과 |
|:-:|---|:--:|
| 1 | JWT 로그인 `POST /api/auth/login` admin/pass1234 | ✅ 200 + cookie 정상 |
| 2 | `GET /api/auth/me` (cookie) | ✅ 200 (id=27 admin 보존) |
| 3 | backend `/api/agents` (cookie) | ✅ 200 — agent 목록 |
| 4 | provider `/api/manage/operations` (cookie) | ✅ 200 — operation 목록 |
| 5 | collector `/api/endpoints` (cookie) | ✅ 200 — endpoint 목록 |
| 6 | **B4 핸들러 회귀** `POST /api/manage/operations/36/test {rel_trans_cgg_code:"3030000"}` | ✅ 200 + 13 컬럼 정합 (permNtFormCode/addr/uwaterSrvCode/digDph/...) |

리네이밍이 검증자 흐름 (`com.infolink.agent.common.auth.AuthVerifierAutoConfiguration` 자동 등록 + `JwtCookieAuthFilter` cookie 검증 + `ApiKeyFilter` soft-mode) 완전 정합 동작 확인.

### Phase 4 — 문서 일괄 갱신

#### 갱신 대상
| 영역 | 패턴 |
|---|---|
| `D:/dev/claude/CLAUDE.md` (root) | 1건 직접 Edit (sync-agent-common JAR 룰 → infolink-agent-common) |
| `docs/` (claude-memory 제외) | sed 일괄 |
| `verify/` (_invariants/, deployment/, checklists/, runs/, map/, tasks/, issues/) | sed 일괄 |
| `test_plan/` (bojo, others) | sed 일괄 |
| `todo/` (system 포함) | sed 일괄 |
| `nexus-dependencies.md` | sed 일괄 |
| `dev_plan/2026_05/07/deploy-package.md` (사용자 별 트랙) | sed 일괄 |
| `docs/claude-memory/` 9개 + `memory/MEMORY.md` + `memory/*.md` (사용자 영역) | sed 일괄 (사용자 결정) + MEMORY.md 트리 다이어그램 우산 해체 직접 Edit |

#### sed 패턴 (순서 중요)
```
sync-orchestrator/{backend,frontend}|-auth|-backend|-frontend → infolink-* (긴 패턴 먼저)
sync-orchestrator → infolink-orchestrator
sync-agent-common → infolink-agent-common
sync-agent-bojo-int → infolink-agent-bojo-internal
sync-agent-bojo → infolink-agent-bojo-dmz
sync-agent-others → infolink-agent-others-dmz
sync-agent-provide → infolink-agent-provide-dmz
sync-proxy-dmz → infolink-proxy-dmz
sync-proxy-internal → infolink-proxy-internal
sync-proxy → infolink-proxy (단독)
sync-agent-* → infolink-agent-*  (와일드)
sync-agent- → infolink-agent-  (prefix fallback)
gims-api-provider → infolink-api-provider
com.sync.agent.bojoint → com.infolink.agent.bojo
com.sync. → com.infolink.
com.gims. → com.infolink.
bojo-int (단어경계) → bojo-internal
```

추가 직접 Edit:
- `docs/dev_guideline/AGENT_YAML_GUIDE.md` brace 표기 `infolink-agent-{bojo|bojo-int|others|provide}` → `infolink-agent-{bojo-dmz|bojo-internal|others-dmz|provide-dmz}`

#### 보존 (의도)
- `dev_plan/2026_05/07/service-naming.md` — 매핑 표 안 "현재" 칼럼 옛 이름 보존 (변경 결정의 증거)
- `dev_logs/` + `dev_plan/2026_0X/` (5/7 외) — 그 시점 작업 기록 보존

---

## 시스템 최종 상태 (5/7 종료)

- 7 서비스 + frontend 가동 중: infolink-auth(8096) / infolink-orchestrator-backend(8080) / infolink-proxy-dmz(8083) / infolink-proxy-internal(8093) / infolink-api-collector(8084) / infolink-api-provider(8095) / infolink-agent-bojo-dmz(8082) / infolink-orchestrator-frontend(3000)
- DB `auth_users`: admin (id=27) + test1 (id=56) — auth_users_id 컬럼명 보존
- B4 등록: id=36 (`opnService/getWellInfo`)
- Internal Oracle: TC_GD000002 / TC_GD000100 표준화 + RGETNPMMS01 9 row 샘플
- Frontend route group: `app/(main)/` + `/login`

## 산출물

| 위치 | 종류 |
|---|---|
| `dev_plan/2026_05/07/service-naming.md` | **신규** dev_plan — 12 모듈 매핑표 + 결정 원칙 + 5 Phase 실행 계획 |
| 11 디렉토리 rename (git mv) | sync-/gims- → infolink-* |
| 모든 .java sed (수백 파일) | com.sync./com.gims. → com.infolink. + bojoint→bojo 통합 |
| 8 settings.gradle / 9 application.yml / 11 build.gradle | rootProject.name / spring.application.name / group / libs/ 경로 |
| `infolink-agent-common-1.0.0-SNAPSHOT.jar` | 새 이름 재빌드 + 9 검증자 모듈 libs/ 복사 |
| `META-INF/spring/AutoConfiguration.imports` | FQCN `com.infolink.agent.common.auth.AuthVerifierAutoConfiguration` |
| 문서 일괄 sed | docs/ + verify/ + test_plan/ + todo/ + memory/ + claude-memory/ + CLAUDE.md (root) + nexus-dependencies.md + dev_plan/2026_05/07/deploy-package.md |
| `infolink-orchestrator-frontend/types/index.ts`, `lib/api.ts` | type 에러 4건 fix |
| `dev_logs/2026_05/2026-05-07.md` | 본 일지 |

---

## 발견 사항 (사후 처리)

- **`infolink-agent-provide-dmz` port 8096 — `infolink-auth` 와 충돌**
  - provide-dmz 가 별 트랙이라 현재 미가동. 향후 가동 시 별 포트 할당 필요 (예: 8097)
  - 본 작업 영향 없음 (Phase 3 smoke test 에서 provide-dmz 미기동)

- **`bojo-int` 약칭 → `bojo-internal` 일괄 변경**
  - 디렉토리 이름은 `infolink-agent-bojo-internal` 로 결정. 문서/메모리 안 약칭 `bojo-int` (마지막에 sed 적용 단계 추가)도 일관성 위해 갱신

---

## 저녁 — 1차 반입 통합 테스트 진입 (test_plan 8 문서 진행)

### test_plan 번호 의미부여 (사용자 5/7)
- 실행 순서 = 번호 (`01-security` / `02-datasource` / `03-bojo` / `04-others` / `05-api-collect` / `06-api-provide` / `07-monitoring` / `08-first-deployment-e2e`)

### 진행 결과

| # | 단계 | 결과 |
|:-:|---|:--:|
| 01 | security | ✅ 통과 (별 세션) |
| 02 | datasource | ✅ 통과 |
| 03-Step1 | bojo RCV (3 업체: keunsan 대문자 / chungnam PG 소문자 / bytek MySQL) | ✅ 데이터 적재 OK + 표 1건 fix |
| 03-Step2 | bojo DMZ Loader | ✅ PASS (사용자 화면 확인) |
| 03-Step3 | bojo SND | ✅ PASS |
| 03-Step4 | bojo Internal RCV | ✅ PASS (사용자 화면 확인) |
| 03-Step5 | bojo Internal Loader | ❌ → fix → **재실행 대기** (사용자 화면 확인 필요) |

### 발견·수정한 버그 (회귀 룰 정합 — 단일 케이스 땜질 X)

| # | 위치 | 증상 | 원인 | fix |
|:-:|---|---|---|---|
| 1 | `infolink-agent-common/.../ExecutionDataController.executeBatchSourceQuery` | 추적 단건 SOURCE 조회 시 검색어 무시 (PG/대문자/MySQL 동일) | pk_batch 모드 (>1000 row) 의 batch SQL 에 `searchClause` 미포함 | `buildSearchClause()` 호출 + 각 batch SQL 에 절 추가 + searchParams concat |
| 2 | `LinkSourceToIfStepFactory` | ClassCastException: `String cannot be cast to List` | `AgentConfigLoader` 가 `source-table`/`target-table` 을 List 로 normalize, 그런데 factory 는 `(String) config.get(...)` | `toSingleString` 헬퍼 추가 (List 첫 element 또는 String) |
| 3 | `ProxyDataSourceService` (DMZ + Internal **양쪽**) | Orchestrator 401 AUTH_REQUIRED | RestTemplate 호출 시 X-API-Key 헤더 누락 (5/6 보강은 ConnectionInfoController 만) | `@Value("${agent.api-key:}")` field + RestTemplate.exchange + HttpHeaders 로 X-API-Key |
| 4 | `infolink-orchestrator-frontend/.../executions/[id]/page.tsx` | 추적 토글 펼친 상태에서 페이지 이동 시 유지 | `expandedRowPk`/`traceResult` state reset 로직 부재 | `useEffect([currentPage, selectedTable])` 로 reset |
| 5 | `AppHeader.tsx` / `users/me/page.tsx` / 텍스트 리브랜딩 | 익명 사용자 분기, logout 호출 누락, 옛 이름 | (개선) | `user==null` 분기 제거 + 비밀번호 변경/탈퇴에 `authApi.logout()` 추가 + "Sync Orchestrator" → "GIMS-Link" |
| 6 | `infolink-auth/application.yml` | access token 15분 (테스트 불편) | (정책 조정) | `access-token-ttl-minutes: 15 → 720` (12h) |
| 7 | **`InternalBojoLoadStep` 헬퍼 3개** | Step 5 14897건 모두 `IllegalArgumentException: 지원하지 않는 날짜 타입: LocalDateTime` | **94e2127 Phase 5 IF 읽기 JPA 전환 회귀** — `IfRsvSecObsvdata.obsvDate : LocalDateTime` 인데 `buildObsrvnDt`/`formatDateStr`/`formatTimeStr` 는 Timestamp 시절 분기만 (Timestamp/Date/LocalDate/String) | 3 헬퍼에 `LocalDateTime` 분기 추가 (buildObsrvnDt 의 timeObj 도 방어) |

### 부수 발견 (이번 fix 범위 밖, 추후 결정)
- **sync_log `error_summary` ORA-01461** — 14897건 실패 시 obsvFailedKeys 누적이 4000자 초과로 저장 실패. 정상 흐름에선 발생 X. 4/30 dev_log 에도 같은 항목 기재됨 (RCV 실패 케이스). truncation 추가 또는 컬럼 LOB 화 결정 필요.
- **dev DB 의 `sec_*_view` JPA ddl-auto 자동 생성** — `feedback_entity_ownership` 룰 정합으로 `sec_*` 는 JdbcTemplate 으로 두는 게 맞음. JPA Entity 매핑 검토 별 사이클.

### 시스템 최종 상태 (저녁 종료 시점)
- 7 서비스 + frontend 가동 중 (오후 기동분 그대로 + agent-bojo-internal 재기동)
- agent-bojo-internal: LocalDateTime fix 적용된 새 빌드 (5/7 17:02 기동)
- 03-Step5 Internal Loader 의 EAV 1:3 적재 + 추적 검증은 사용자 직접 화면 확인 단계에서 멈춤

---

## 다음 이어서

### 즉시 (다음 세션 시작 지점)
1. **03-Step5 Internal Loader 재실행** (사용자 직접 화면)
   - 기대: read=14897 / write≈14897×3 (EAV gwdep/gwtemp/ec) / skip 0
   - 실패 시 LocalDateTime 외 다른 회귀 점검
2. **03-Step5 추적 검증** (`/trace-source` 단건 역추적)
3. 03 bojo 마무리 (Retention/스케줄/통합 시나리오는 메타 정책 — 가벼움)

### 그 다음
- 04 others (yaksoter/nara/news/use 등 8085)
- 05 api-collect (DMZ 8084 + Internal 8094)
- 06 api-provide (8095, B1~B18 핸들러)
- 07 monitoring
- 08 first-deployment-e2e (통합 시나리오)

### 별 트랙 (보류)
- **1차 반입 전 전체 기능 테스트 baseline tag** (위 03~08 통과 후 stable tag + GitHub Release)
- 프로시저 관리 모듈 (`infolink-procedure`, port 8097 후보) — `dev_plan/2026_05/06/procedure-management-module.md` §12
- 폐쇄망 운영 yml override 패턴 정립 (`mock.api.enabled=false`, JWKS URL 등)
- IDE workspace.xml SPRING_BOOT_MAIN_CLASS 캐시 재로드 (sed 로 처리됐지만 IDE 캐시 별)
- sync_log `error_summary` Oracle 4000자 한계 (ORA-01461) — 실 운영 실패 흐름 시 발생 가능
