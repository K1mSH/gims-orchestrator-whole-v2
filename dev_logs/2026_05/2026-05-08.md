# 2026-05-08 작업 일지

## 요약 (보고용)

### 파트별 진행도

| 파트 | 상태 | 비고 |
|------|:----:|------|
| **5 서비스 기동** (auth/backend/proxy-internal/bojo-internal/frontend) | **완료** | DMZ/collector/provider 제외 (사용자 PC 부담 절감 룰) |
| **03 Step 5 LocalDateTime 회귀 fix 마무리** (5/7 잔여) | **완료** | bojo-internal `IfRsvSecObsvdata` + `IfRsvSecJewon` 7 fields **Long → Double** |
| **03 Step 5 카운터 회귀 (5dfa203 패턴) 복원** | **완료** | `writeCount = obsvSuccess` (14897) → `+= inserted` (44691, EAV 1:3 팽창). `da653f2` 의 숨은 회귀 fix |
| **03 Step 5 Target 추적 fix** (batchUpsertLink + ensureResultId) | **완료** | TM_GD980002 / TM_GD970101 INSERT/UPSERT 시 `execution_id` 박기 |
| **03 Step 5 multi-target count 메타 추가** (sync_log JSON 형식 확장) | **완료** | `target_tables: [{"name":..,"count":N}]` + TableStatsDto.targetCounts + frontend per-target count 표시 |
| **VER 별 사이클 등록 / 갱신** | **완료** | VER-014 (Tibero NUMBER(p,s) precision), VER-015 (확장 + 추적 정책 확정 → CLOSE 후보) |
| **03 Step 5 진정 PASS 선언** | **완료** | 카운터 / 소수 보존 / target 표시 / SOURCE 펼침 추적 / 사용자 화면 검증 모두 통과 |

---

## 오전 — 5 서비스 기동 + Step 5 재실행 + LocalDateTime fix 마무리

### 1. 5 서비스 기동 (사용자 PC 부담 룰)

처음 9 서비스 일괄 기동 시도 → 사용자 피드백 "테스트 대상 서비스만 켜자". DMZ/collector/provider 4개 종료 후 5 서비스 유지:

| 포트 | 서비스 | health |
|:-:|---|:-:|
| 8096 | infolink-auth | 401 (UP) |
| 8080 | infolink-orchestrator-backend | 401 (UP) |
| 8093 | infolink-proxy-internal | 200 |
| 8092 | infolink-agent-bojo-internal | 200 |
| 3000 | infolink-orchestrator-frontend | 200 (`/login`) |

> 메모리 룰 **`feedback_test_target_services_only`** 후속 등록 검토 — "테스트 대상 서비스만 기동, 9개 일괄은 PC 부담".

### 2. Step 5 1차 시도 (read=0/write=0)

원인: 어제 1차 LocalDateTime 실패 시 IF 14897건 `LINK_STATUS=FAILED` 마킹. Loader 는 PENDING 만 읽음.
복구: `UPDATE IF_RSV_SEC_OBSVDATA SET LINK_STATUS='PENDING' WHERE LINK_STATUS='FAILED'` (14897 rows)

### 3. Step 5 2차 시도 (read=14897/write=14897, 카운트 의문 발생)

사용자 "카운터가 안 맞는다" — 실 PM_GD970201 INSERT 44691행 vs sync_log write=14897 차이.

추적 검증 2단계 단건 역추적 진행 중 **데이터 손실 발견**:
- IF id=132 GWDEP=`6.27` → PM OBSRVN_DATA_VL=`"6"` (소수점 절단)
- 원인: `IfRsvSecObsvdata.gwdep` field type = **Long** (Oracle NUMBER → Long 변환 시 정수화)

### 4. 표준화 자료 비교 + Long → Double fix

`dev_plan/2026_04/06/표준화_컬럼매핑.md` (보조망 4 Target 환경부 표준) + 외부 PG view (`double precision`) + DMZ entity (Double) vs bojo-internal entity (Long) 비교 결과 — **bojo-internal IF entity 만 회귀**.

회귀 commit: `94e2127 feat: bojo-int Entity 전환 Phase 5 완료 — Step 4개 IF 읽기 JPA 전환` (3월). bojo-internal 의 IF JPA 전환 시 entity field type 추정 실수.

#### 수정 (Long → Double 7 fields)

| 파일 | field |
|---|---|
| `IfRsvSecObsvdata.java` | gwdep, gwtemp, ec |
| `IfRsvSecJewon.java` | pyogo, guldep, guldia, casingHeight |

#### 사용자 룰 정합 결정

- **Tibero NUMBER(p,s) → PG 매핑**: 운영 환경 = Tibero `NUMBER(8,2)` 명시. dev = PG `double precision`. Java entity = `Double` 정합 (sensor 정밀도 IEEE 754 충분 + DMZ baseline 일관). BigDecimal 마이그레이션은 별 사이클 (**VER-014**)
- 사용자 룰 **"엔티티 스팩은 표준화 기준으로 맞추는거라 임의 수정 금지"** 정합 — DDL/외부소스/DMZ baseline 모두 소수 허용 → bojo-internal 의 Long 만 회귀이고 fix 정당

### 산출물 (오전)

- `dev_plan/2026_05/08/bojo-internal-if-entity-standardization-fix.md` — Long → Double 표준화 정합 fix 계획
- `verify/issues/OPEN/VER-014-numeric-precision-ddl-explicit-tibero-alignment.md` — Tibero NUMBER(p,s) DDL precision 명시화 별 사이클
- `IfRsvSecObsvdata.java`, `IfRsvSecJewon.java` 7 field 수정

---

## 오후 — 카운터 회귀 추적 + 5dfa203 패턴 복원 + Target 추적 통합 fix

### 5. 카운터 회귀 git 추적 — `da653f2` 의 숨은 변경

사용자 의견 "팽창이 총합에 계산이 됐던 거 같다" — git blame + dev_logs 추적.

| Commit | 일자 | 변경 (commit message) | 실 변경 |
|---|---|---|---|
| `5dfa203` | 3/4 | "프록시 Agent 분리, **LINK SyncLog**, pm_gd970201 추적 지원" | writeCount += inserted (44691) + LINK SyncLog 별 row + pm.execution_id/source_refs 컬럼 추가 |
| **`da653f2`** | **3/9** | "InternalLoadStep RESYNC 상태 지원" | (1) WHERE link_status `'PENDING'` → `IN ('PENDING','RESYNC')` ✅ + **(2) `writeCount = obsvSuccess` ❌ + (3) LINK SyncLog 제거 ❌ — commit message 비공개** |

`da653f2` 의 RESYNC fix 의도와 무관한 (2)(3) 변경이 끼어들어간 **숨은 회귀**. 그 후 sample 작아 (804 IF) 차이 미인지 → 5/8 통합 테스트 14897 IF 큰 데이터셋에서 노출.

### 6. 5dfa203 패턴 복원

| 변경 | line |
|---|---|
| `inserted` 변수 outer scope 로 이동 | InternalBojoLoadStep.java:99 |
| `writeCount = obsvSuccess` → `writeCount += inserted` | line 244 |
| saveSyncLogMapping mappingName=stepId → "obsvdata" 로 통일 (fail path 와 정합) | line 289+ |
| obsvdata mapping write count = `inserted` (실 INSERT 행) | line 292 |

### 7. link mapping 분리 → 되돌림 (Agent 정의 정합 우선)

처음엔 5dfa203 패턴 정합으로 link mapping 별 row 추가했으나, 사용자 화면에서 SOURCE/TARGET 행 두 줄 표시 + 사용자 의견 "Agent 정의 (Datasource & 테이블) Source 1 / Target 3 이 정답" 으로 결정 변경:

- link mapping 분리 → **되돌림** (sync_log 1 row 만)
- yaml `target-table: [PM_GD970201, TM_GD970101, TM_GD980002]` **그대로 유지**
- TM_GD980002 추적은 별 mapping 없이도 batchUpsertLink execution_id fix 로 가능

### 8. Target 추적 통합 fix

| 변경 | 파일 |
|---|---|
| `batchUpsertLink` MERGE INTO 에 `execution_id` 컬럼 추가 (Oracle + PG 양쪽) | `GimsTargetRepository.java` |
| linkRows.add 시 executionId 인자 전달 | `InternalBojoLoadStep.java` |
| `ensureResultId` INSERT SQL 에 `execution_id` 컬럼 추가 (Oracle + PG) + 시그니처에 executionId 인자 추가 | `GimsTargetRepository.java` |
| ensureResultId 호출 3건 (rsltGwdep/rsltGwtemp/rsltEc) executionId 전달 | `InternalBojoLoadStep.java` |

### 산출물 (오후)

- `dev_plan/2026_05/08/step5-counter-regression-restore.md` — 5dfa203 패턴 복원 + Target 추적 통합 fix 계획
- `verify/issues/OPEN/VER-015-internal-bojo-loader-tm-gd970101-target-mapping-mismatch.md` (확장) — multi-target 표시 룰 + 추적 정책 정리

---

## 저녁 — multi-target count 메타 추가 + Step 5 진정 PASS

### 9. multi-target count 메타 회귀 추적

사용자 화면 "테이블별 처리 현황" 표:
- TARGET PM_GD970201 = 44691 / TM_GD970101 = 44691 / TM_GD980002 = 44691 (모두 같은 카운트, 펼침 표시)
- 실 적재 = PM 44691 / TM_GD970101 3618 / TM_GD980002 1206 (다른 값)
- 사용자 "건수 개별 테이블도 합산으로 뜨는 거 같다"

회귀 추적 결과 — **`da653f2` 와 같은 commit (3/9 per-table → per-mapping 전환) 시점**:
- 5dfa203 ~ da653f2 직전: per-table SyncLog (각 target row 별도 카운트)
- da653f2 이후: per-mapping (mapping 1 row, target array, 단일 write count)
- → **per-target 카운트 분리 메타가 통합 과정에서 누락**

### 10. multi-target count 메타 추가 (3 영역 변경)

| 영역 | 변경 |
|---|---|
| common | `TableStatsDto.targetCounts: Map<String, Long>` 필드 추가, `ExecutionDataController.parseTargetCounts` 신규 메서드, `/tables` endpoint 에서 채워 반환 |
| Loader | `saveSyncLogMappingWithCounts` 신규 helper (target_tables JSON = `[{"name":..,"count":N}]` 형식) + InternalBojoLoadStep main path 에서 호출 |
| Frontend | `TableStats.targetCounts?: Record<string, number>` 추가, `flatTableStats` 변환 시 각 target 별 count fallback 룰 (`mapping.targetCounts?.[t] ?? mapping.writeCount`) |

backward-compat: 기존 형식 `["str"]` 도 그대로 처리 — 다른 1:1 mapping Loader 영향 0.

### 11. ensureResultId fix (TM_GD970101 행 클릭 추적)

`ensureResultId` 신규 INSERT 시 execution_id 미박음 → /target?tableName=TM_GD970101 매칭 0건 → 행 클릭 시 빈 응답.

INSERT SQL 에 execution_id 컬럼 추가 + 호출 시 executionId 인자 전달.

> **다음 실행** 의 ensureResultId 는 cache hit 또는 SELECT 만 (신규 INSERT 0) → 그 실행의 execution_id 박힘 X. **마스터 row 의 추적은 첫 INSERT 시점 한정**. 의도 정합.

### 12. 추적 정책 확정 (사용자 결정)

`step5-counter-regression-restore` + `sync-log-multitarget-counts` + ensureResultId fix 적용 후 사용자 검증:

| Target | 추적 정책 | 이유 |
|---|---|---|
| **PM_GD970201** | ✅ **정수 추적** | IF 의 측정 데이터 직접 적재 (EAV 1:3), 1:1 매핑. source_refs = `["I:internal:IF_RSV_SEC_OBSVDATA:{id}"]` |
| **TM_GD970101** | ⚠️ 데이터 표시만, 펼침 X (**의도**) | 마스터 메타 (brnch+IEM rsltId). IF 1:1 매칭 X (1 rsltId : N IF) |
| **TM_GD980002** | ⚠️ 데이터 표시만, 펼침 X (**의도**) | Link 인덱스 (obsv_code 별 1 row, max date). Link 개념은 추적 비대상 |

→ 현 상태가 의도 정합. VER-015 CLOSE 후보.

### 13. 산출물 (저녁)

- `dev_plan/2026_05/08/sync-log-multitarget-counts.md` — multi-target count 메타 추가 계획
- `TableStatsDto.java` (targetCounts 필드)
- `ExecutionDataController.java` (parseTargetCounts 메서드 + /tables endpoint)
- `types/index.ts` (TableStats.targetCounts)
- `executions/[id]/page.tsx` (per-target count fallback 룰)
- `InternalBojoLoadStep.java` (saveSyncLogMappingWithCounts 신규 helper + ensureResultId 호출 변경)
- `GimsTargetRepository.java` (ensureResultId + batchUpsertLink execution_id)
- `VER-015` 갱신 — 추적 정책 확정 → CLOSE 후보

---

## 시스템 최종 상태 (5/8 종료)

- 5 서비스 가동: auth(8096) / backend(8080) / proxy-internal(8093) / bojo-internal(8092, fix 적용 빌드) / frontend(3000)
- DB cleanup 완료 (Internal Oracle: PM/TM_GD970101/TM_GD980002 모두 0 / IF PENDING 14897, Orchestrator PG: execution_history 정리)
- Step 5 1회 실행 검증 완료 (사용자 화면 PASS)

---

## 03 bojo Step 5 진정 PASS 검증 결과

| 검증 영역 | 결과 |
|---|:-:|
| 카운터 5dfa203 패턴 (write=44691, EAV 1:3) | ✅ |
| Long → Double 소수 보존 (예: GWDEP=10.41) | ✅ |
| multi-target count 메타 (PM=44691, TM_GD970101=3618, TM_GD980002=1206) | ✅ |
| TARGET PM_GD970201 행 클릭 + 펼침 추적 (source_refs IF 매칭) | ✅ |
| TARGET TM_GD970101 행 클릭 (3618 행 표시) | ✅ |
| TARGET TM_GD980002 행 클릭 (1206 행 표시) | ✅ |
| TM_GD970101/TM_GD980002 펼침 미추적 (의도) | ✅ |

---

---

## 추가 — §7 Schedule + §8 Retention 검증 (저녁 마무리)

### 14. UI Retention 라벨 한국어 친화 변경
- 사용자 룰: "저 단어(Retention) 모르는 사람이 보면 안 되니까"
- `InfoTab.tsx:968` `데이터 보존 (Retention)` → `데이터 보존` (영어 병기 제거)
- 다른 라벨은 이미 한국어 (보존 기간, 보존 정책 등)

### 15. §7 Schedule 검증 (API 직접)

| 단계 | 결과 |
|---|:-:|
| API 키 평문 확보 (Jasypt CLI 복호화) | ✅ `sync-proxy-api-key-2024-secure` |
| POST /api/schedules (id=29, internal-bojo-loader, `0 */2 * * * *`) | ✅ 200 |
| GET /api/schedules/29 | ✅ |
| PUT /api/schedules/29/toggle (활성↔비활성 양방향) | ✅ |
| **cron 자동발화** (등록 13:44:51 → 발화 **13:46:00.05961**) | ✅ 정확 |
| triggered_by=SCHEDULE / status=SUCCESS / read=14897 / write=44691 / duration=40s | ✅ |
| DELETE /api/schedules/29 | ✅ 204 |

### 16. 데이터 시뮬레이션 (Retention 검증용)

#### Internal Loader 1회 검증 (internal-bojo-loader)
- 5/6 5000 + 5/7 9897 분포 → Schedule 자동발화 후 IF SUCCESS, PM 5/6=15000 + 5/7=29691 (EAV 1:3)
- Retention `retentionDays=1` cleanup → cutoff=2026-05-07
  - PM 5/6 15000 삭제 / 5/7 29691 보존 ✅
  - IF 5/6 5000 삭제 / 5/7 9897 보존 ✅
  - TM_GD970101 (3618) / TM_GD980002 (1206) / IF_JEWON (1206) 영향 없음 ✅
- enabled=false → 404 skip ✅
- targets=[] → 404 skip ✅

#### 보조망 라인 6 Agent Retention 검증 (사용자 룰 — 늘 하던 3 업체 라인)

대상: dmz-bojo-rcv-daejeon (id=7) / dmz-bojo-rcv-infoworld-local (id=9) / dmz-bojo-rcv-keunsan (id=12) / dmz-bojo-loader (id=17) / dmz-bojo-snd (id=18) / internal-bojo-rcv (id=19)

**시뮬레이션 (각 3 테이블 × 5000 → 5/6, 9897 → 5/7)**:
- DMZ if_rsv_sec_obsvdata: 5/6 5000 + 5/7 9897
- DMZ sec_obsvdata: 5/6 5000 + 5/7 9897
- DMZ if_snd_sec_obsvdata: 5/6 5000 + 5/7 9897
- Internal IF_RSV_SEC_OBSVDATA: 5/6 5000 + 5/7 4897

**6 Agent retention 설정 + cleanup 결과**:
| Agent | 대상 | 삭제 | 5/7 보존 |
|---|---|---:|---:|
| daejeon RCV | DMZ if_rsv | **5000** | 9897 ✅ |
| infoworld-local RCV | (위와 동일) | 0 (이미 삭제) | — |
| keunsan RCV | (위와 동일) | 0 | — |
| DMZ Loader | DMZ sec_obsvdata | **5000** | 9897 ✅ |
| DMZ SND | DMZ if_snd | **5000** | 9897 ✅ |
| Internal RCV | Internal IF_RSV (Oracle) | **5000** | 4897 ✅ |
| **합계** | 4 테이블 | **20000** | 정합 |

**잘못 지운 데이터 0건**: jewon (DMZ 3 + Internal 1) / PM/TM_GD970101/TM_GD980002 모두 그대로.

### 17. 안전장치 논의 (사용자 의문 — "잘못 지워지면 머리 아프니까")

후보 7가지 정리 (Dry-run / Hard-cap / 화이트리스트 / 최소 retentionDays / Audit log / soft delete / 운영자 승인 게이트). 사용자 응답 — "자동화하면서 승인 영역 지키는 건 욕심" → 추후 결정. **별 사이클 검토** (VER-016 후보).

### 18. 정리

- Schedule 29 DELETE ✅
- 7 Agent Retention 설정 모두 enabled=false 로 되돌림 ✅
- 7 서비스 가동 상태 (DMZ Proxy + DMZ Agent 추가 기동 후)

---

---

## 추가 — Retention 안전장치 (VER-016, yml retention-candidates 도입) — 사고 사전 차단

### 19. 사고 retrospective + 본질 분석

DMZ Others 5 Agent 검증 중 dateColumn 부적합 (`first_reg_dthr`/`instl_ymd` — 등록일/시설 설치일 컬럼)으로 **saeol 12 + yaksoter 437 = 449 row 의도 외 삭제** 발생. 사용자 룰 "잘못 지워지면 머리 아프다" 위반.

본질 분석 (사용자 통찰):
- Retention 룰의 부적절한 일반화 — 시계열 데이터에만 정합, 마스터/Link/메타에는 부적합
- Agent boundary 미준수 — frontend dropdown 이 datasource 의 모든 테이블 노출
- dateColumn 자유 입력 — 비즈니스 의미 모르고 "*_ymd"/"*_dt" 류 컬럼명만 보고 박을 위험
- 사용자 핵심 통찰: "yml 에서 date-col 박아주면 Agent 기반 제한 가능, 이중 설정 방지"

### 20. yml retention-candidates 도입 (4 layer 검증, 7 Phase)

| Phase | 영역 | 변경 |
|---|---|---|
| 1 | common | `RetentionCandidate.java` 신규 (table, dateColumn, description) |
| 2 | 4 모듈 AgentDefinition + AgentConfigLoader | `retention-candidates` 필드 + yml 파싱 |
| 3 | 4 모듈 PipelineController | `GET /api/pipeline/{agentCode}/retention-candidates` endpoint |
| 4 | 4 모듈 PipelineAssembler | `RetentionCandidatesProvider` 빈 등록 (Spring lambda) |
| 5 | common | `RetentionCandidatesProvider` 인터페이스 + `DataRetentionController` Layer D 검증 |
| 6 | backend | `AgentController` GET endpoint + `AgentService` PUT validation (Layer C) |
| 7 | frontend | `lib/api.ts` getRetentionCandidates + `InfoTab.tsx` dropdown 교체 + 빈 배열 비활성 |

29 yml 모두 retention-candidates 명시:
- 보조망 14 Agent (RCV 10 + Loader + SND + Internal RCV/Loader): 자기 target 1개씩 (이중 설정 방지)
- DMZ Others 5 + Internal Others 10: **빈 배열** (마스터/Link/시설 등록 — 비대상)

### 21. 추적 정책 명확화 (사용자 5/8 결정)

> "Retention 후보 = Agent 의 target 만 (자기 책임). source 는 다른 Agent 의 target 이라 거기서 박음. 이중 설정 방지."

initial draft 에서 internal-bojo-loader 에 PM_GD970201 + IF_RSV_SEC_OBSVDATA 둘 다 박음. 사용자 지적 — IF_RSV 는 internal-bojo-rcv 의 target 이라 거기서만 박는 게 정합. yml 수정 → loader 는 PM 만, RCV 는 IF 만.

### 22. enabled 필드 deprecate (사용자 5/8 결정)

> "활성/비활성 기준 없앰. 그냥 등록하면 적용."

기존: `enabled=true` 인 retentionConfig 만 cleanup. 사용자가 활성화 checkbox 안 켜면 의도 외 미적용.
변경: targets.length>0 만 검사. enabled 필드 (DB 호환성 위해 유지하나) 의미 없음.

영향 영역 4개:
- Backend `DataRetentionScheduler` filter — `Boolean.TRUE.equals(enabled)` → `targets instanceof List && !((List<?>) t).isEmpty()`
- Backend `AgentService.updateRetentionConfig` — `enabled` 의존성 제거
- Agent `DataRetentionController.cleanup` — `!config.isEnabled()` 분기 제거
- Frontend `InfoTab.tsx` — 활성화 checkbox 제거 + read mode 분기 단순화 + 자동 `enabled=true` 박음

### 23. UX 개선

- 테이블 추가 시 자동 `enabled=true` (등록 = 적용 의도)
- read mode 에서 enabled 분기 제거 (targets 있으면 표시)
- 활성화 안내 문구 — "* 기준 날짜 컬럼 기준으로 보존 기간 이전 데이터가 자동 삭제됩니다. 제거하려면 행 삭제 후 저장하세요."
- 비대상 Agent (yml 빈 배열): "비대상 (마스터 / Link / 메타 데이터)" 회색 메시지 + 설정 버튼 hide
- 정상 Agent: dropdown = retention-candidates table list, dateColumn = candidate 의 dateColumn 자동 (read-only, 회색 배경)
- UI 라벨 한국어 친화: `데이터 보존 (Retention)` → `데이터 보존`

### 24. Manual trigger endpoint 추가

`POST /api/agents/retention/trigger` — DataRetentionScheduler.executeRetentionCleanup() 즉시 호출. 운영자 검증 + 시나리오 시뮬레이션용.

### 25. 종합 검증 (frontend 흐름 시뮬레이션)

데이터 시뮬레이션 (DMZ 3 테이블 5/6 3000 + 5/7 1897 / Internal IF 5/6 1500 + 5/7 1397):

| 단계 | 결과 |
|---|:-:|
| 6 Agent PUT 등록 | 모두 200 |
| `POST /retention/trigger` (자동 cron 동일) | 200 |
| 5/6 데이터 자동 삭제 (DMZ 3 테이블 9000 + Internal IF 1500) | **0** ✅ |
| 5/7 데이터 보존 (DMZ 3 테이블 5691 + Internal IF 1397) | **그대로** ✅ |
| jewon (DMZ) / Link / 마스터 영향 | **0** ✅ |

추가 6 케이스 검증:
- 빈 targets PUT (취소 흐름) → 200
- enabled=false + targets PUT → 200 + 자동 cleanup 적용 (룰 deprecate 정합)
- retentionDays=365 (cutoff 미래) → deletedCount=0
- 다른 Agent target 박기 (yml 외) → 거부 (500 — 별 사이클 400 정상화)
- retentionDays=0 (음수/0 방어) → 거부
- enabled=false 자동 trigger → targets.length>0 라 적용 ✅

### 26. 산출물

- `dev_plan/2026_05/08/retention-candidates-safety.md` — 본 fix 의 근간 + 7 Phase 설계
- common 신규: `RetentionCandidate.java`, `RetentionCandidatesProvider.java`
- common 변경: `DataRetentionController.java` (Layer D)
- 4 모듈 변경: AgentDefinition + AgentConfigLoader + PipelineController + PipelineAssembler
- backend 변경: `AgentController.java` (GET + trigger), `AgentService.java` (PUT validation Layer C), `DataRetentionScheduler.java` (enabled deprecate)
- frontend 변경: `lib/api.ts`, `components/agent/InfoTab.tsx` (dropdown + 비대상 카드 비활성 + UX 정리)
- 29 yml retention-candidates 명시

---

## 다음 이어서

### 즉시
1. ~~5/8 미커밋 변경분 git commit (5/7 미커밋 + 5/8 fix 다수)~~ — 완료
2. 5/8 §7§8 + UI 변경 + 6 Agent 검증 commit — 완료
3. VER-016 (Retention 안전장치) commit + 메모리 룰 추가 (현재 단계)

### 그 다음
- 03 bojo 종료 → 04 others-test
- 05 api-collect-test
- 06 api-provide-test
- 07 monitoring-test
- 08 first-deployment-e2e

### 별 사이클
- VER-014 (Tibero NUMBER(p,s) DDL precision 명시 + BigDecimal 마이그레이션)
- VER-015 CLOSE 처리
- TM_GD970130.ph (Long, 수질) — 04 others 또는 별 사이클 표준화 자료 비교
- frontend SOURCE 다중 표시 룰 (link mapping 되돌리며 발견된 부수)

---

## 핵심 회귀 commit 정리 (사용자 추적 결과)

| Commit | 일자 | 회귀 영역 | 본 사이클 fix |
|---|---|---|---|
| `94e2127` | 3월 (Phase 5) | bojo-internal IF entity Long 매핑 | Long → Double 7 fields ✅ |
| `da653f2` | 3/9 | (1) writeCount = obsvSuccess (commit 비공개) (2) LINK SyncLog 제거 (3) per-target count 메타 누락 | (1) `+= inserted` 복원 ✅ (2) link mapping 안 함 (Agent 정의 우선) (3) target_tables JSON 형식 확장 ✅ |

`da653f2` 의 본 의도 (RESYNC 상태 지원) 외에 끼어든 (1)(2)(3) 변경이 commit message 에 명시되지 않은 채 들어감 — 큰 데이터셋 검증 시점에서 노출. 본 사이클에서 의도 정합 차원으로 복원.
