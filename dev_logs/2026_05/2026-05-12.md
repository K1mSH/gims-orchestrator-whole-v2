# 2026-05-12 작업 일지

## 요약 (보고용)

| 파트 | 상태 | 비고 |
|------|:----:|------|
| 서비스 7종 기동 (backend/bojo-dmz/proxy-dmz/bojo-internal/proxy-internal/auth/frontend) | 완료 | 첫 기동 시 Docker PG(29001) 부팅 중이라 backend/auth/bojo-dmz 실패 → PG ready 확인 후 재기동 |
| 03-bojo §5 진행 중 **WHERE(conditions) 버그 발견·수정** | **완료** | jewon 전용 컬럼(sigungu) 조건 시 obsvdata step 크래시 → common 수정·빌드·배포·재기동. 사용자 화면 재검증 OK |
| WHERE 기능 설계 토의 (레거시 보조 내부망 loader 분석) → **yml 선언형 WHERE 필터 구현 (Phase 1~4 + 6)** | **완료** | common 모델 + 4개 모듈(bojo-dmz/bojo-internal/others-dmz/provide-dmz) 인프라(필드/파싱/엔드포인트) + normalizeToList 하드닝 + bojo loader yml where-filters + orchestrator 중계 + frontend 큐레이션 UI. Phase 5(백엔드 가드)·나머지 로더 yml 선언 미진행 |
| **`dmz-bojo-loader.yml` comma-string 버그 fix** | **완료** | `source-table: a, b` (문자열) → `[a, b]` (리스트). 기존 버그 — select-tables/table-mappings 가 깨진 단일 원소로 떠서 "미등록" 표시되던 것 |
| **retention 스케줄 테스트** (DMZ sec_obsvdata + Oracle pm_gd970201) | **완료(원복)** | synthetic 50행 깔고 cron 임시 2분으로 → 스케줄러 발동 확인 (agent17 10건 / agent20 10건 삭제, 실데이터 보존) → 셋업 원복 |
| 04-others 진입 → **auth RSA 키 만료 발견·수정** | **완료** | 5/4 키 8일 TTL 만료 + 자정 회전 미동작 → 임시 복구(C) → `KeyService.ensureValidActiveKey()`+`InitialKeyLoader` 가 기동 시 만료 active 키면 회전(D). 검증 OK |
| **프론트 무한 새로고침 fix** | **완료** | 무효 cookie(HttpOnly)+middleware "쿠키존재=로그인" → /login↔/ 루프. authInterceptor 가 refresh 실패 시 /auth/logout 으로 cookie 만료 후 /login |
| **04-others §4 — 제주 SND(agent 23) + Internal RCV(agent 30)** | **PASS** | agent 23: 3 step 각 read=write=12, source_refs `["D:1018:{dsTableId}:{pk}"]`, 2회 0건, trace-source 3테이블 FOUND. agent 30: read=write=36, Oracle IF_RSV jeju 12/12/12, DMZ if_snd SUCCESS |
| 04-others 진행 중 → **trace prefix-collision 버그 발견·수정** | 코드·빌드 완료 / 검증 미완 | `IF_RSV_TB_JEJU` 역추적이 `if_snd_tb_jeju_jewon` 으로 잘못 (`if_snd_tb_jeju` ⊂ `if_snd_tb_jeju_jewon` — traceToSource 가 IF 이름 접두사 떼고 `contains` 매칭). source_refs 의 tableId(=datasource_table.id) 로 정확 조회하도록 수정 (역방향+정방향 양쪽). common+4 agent 재빌드·재기동했으나 **proxy 재기동 전 PC 렉 → 전 서비스 종료**. 검증·커밋 다음 세션 |
| **PC 렉 → 전 서비스 종료** | — | gradle 데몬 34개+ 누적 + bootRun 다수 → 렉. 다 kill (서비스 미가동 상태로 세션 종료) |
| 03-bojo §5~ 본 검증 | 보류 | 다음 세션 |

---

## 오전 — 서비스 기동 & 03-bojo §5 진입

- 7종 bootRun. backend(8080)/auth(8096)/bojo-dmz(8082) 첫 기동 실패 — `FATAL: the database system is starting up` (Docker `gims_orchestrator_dmz` PG 29001 부팅 중). `pg_isready` 확인 후 3개 재기동 → 전부 health 200.
- DB 상태 점검: DMZ dev `if_rsv_sec_jewon`=3 / `if_rsv_sec_obsvdata`=36 (§5 잔여) / `sec_*`=0 / `if_snd`=0 / `sync_log`=6, `link_ngwis`=4006 (기준점 2026-05-06/235959 유지). orchestrator `execution_history`=0. Internal Oracle `pm_gd970201`=44706 / `tm_gd970101`=3618 / `tm_gd980002`=1206 / `tm_gd970001`=1206.
- 사용자 판단: 클린 안 함 — "클린하면 다 INSERT라 UPSERT가 진짜 UPSERT로 도는지 검증 안 됨". 잔여 위에서 표준 3업체 E2E 진행하기로.
- Step 1 RCV daejeon (조건 없음, 증분) 실행 #1335 — read=write **5359** (jewon 402 / obsvdata 4957) ✅ 예상 일치. if_rsv 누적 404 / 4991 (잔여 +2 / +34 흡수, 안 늘어서 UPSERT 정상 확인).

## 오후 — WHERE(conditions) 버그 발견 → 수정

### 증상
사용자가 화면에서 보조 RCV daejeon 에 `sigungu=중구` (tableName=`sec_jewon_view`) 조건 실행 → `PreparedStatementCallback; bad SQL grammar [SELECT ... "link_status" FROM "sec_obsvdata_view" WHERE "link_status" IN (?,?,?)]; column "link_status" does not exist`. 실행 FAILED. (단 jewon step 은 80건 정상 적재 후였음 → 화면에 jewon 80 / 전체 FAILED)

### 원인 (`SourceToTargetStep.fetchSimpleCopy()`)
- 보조 RCV = jewon step + obsvdata step. conditions 실행이면 둘 다 SIMPLE_COPY 경로.
- `sigungu` 는 `sec_jewon_view` 에만 있는 컬럼. tableName=`sec_jewon_view` 조건은 jewon step 에만 적용(80건), obsvdata step 은 적용 조건 0개.
- 옛날 코드: 적용 조건 0개 + `!fullCopy` → 디폴트 `link_status IN ('PENDING','RESYNC','FAILED')` 폴백. `sec_obsvdata_view` 는 평소 CUSTOM_STAGING(link_ngwis 조인)으로 도는 뷰라 `link_status` 컬럼 없음 → 크래시.
- (tableName 미지정 시엔 obsvdata step 이 `WHERE sigungu=...` 시도 → `column "sigungu" does not exist` — 같은 계열의 다른 실패)
- 참고: DMZ Loader(`DmzBojoLoadStep`)는 이미 "조건 있지만 이 테이블 대상 조건 없으면 skip" 처리됨. RCV(`SourceToTargetStep`)만 미커버였음.

### 수정 (`infolink-agent-common/.../step/SourceToTargetStep.java` `fetchSimpleCopy()`)
1. `execConditions` 산출 시 tableName 필터에 더해 **컬럼 존재 필터** 추가: condition.column 이 이 source-table 의 조회 컬럼(`columns`)에 없으면 제외 (jewon 전용 컬럼이 obsvdata step 으로 새지 않게)
2. **execution-level conditions 실행인데 step-level 적용 조건 0개면 → `return new ArrayList<>()`** (빈 리스트 → readCount=0/writeCount=0/SUCCESS 경로). 디폴트 `link_status` 폴백 도달 안 함. (DMZ Loader 와 동일 시맨틱)
3. legacy startTime/endTime 경로는 불변. 회귀 없음 (조건 없는 일반 실행·공통 컬럼 조건 동작 그대로).

### 빌드/배포
- `infolink-agent-common` clean build → `infolink-agent-common-1.0.0-SNAPSHOT.jar` 을 9개 모듈 `libs/` 에 복사 (bojo-dmz / bojo-internal / others-dmz / provide-dmz / orchestrator-backend / api-collector / api-provider / proxy-dmz / proxy-internal)
- `infolink-agent-bojo-dmz` / `infolink-agent-bojo-internal` clean build → 기존 인스턴스 kill 후 재기동. 7종 전부 health 200 재확인.
- 화면 재검증(`sigungu=중구` → SUCCESS, jewon 80 + obsvdata skip)은 사용자 진행 예정.

## 오후 — WHERE 기능 설계 토의

레거시 소스(`D:\dev\claude\copySource\보조 내부망 loader`, 안양 제외) 분석 + 우리 구조 비교:

- **레거시 `executeSending()`**: `getJewon`(= `SELECT obsrvt_id FROM TM_GD60001 WHERE spot_ty_mng_word_nm='보조지하수관측망'`) 이 드라이버. "지역 단위 실행" = 이 SQL 에 `AND obsrvt_id LIKE 'GN-SAC-G1%'` 끼우기. 그 리스트로 루프 돌며 코드별 obsvdata 가져와 pm_gd60201 EAV 1:3 적재 + 링크 테이블(tm_gd70002) 갱신.
- **우리가 레거시 구조를 안 따라간 이유**: ① DMZ↔내부망 망 분리 (레거시는 내부에서 sec_obsvdata 직접 조회 — 우리는 IF 테이블 거침) ② N+1 → 벌크 read + JDBC batch ③ 증분 추적을 RCV(link_ngwis)에서 한 번 끊음 ④ 단계별 IF + execution_id 로 추적/감사 ⑤ 범용 파이프라인 vs 전용 스크립트. **레거시 구조는 안 따라간 게 맞는 선택.**
- **레거시의 "지역 필터" 만 가져올 가치 있음**: `obsrvt_id LIKE` = 우리 `obsv_code LIKE`. obsv_code 가 곧 지역이고 모든 IF 테이블에 있음. 보조 Internal Loader 지역 실행 = `IF_RSV_SEC_OBSVDATA.obsv_code LIKE 'GN-SAC-G1%'` 조건 한 줄 (지금 기능으로 됨, 버그 fix 만 되면). 제원 등록은 별개 — 보조 제원(`tm_gd970001`)은 GIMS 마스터, **READ ONLY** (`GimsTargetRepository` 주석). dev_log 의 "§4.5 제원 등록"은 테스트 환경 시딩일 뿐.
- **결론 — "yml 선언형 WHERE 필터"**: 로더는 내부 로직이 "어느 테이블 어느 컬럼이 의미 있는지" 알지만 운영자는 모름 → Agent yml 에 `where-filters` 선언 (`steps`/`retention-candidates` 와 나란히), 프론트는 그 목록만 노출. 큐레이션 항목(`column: OBSV_CODE`, label/operators/hint) + 범용 인식자(`column: "*"` = 테이블 전체, 카피성 단계용) 혼용. `where-filters` 미선언 = 현재 범용 동작 유지(하위호환). 백엔드는 선언한 Agent 에 한해 화이트리스트 밖 조건 거부.
- WHERE 실행 시 `link_ngwis` 등 증분 마커는 갱신 안 함 (`skipLinkUpdate=true`) — 의도된 동작 (재동기화지 증분 전진 아님). 단 적재 행의 `execution_id`/`link_status` 는 갱신 (다음 단계가 PENDING 집어가야 함).

→ **그 자리에서 Phase 1~4 구현함** (아래). 사용자: 단계별 진행 + 커스텀 스텝은 안 건드림(이미 ExecutionCondition 처리 중) 확인.

## 오후 — yml 선언형 WHERE 필터 구현 (Phase 1~4)

`select-tables` / `retention-candidates` 와 동일 패턴 차용 (yml → AgentDefinition → agent `GET /api/pipeline/{code}/...` → Orchestrator 중계 → frontend).

- **Phase 1 (common)**: `model/WhereFilterDef.java` — `{key,label,table,column,operators,valueType,hint}` + `ALL_COLUMNS="*"` 인식자(`column:"*"` = 그 테이블 전체 컬럼 허용 = 범용). common build → JAR 9개 모듈 복사.
- **Phase 2 (bojo-internal)**: `AgentDefinition.whereFilters` 필드 + `AgentConfigLoader.parseAgentDefinition` 에 `where-filters` 파싱 + `PipelineController GET /{code}/where-filters` + `internal-bojo-loader.yml` 에 `where-filters` 작성 (region: `OBSV_CODE` LIKE/IN, period: `OBSV_DATE` BETWEEN, 둘 다 `IF_RSV_SEC_OBSVDATA`). 빌드·기동 → `curl /api/pipeline/internal-bojo-loader/where-filters` 노출 확인.
- **Phase 3 (orchestrator-backend)**: `AgentService.getWhereFilters(id)` (agent 중계) + `AgentController GET /{id}/where-filters`. 빌드·기동 → `curl /api/agents/20/where-filters` 중계 확인. agent 7(RCV, 미선언) → `[]` 확인.
- **Phase 4 (frontend)**: `types/index.ts WhereFilterDef` + `lib/api.ts agentApi.getWhereFilters` + `agents/[id]/page.tsx` — `whereFilters` 있으면 큐레이션 conditions UI([필터 선택][허용 연산자만][값+hint], DATE면 date picker), `column:"*"` 항목은 그 테이블 컬럼 dropdown, 미선언이면 기존 범용 UI. `tsc --noEmit` clean.
- **미진행**: Phase 5 (백엔드 가드 — 화이트리스트 밖 조건 거부), Phase 6 (dmz-bojo-loader 등 나머지 모듈에 AgentDefinition/파싱/엔드포인트 + yml). 미선언 모듈은 기존 동작 유지라 급하지 않음.
- 화면 검증: agent 20 에 `region=DJ-%` 실행 → 정상 동작. 단 테스트 데이터(`if_rsv_sec_obsvdata` 4991건)가 99.5% 가 `DJ-%`(DJ-DJC 4962 + DJ-TES 5, 나머지 GG-GMP 14 + GN-CWN 10 = §5 잔여)라 풀 대비 차이 24건뿐 — 필터 자체는 정상. `GN-%`(10건)으로 하면 차이 명확. (참고: `pm_gd970201` 은 INSERT 라 Internal Loader 재실행마다 누적 — 덮어쓰기 X. tm_gd970101/tm_gd980002 는 UPSERT)

### Phase 6 — 인프라 나머지 모듈 롤아웃 (계획 `where-filters-phase6-rollout.md`)

- `bojo-dmz` / `others-dmz` / `provide-dmz` 각 모듈에 `AgentDefinition.whereFilters` + `AgentConfigLoader` `where-filters` 파싱 + `PipelineController GET /{code}/where-filters` 추가 (= bojo-internal 패턴 복제). config/agents 갖는 모듈 = 이 4개 (bojo-dmz/bojo-internal/others-dmz/provide-dmz) 뿐 — api-collector 는 해당 없음.
- (덤) `normalizeToList` 하드닝 — `String` 에 `,` 있으면 split+trim. dmz-bojo-loader.yml 같은 comma-string 버그 부류 원천 차단. 회귀 없음(List·단일값 그대로).
- `dmz-bojo-loader.yml` 에 `where-filters` 선언: region_jewon(`if_rsv_sec_jewon.obsv_code` LIKE/IN) + region_obsvdata(`if_rsv_sec_obsvdata.obsv_code` LIKE/IN) + period(`if_rsv_sec_obsvdata.obsv_date` BETWEEN). 나머지 로더(jeju/use/yaksoter/saeol/api-collect/SND) where-filters 선언은 각 테스트 사이클에서 (도메인 봐야).
- 빌드: bojo-dmz/others-dmz/provide-dmz 재빌드. 가동 중인 bojo-dmz 만 재기동 → `curl /api/pipeline/dmz-bojo-loader/where-filters` → region×2 + period 노출, `/select-tables` 여전히 2개, `dmz-bojo-rcv-daejeon/where-filters` → `[]` 확인. others-dmz/provide-dmz 는 미기동이라 코드만 (자기 테스트 때 반영).
- 미진행: Phase 5 (백엔드 화이트리스트 가드), 나머지 로더 where-filters yml 선언.

## 오후 — dmz-bojo-loader.yml comma-string 버그 fix

- 증상: agent 17(dmz-bojo-loader) InfoTab 에서 source/target 테이블이 "미등록" 표시. 조건실행 UI 에서 테이블 드롭다운 대신 텍스트 입력칸.
- 원인: `dmz-bojo-loader.yml` 의 `source-table: if_rsv_sec_jewon, if_rsv_sec_obsvdata` / `target-table: sec_jewon, sec_obsvdata` — YAML 에서 `[...]` 없이 쉼표 쓰면 **문자열 하나** 로 파싱됨. `AgentConfigLoader.normalizeToList` 가 문자열을 `[그 문자열]` 한 원소 리스트로 → `select-tables` 가 `["if_rsv_sec_jewon, if_rsv_sec_obsvdata"]` (쉼표 박힌 가짜 테이블명) 반환 → 등록 테이블 매칭 실패. (DMZ Loader 실행 자체는 하드코딩 엔티티로 도니까 정상 — 표시만 깨졌던 것)
- fix: `source-table: [if_rsv_sec_jewon, if_rsv_sec_obsvdata]` / `target-table: [sec_jewon, sec_obsvdata]`. 다른 agent yml 전수 점검 — 이 한 파일만 버그. bojo-dmz 재빌드·재기동 → `select-tables` 가 2개로 분리됨 확인.
- (`normalizeToList` 에서 "쉼표 문자열이면 split" 하드닝은 별도 — 각 모듈 AgentConfigLoader 손대야 함)

## 오후 — retention 스케줄 테스트 (셋업 → 검증 → 원복)

사용자가 retention(고위험 기능) 을 육안 검증하고 싶어함 → 테스트 베드 셋업 + 스케줄 경로 검증.

- **DMZ `sec_obsvdata`**: synthetic 50행 (`obsv_code LIKE 'RTN-%'`, 5 버킷 × 10: 2024-06-15 / 2025-12-15 / 2026-03-15 / 2026-04-10 / 2026-05-07). agent 17 retention_config = `{sec_obsvdata/obsv_date/365}`.
- **Oracle `pm_gd970201`**: synthetic 50행 (`execution_id='RTN-SETUP'`, 동일 5 버킷). `OBSRVN_DT` DATE 타입 확인. agent 20 retention_config = `{PM_GD970201/OBSRVN_DT/365}`.
- 백엔드를 `RETENTION_CRON='0 */2 * * * *'` (2분마다) 로 임시 재기동 → `DataRetentionScheduler` 발동 관찰.
- 결과: 14:28 → agent 17 **10건 삭제** (B1/2024, cutoff=2025-05-12, `<`) → 5041→5031, 실데이터(2026) 보존. 14:38 → agent 20 **10건 삭제** (B1/2024) → 74678→74668. 실제 행 사라짐 확인(`obsv_date='2024-06-15'` → 0건, `RTN-B2-%` 멀쩡). 스케줄 경로(스케줄러 → DB config 읽기 → agent `/api/cleanup/{code}` 호출) 정상. ("대상 Agent 1개" 였던 건 agent 17 의 config 가 중간에 UI 에서 `targets:[]` 로 비워져서 — 스케줄러는 targets 비면 skip)
- **retention 에러 케이스 정리** (코드 확인): `DataRetentionService.executeCleanup` = `DELETE FROM {table} WHERE {dateColumn} < ?` (`?=Timestamp`), 테이블별 try/catch (한 테이블 에러나도 전체 안 죽고 error 메시지+0건). 에러 안 나는 조건 = ① table/column 실존 ② **dateColumn 이 date/timestamp 타입** (varchar 날짜는 PG 에서 `operator does not exist` 에러, Oracle 은 NLS 운빨) ③ retentionDays ≥ 1 (0/음수 = skip+에러메시지) ④ yml retention-candidates 등록(VER-016) ⑤ DB 연결. `<` 라 cutoff 당일 데이터는 보존.
- **string 날짜 컬럼 지원** = 미지원, 별도 enhancement 후보. yml candidate 에 `dateColumnType` (예: `STRING_YYYYMMDD`) 같은 거 도입하면 cutoff 를 컬럼 포맷 문자열로 만들어 사전식 비교(고정폭 ISO 정렬순 포맷) — 추후 필요 시.
- **원복 완료**: agent 17·20 retention_config → `{enabled:false, targets:[]}`. synthetic 행 삭제 (DMZ `sec_obsvdata` 4991 / Oracle `pm_gd970201` 74628, RTN 0). 백엔드 평소대로 재기동 → `retention.cron` `0 0 2 * * *` 원복.

## 오후 — 04-others 진입 중 auth RSA 키 만료 발견 (계획 `auth-key-selfheal-on-startup.md`)

- 증상: 04-others 테스트 진입 → 프론트 "인증 서버가 일시적으로 응답하지 않습니다" + backend `JwksClient` `JWKS response has no usable RSA keys` + auth `GET /.well-known/jwks.json` → `{"keys":[]}`. (API 는 X-API-Key 로 우회됐어서 안 보였음 — 쿠키 경로만 다운)
- 원인: auth 모듈 5/4 신규. 유일한 키 `K-2026-05-04-...` 의 `expires_at` = 생성 + `retention-days(8)` = 2026-05-12 14:51 → 오늘 만료. `KeyRotationJob`(자정 cron)은 auth 가 자정에 켜진 적 없어 한 번도 안 돎 → 8일 윈도우가 닫히는 첫날 키가 그냥 만료 → JWKS(`findAllValidPublicKeys` = 만료 안 된 키만) 빈 응답. `InitialKeyLoader` 는 `findByActiveTrue()` 로만 보고 만료 체크 안 해서 재기동해도 자가복구 안 됨 (사실상 버그).
- 임시 복구(C): 만료 키 `expires_at` 을 `2026-06-30` 연장 → JWKS 다시 노출, JwksClient 다음 요청에 자동 refresh → 쿠키 인증 200. (auth/backend 재기동 불필요)
- **본 수정(D)**: `KeyService.ensureValidActiveKey()` 추가 — active 키 없으면 생성 / 만료됐으면 `rotate()`(deactivateAll + generateAndSave + deleteExpired) / 유효하면 그대로. `InitialKeyLoader.onReady()` 가 이걸 호출. `infolink-auth` 빌드 (common 의존 없음 → JAR 복사 불필요).
- 검증: `UPDATE auth_rsa_keys SET expires_at=now()-interval'1 hour'` 로 만료 시뮬 → auth 재기동 → 로그 `[KeyService] active RSA key expired ... — rotating` + `rotated: deactivated=1, newKid=K-2026-05-12-..., deletedExpired=1` + `[InitialKeyLoader] active RSA key ready: kid=K-2026-05-12-...` → JWKS 새 키 노출 → 재로그인 → 쿠키 경로 200. ✅ (band-aid 했던 옛 키는 만료라 cleanup 됨)
- 실 배포 영향: 운영(24/7 + 자정 회전)에선 평상 재기동 시 동작 무변화. 발동 = "모듈이 키 만료 시점 넘겨서 다운돼 있었을 때" 자가복구 → 다운사이드 없음.

## 오후 — 프론트 무한 새로고침 fix

- 증상: auth 키 복구 후, 옛 무효 쿠키 가진 브라우저가 새로고침 무한 루프. `/login` 가도 `/` 로 튕김.
- 원인: `middleware.ts` 가 쿠키 *존재* 만 보고 "로그인됨" 판단 → `/` 통과 → 페이지 API 401 → `authInterceptor` refresh 시도 → refresh 토큰도 무효(+`auth_refresh_tokens` 비웠음) → 실패 → `/login` redirect → middleware "쿠키 존재 + /login → /" → 루프. 쿠키가 HttpOnly 라 JS 로 못 지움 → 빠져나갈 길이 `/auth/logout`(permitAll, idempotent, cookie 만료) 호출뿐인데 안 부름.
- fix: `lib/authInterceptor.ts` — refresh 실패 시 `await authClient.post('/logout')` (서버측 cookie 만료) 후 `/login` redirect. tsc clean. (middleware 의 "쿠키존재=로그인" 단순화는 의도된 거 — 주석 "실 토큰 검증은 백엔드" — 안 건드림. 무효 쿠키만 정리되면 루프 안 생김)
- 검증: 사용자 브라우저 루프 풀림 확인.

## 오후/저녁 — 04-others §4 (제주) + trace prefix-collision 버그

### 04-others 진입
- 04-others-test.md = DMZ Others SND(agent 23 jeju, 24 use) — 단 실제 others SND agent 는 5개: 23 jeju / 24 use / 25 saeol / 34 api-collect / 43 yaksoter (문서 §1-2 outdated). 소스별로 하나씩 진행 방침.
- backend↔auth 쿠키 경로 — `X-API-Key` 로 우회해서 API 테스트 (cookie 는 위 auth fix 후 정상).
- 클린: 제주 소스 `tb_jeju_jewon`/`tb_jeju`/`rgetstgms01` link_status → PENDING(12/12/12), `if_snd_tb_jeju*` TRUNCATE. (소스 데이터는 이미 적재돼 있어 api-collector 불요)

### §4-1 제주 SND (agent 23) — PASS
- SUCCESS, total read=36/write=36/skip=0. 3 step: jeju-jewon-snd / jeju-obsv-snd / jeju-stgms-snd 각 read=write=12.
- IF_SND 각 12건. 소스 link_status 전부 SUCCESS. source_refs `["D:1018:89:SC001"]`(jewon VARCHAR pk) / `["D:1018:103:145"]`(obsvdata BIGINT pk) / `["D:1018:105:5001-001"]`(stgms VARCHAR pk) — `["D:1018:{datasource_table.id}:{pk}"]` 정상, U:0:0 아님, execution_id 박힘.
- (참고: `detail` 엔드포인트가 실행 시작 직후엔 UNKNOWN 반환 → wait 루프가 일찍 빠질 수 있음. execution_history(orchestrator) 의 status 로 완료 판정하는 게 안전)

### §4-2 (2회 재실행, 증분 0건) — PASS / §4-5 (trace-source) — PASS
- 2회: SUCCESS, 3 step 다 0/0/0.
- trace-source 3테이블 다 FOUND: `if_snd_tb_jeju`(`["D:1018:103:145"]`)→`tb_jeju` / `if_snd_tb_jeju_jewon`→`tb_jeju_jewon` / `if_snd_rgetstgms01`→`rgetstgms01`. VARCHAR/BIGINT PK + IF→Source 자동해석 정상.

### internal-jeju-rcv (agent 30) — PASS
- jeju 파이프라인 다음 단계: DMZ SND → Internal RCV. Oracle `IF_RSV_TB_JEJU*` 3테이블 TRUNCATE 후 실행.
- SUCCESS, total read=36/write=36. 3 step(jeju-jewon-rcv/jeju-obsv-rcv/jeju-stgms-rcv) 각 read=write=12. Oracle IF_RSV jeju 12/12/12. source_refs `["D:1018:206:SC001"]`(src=DMZ if_snd_tb_jeju_jewon datasource_table.id 206)/`["D:1018:207:145"]`(src=if_snd_tb_jeju 207). DMZ if_snd_tb_jeju* link_status → 전부 SUCCESS.

### trace prefix-collision 버그 (사용자가 화면에서 발견) — 코드·빌드 완료, 검증 미완
- 증상: `IF_RSV_TB_JEJU` 행(source_refs `["D:1018:207:145"]`) 역추적 → "원본 없음" / `if_snd_tb_jeju_jewon` 0건. (`if_snd_tb_jeju` 가 `if_snd_tb_jeju_jewon` 의 *완전한 접두사*)
- 원인: `ExecutionDataController.traceToSource()` — `if_rsv_*`/`if_snd_*` sourceTable 이면 접두사 떼서 `baseName="tb_jeju"` → SyncLog source_tables 에서 `equals` 안 되면 `contains` 매칭 → `if_snd_tb_jeju_jewon` 이 `tb_jeju` 포함해서 stream 순서상 먼저 잡힘. 정방향(`traceBySourcePk`)도 `%:{테이블명}:{pk}"]` 패턴인데 source_refs 엔 tableId(숫자)가 들어가서 안 맞고 느슨한 pk만 패턴으로 떨어짐. — source_refs 가 `{zone}:{dsId}:{tableId}:{pk}` 로 테이블을 *정확히 특정*하는데 이름 추측으로 빠지는 게 안티패턴 (사용자 지적: "접두사 안 쓰기로 했다").
- 발견: tableId = `datasource_table.id` (agent_table 아님). 조회 = `SELECT table_name FROM datasource_table WHERE id=?`.
- 수정 (`infolink-agent-common/.../controller/ExecutionDataController.java` + `ExecutionDataReader.java`):
  - 헬퍼: `parseSourceRefsTableId`(D/E 포맷 parts[2]), `ExecutionDataReader.findTableNameById`(datasource_table by id), `findTableIdByName`(이름+dsId → id).
  - `traceToSource`: `if_rsv_*`/`if_snd_*` 분기 + Loader-TARGET 분기 모두 — 1순위 = source_refs tableId 로 정확 조회, 2순위 = `I:` 포맷 이름, 3순위 = SyncLog **정확 매칭만**(`contains`/substring 제거).
  - `traceBySourcePk`: Pattern A0 추가 — `sourceTable`+(execution sourceDsId) → tableId 찾아 `%:{tableId}:{pk}"]` 정밀 패턴 우선. 기존 이름 패턴(I: 호환)/pk만 패턴은 fallback 유지.
- 빌드: common build → JAR 9모듈 복사 → others-dmz/bojo-internal/bojo-dmz/provide-dmz 재빌드·재기동.
- **미완**: trace-source 는 Proxy(8083/8093)가 처리(bojo/others agent 들은 ExecutionDataController excludeFilter)인데 Proxy 들은 세션 시작 때부터 *원본* common JAR 로 돌고 있어서 fix 미적용 → Proxy 재기동 필요. 재기동 시도 중 gradle 데몬 누적(34개+)으로 bootRun 이 데몬 못 잡고 멈춤 → `--no-daemon` 으로 재시도 → 그 와중에 PC 렉 심해서 **전 서비스 종료하고 세션 마무리**.
- **uncommitted**: `ExecutionDataController.java`, `ExecutionDataReader.java` + 계획서 `dev_plan/2026_05/12/trace-resolve-by-tableid.md`. common JAR 은 9모듈 libs/ 에 복사됨(미커밋), 4 agent 재빌드됨.

---

## 산출물

- `dev_plan/2026_05/12/fix-rcv-conditions-step-skip.md` — RCV conditions step skip 버그 수정 (코드·빌드·배포 완료)
- `dev_plan/2026_05/12/yml-declared-where-filters.md` — yml 선언형 WHERE 필터 (Phase 1~4 완료, 진행상황 섹션 갱신함)
- `dev_plan/2026_05/12/where-filters-phase6-rollout.md` — Phase 6 인프라 롤아웃 (bojo-dmz/others-dmz/provide-dmz 인프라 + normalizeToList 하드닝 + dmz-bojo-loader.yml where-filters 완료)
- `dev_plan/2026_05/12/auth-key-selfheal-on-startup.md` — auth 기동 시 만료 active 키 자동 회전 (구현 완료)
- `dev_plan/2026_05/12/trace-resolve-by-tableid.md` — trace 테이블 해석을 source_refs tableId 기반으로 (코드·빌드 완료, 검증/커밋 미완)
- **코드 변경**:
  - `infolink-agent-common/.../step/SourceToTargetStep.java` `fetchSimpleCopy()` (컬럼 존재 필터 + 조건 N/A step skip)
  - `infolink-agent-common/.../model/WhereFilterDef.java` (신규)
  - `infolink-agent-{bojo-internal,bojo-dmz,others-dmz,provide-dmz}/.../config/pipeline/AgentDefinition.java`(`whereFilters` 필드) + `AgentConfigLoader.java`(`where-filters` 파싱 + `normalizeToList` 쉼표 split 하드닝) + `controller/PipelineController.java`(`GET /{code}/where-filters`)
  - `infolink-agent-bojo-internal/.../config/agents/internal-bojo-loader.yml` + `infolink-agent-bojo-dmz/.../config/agents/dmz-bojo-loader.yml` (where-filters 선언 + dmz는 source/target-table 리스트 문법 fix도)
  - `infolink-orchestrator-backend/.../service/AgentService.java`, `controller/AgentController.java` (where-filters 중계)
  - `infolink-orchestrator-frontend/types/index.ts`, `lib/api.ts`, `app/(main)/agents/[id]/page.tsx` (큐레이션 UI), `lib/authInterceptor.ts` (refresh 실패 시 /auth/logout 으로 cookie 만료 — 무한 새로고침 fix)
  - `infolink-auth/.../service/KeyService.java`(`ensureValidActiveKey()` 추가), `bootstrap/InitialKeyLoader.java`(만료 active 키면 회전)
  - **[미커밋]** `infolink-agent-common/.../controller/ExecutionDataController.java`(traceToSource/traceBySourcePk — tableId 기반 해석) + `ExecutionDataReader.java`(`findTableNameById`/`findTableIdByName` 헬퍼)
  - common JAR → 9개 모듈 libs/ 복사 (3회: 버그fix, WhereFilterDef, trace fix). bojo-dmz / bojo-internal / orchestrator-backend / others-dmz / provide-dmz / infolink-auth 재빌드
- 메모리 룰 추가: `feedback_where_filter_scoping.md`, `feedback_plan_not_approval.md` (양쪽 동기화)
- 커밋: `7e1a795`(where-filters Phase 1~4 + RCV 조건 step skip) / `327d7ff`(Phase 6) / `0e428ea`(auth 자가복구 + 프론트 루프 fix). **trace fix 는 미커밋** (검증 후 커밋 예정)

## 내일 할 일

> 시작 시: 0) gradle 데몬 정리 (`gradlew --stop` — 실행 중 앱 없으니 안전) → PC 가벼워짐. 1) 필요 서비스 기동 (backend/proxy-dmz/proxy-internal/others-dmz/bojo-internal/auth/frontend — bojo-dmz 는 04-others/trace 검증엔 불요). 2) auth 키는 어제 D fix 로 새로 생성됐고(만료 2026-05-20) InitialKeyLoader 가 기동 시 자가복구하니 신경 안 써도 됨.

1. **trace fix 검증·커밋** (최우선 — 코드는 어제 다 됐는데 proxy 재기동 전에 끊김): proxy 재기동 후 → `internal-jeju-rcv` 의 `IF_RSV_TB_JEJU` 행 trace-source → `sourceTableName=if_snd_tb_jeju`, FOUND 확인. 회귀: `IF_RSV_TB_JEJU_JEWON`→jewon, `IF_RSV_RGETSTGMS01`→rgetstgms01, DMZ side `if_snd_tb_jeju`→`tb_jeju`, 정방향 `/trace`, 03-bojo sec_* . 통과 시 커밋.
2. **04-others 본 테스트 이어서** — §4(제주) PASS, internal-jeju-rcv PASS 까지 했음. 다음: jeju 파이프라인 마저(internal-jeju-loader agent 32 → GIMS jeju 테이블) → §5 이용량(agent 24) → 새올(25) → yaksoter(43) → api-collect(34). 끝나면 그 agent 들 where-filters/retention.
3. 03-bojo §5 Conditions 본 검증 → §6 Trace / §7 Schedule / §9 Frontend UI / §10 문서갱신 + stable tag `bojo-stable-...` + Release.
4. (별도) yml-where-filters Phase 5 (백엔드 화이트리스트 가드) + 나머지 로더 where-filters yml 선언 + 기능 검증(GN-% 케이스). retention `RetentionCandidate` 주석/safety 문서에 "dateColumn date/timestamp 타입 필수" + string 날짜 지원 enhancement 계획.

## 메모

- `IF_RSV_SEC_JEWON` 이 agent 20(internal-bojo-loader) 소스로 등록돼 있지만 `InternalBojoLoadStep` 이 실제로 안 읽음 (`ifObsvdataTable=sourceTables.get(0)` 만 사용) — 조용한 함정. 정리 후보.
- `WhereFilterDef.isAllColumns()/isAllTables()` 가 Lombok `@Data` 로 JSON 에 `allColumns`/`allTables` 직렬화됨 — 무해하나 거슬리면 `@JsonIgnore`.
- `where-filters` 인식자 토큰 = `"*"` (`ALL_COLUMNS` 상수) — Phase 6/문서 확정.
- E2E 로 생긴 데이터 그대로 둠 (사용자 미요청): DMZ `if_rsv_sec_jewon`=404 / `if_rsv_sec_obsvdata`=4991 / `sec_*`=4991 (RTN 정리됨) / `if_snd_*`=4991, Oracle `if_rsv_sec_obsvdata`=4991 / `pm_gd970201`=74628(INSERT 누적, dupes) / `tm_gd970101`=3618 / `tm_gd980002`=1206 / `tm_gd970001`=1206. orchestrator `execution_history` 에 5/12 실행분 누적.
- 스테일 bootRun 태스크 알림(bbzjzmt2f/bc4e4fhc1/bsdui461b 등)은 재빌드/원복 위해 옛 인스턴스 kill 한 거라 무관.
- **gradle 데몬 누적 주의**: 하루에 `gradlew clean build` + `gradlew bootRun` 을 여러 번 반복하면 데몬이 수십 개 쌓여(오늘 34개+) PC 렉 + 새 `bootRun` 이 데몬 못 잡고 멈춤. 회피: 새 `bootRun` 은 `--no-daemon` 으로, 또는 주기적으로 `gradlew --stop`(실행 중 앱이 없을 때). 세션 끝/시작에 `--stop` 한 번이 깔끔.
- **세션 종료 시 전 서비스 OFF** (PC 렉으로 다 kill). Docker 컨테이너(29001~29010 등)는 그대로. 다음 세션 = 처음부터 서비스 기동 필요. trace fix 검증/커밋이 최우선.
- `auth_refresh_tokens` 는 D 테스트 중 비웠음(무효 토큰이라 무방). `auth_rsa_keys` = 새 키 `K-2026-05-12-...` 1개(만료 2026-05-20).
