# 2026-05-04 작업 일지

## 요약 (보고용)

### 파트별 진행도

| 파트 | 상태 | 비고 |
|------|:----:|------|
| **사전 정리 — AUTH_DESIGN.md / AUTH_FLOW.md / dev_plan** | **완료** | peer multiplication 정정, B 옵션 (jjwt + Nimbus), 검증자 매핑, 응답 정책, 키 보관, 키 활용 흐름, RSA 분리 보강, dev_plan §2.3 명시 |
| **§9.3 검증자 모듈별 endpoint 매핑 + 이슈 3개 결정** | **완료** | callback permitAll / Mock 2개 `@ConditionalOnProperty`+denyAll 이중 방어 |
| **dev_plan/2026_05/04/auth-system-phase1.md** | **완료** | Phase 1 + 1.5, Step 11단계, 검증 시나리오 매핑 |
| **Step 1 — 모듈 신설 + 기본 설정** | **완료** | sync-orchestrator-auth 신규 — 8096 listen + jasypt ENC 복호화 OK |
| **Step 2 — Entity + Repository** | **완료** | 3 entity + 3 repo + ddl-auto 자동 테이블 생성 검증 OK |
| **Step 3 — KeyService (RSA)** | **완료** | RSA 2048 페어 자동 생성 / DB INSERT (jasypt ENC) / 재부팅 시 재사용 |
| **Step 4 — TokenService (jjwt)** | **완료** | RS256 발급/검증 + SigningKeyResolver(kid 자동 lookup) + 단위 테스트 7/7 PASS |
| **Step 5 — UserService** | **완료** | 사용자 CRUD + BCrypt + 마지막 1명 차단 + refresh revoke + 단위 테스트 10/10 PASS |
| **Step 6 — AuthService** | **완료** | 로그인/refresh/로그아웃 + 5회 잠금 + 1회용 회전 + timing-safe + 통합 테스트 10/10 PASS |
| **Step 7 — Controller + SecurityConfig** | **완료** | 3 Controller + SecurityConfig + ControllerAdvice + AuthenticationEntryPoint + curl 검증 OK |
| **Step 8 — KeyRotationJob (자정 cron)** | **완료** | @Scheduled wrapper + 회전 단위 테스트 4/4 PASS (deactivate / new active / cleanup / 이전 키 검증 보존) |
| **Step 9 — UserGeneratorCli** | **완료** | Spring 안 띄우고 SnakeYAML+jasypt+JDBC 직접. 첫 사용자 (admin) 발급 + UNIQUE 충돌 검증 OK |
| **Step 10 — sync-agent-common 검증자 자산 (Phase 1.5)** | **완료** | JwksClient + JwtCookieAuthFilter + 헬퍼 + 단위 테스트 5/5 PASS + JAR 복사 8개 모듈 + 회귀 검증 OK |
| **Step 11 — 통합 검증 (curl 시나리오)** | **완료** | 17 시나리오 모두 PASS — auth 모듈 E2E 검증 완료 |

---

## 오전 — 사전 정리 (문서 + 결정)

### 4/30 EOD → 5/4 작업일 진입
- 4/30 약수터 E2E 커밋 (637cc90) 후 첫 작업일 (5/1~5/3 작업 없음)
- 5/4 시작 — 5 row datasource_column 화면 재수집 완료 (사용자 직접 처리)
- 본 세션 메인 트랙 = **인증/로그인 시스템 신규 도입**

### 사전 결정 — 큰 흐름

다수의 의사결정 + 정정 필요:

#### 사용자 관리 정책 (Peer Multiplication)
- 4/28 AUTH_DESIGN.md 작성 시 = "CLI 만, 운영 화면 없음" 정책
- 5/4 정정 = **최초 1명 CLI / 이후 운영 화면에서 동급 계정 증식**
  - 등록자가 ID/PW/이름 모두 입력해서 INSERT → 별도 채널 전달
  - 사용자 목록 조회 (모두), 본인 비번 변경 / 본인 탈퇴, 마지막 1명 차단
  - 등록자 추적 미저장, 타인 정보 수정 불가

#### 라이브러리 결정 (Spring Boot 2.7.12 제약)
- 4/28 = Spring OAuth2 Authorization Server / Resource Server 사용 가정 (Boot 3.x)
- 5/4 발견 = **Spring Boot 2.7.12 고정** (사용자 명시) → Authorization Server 1.x 사용 불가
- 결정 = **B 옵션** = `jjwt 0.11.5 + Nimbus JOSE 9.x` 자체 구현 (RS256/JWKS/회전 모두 살림)
- 코드 ~300줄 추가 부담 / 보안 강도 동일

#### 검증자 모듈 SecurityConfig (이슈 3개 발견 + 결정)
- 검증자 = Backend / api-provider / api-collector 3개. Agent/Proxy 미적용
- **이슈 1**: `/api/callback/**` (Backend) = Agent → Backend 콜백 (시스템 간 호출). **permitAll** + 시스템 간 인증 강화 = 별 후속 작업
- **이슈 2**: `MockApiKeyController` (api-provider) = 테스트용. **`@ConditionalOnProperty` + SecurityConfig denyAll 이중 방어**
- **이슈 3**: `MockApiController` (api-collector) = 테스트용. 동일 이중 방어

#### JWKS fetch 정책
- **Lazy** (모듈 부팅 시 안 가져옴, 첫 호출 시 fetch)
- **retry 2회** (200ms / 500ms) 후 실패 → **503 + `Retry-After: 30`** + 명확 메시지
- 401 (자격 문제) vs 503 (시스템 문제) 구분 — 모니터링 알람 후크

### 문서 작성 / 정정

| 문서 | 작업 |
|------|------|
| `docs/AUTH_DESIGN.md` (4/28 작성) | 정정 — 1424줄 (881 → 1424, +543) |
| `docs/AUTH_FLOW.md` 신규 | 856줄 — 시간순 시나리오 + 키 라이프사이클 + 응답 매트릭스 + FAQ |
| `dev_plan/2026_05/04/auth-system-phase1.md` 신규 | 687줄 — Phase 1+1.5 작업 계획 + 검증 시나리오 매핑 |

#### AUTH_DESIGN.md 정정 핵심 부분
- §2 결정 표 — 사용자 관리 9행 분리 명시 (peer multiplication 강조)
- §5 라이프사이클 — §5.7~5.10 신규 (사용자 추가/탈퇴/비번변경/목록)
- §6 패키지 구조 — UserController / UserGeneratorCli / 검증자 자산 박스
- §7.1 DB — `name` 컬럼 추가 (담당자 이름)
- §8 API 명세 — 4 endpoint 추가 (사용자 CRUD)
- §9.1 SecurityConfig — `oauth2ResourceServer()` 제거 → `addFilterBefore(jwtCookieAuthFilter)` 자체 필터
- §9.3 검증자 모듈별 endpoint 매핑 (callback permit / mock denyAll)
- §10 Frontend — 글로벌 슬림 헤더 + 사용자 관리 화면
- §11 Phase 1.5 신규 (sync-agent-common 검증자 자산)
- §12.4~§12.7 신규 — 테스트 전략 / 키 매트릭스 / 검증 시나리오 28개 / 키 충돌 방지
- §13.6~§13.8 신규 — 마지막 1명 차단 / refresh revoke / 인증 실패 응답 정책
- §17 승인 항목 (a)~(o) 갱신

#### AUTH_FLOW.md 신규
- §1 등장 인물 (10종 키/토큰)
- §2 auth 모듈 부팅 / §3 검증자 모듈 부팅 (Lazy 정책)
- §5~§12 시나리오 4~11 (로그인 ~ 회전 / 다운 / 키 탈취)
- §13.5 RSA 키 다중 보관 메커니즘 (8쌍 동시 존재)
- §14 auth 다운 시 retry + 503 + 모니터링 3중 후크
- §17 FAQ (JWT 평문 / OAuth2 미사용 / 외부 네트워크 / 마스터 PW 등)

#### dev_plan §2.1~§2.4 명시 분리
- §2.1 인프라 / §2.2 토큰 / §2.3 사용자 관리 모델 (peer multiplication 9행) / §2.4 endpoint 4개

---

## 오후 — Step 1 시작 (auth 모듈 신설)

### 사전 확인
- 다른 모듈 = 독립 Gradle 프로젝트 (root settings.gradle 없음)
- sync-agent-common 의존 패턴 = `libs/*.jar` 로컬 JAR 복사
- Java 17 / Spring Boot 2.7.12 / Jasypt 3.0.5
- ENC 값 = orchestrator backend 의 PG 자격증명 그대로 재활용 가능 (같은 29001/orchestrator DB)

### Step 1 작업 항목
1. `sync-orchestrator-auth/` 디렉토리 + 하위 구조 생성
2. `build.gradle` 작성
3. `settings.gradle` 작성 (독립 모듈)
4. `application.yml` (ENC 값은 backend yml 재활용)
5. `AuthApplication.java`
6. `JasyptConfig.java`
7. 빌드 테스트 (`./gradlew :sync-orchestrator-auth:build` 또는 모듈 디렉토리에서 직접)

### Step 1 결과 ✅

**작성된 파일** (7개):
| 파일 | 라인 | 내용 |
|------|:--:|------|
| `sync-orchestrator-auth/settings.gradle` | 1 | 독립 모듈 |
| `sync-orchestrator-auth/build.gradle` | 60 | Spring Boot 2.7.12 + jjwt 0.11.5 + Nimbus 9.37 + Jasypt + JPA + PG. createUser gradle task |
| `sync-orchestrator-auth/src/main/resources/application.yml` | 56 | 8096 / Orchestrator PG ENC (backend 재활용) / auth 설정 / Jasypt |
| `src/main/java/com/gims/auth/AuthApplication.java` | 22 | `@SpringBootApplication` + `@EnableScheduling` |
| `src/main/java/com/gims/auth/config/JasyptConfig.java` | 13 | `@EnableEncryptableProperties` |
| `gradle/wrapper/*` + `gradlew*` | (sync-agent-bojo 에서 복사) | Gradle 8.10 wrapper |
| `src/main/java/com/gims/auth/{config,controller,service,entity,repository,dto,scheduler,bootstrap,tools}/` | (디렉토리만) | Step 2~9 에서 채움 |

**검증**:
- ✅ `./gradlew compileJava` BUILD SUCCESSFUL (16s)
- ✅ `./gradlew bootRun` Started AuthApplication in 3.93s
- ✅ Tomcat 8096 listen
- ✅ `curl http://localhost:8096/actuator/health` → `{"status":"UP"}`
- ✅ Jasypt ENC 복호화 정상 (`Converting PropertySource ... to EncryptableMapPropertySourceWrapper`)
- ⚠️ JPA/Hikari 로그 안 뜸 — Entity 0개라 lazy. **Step 2 에서 Entity 추가 후 검증**.

**발견 사항 / 메모**:
- root level multi-module 아님 (각 모듈 독립 Gradle)
- Spring Boot 2.7.12 + Java 17 패턴 = 다른 모듈과 통일 OK
- ENC 값 backend yml 재활용 가능 (같은 29001/orchestrator DB)
- gradlew/gradle wrapper = sync-agent-bojo 에서 복사 (Gradle 8.10)

### Step 2 결과 ✅

**정정 사항** (사용자 피드백):
- ❌ SQL DDL 파일 작성 — 한 번 만들었다 즉시 삭제. **테이블 생성 = 엔티티 + ddl-auto=update 만**
- AUTH_DESIGN.md §7.1 = "참고용" 명시
- dev_plan Step 2 = "scripts/ddl/.. 신설" 줄 제거

**작성된 파일** (6개):
| 파일 | 내용 |
|------|------|
| `entity/AuthUser.java` | id (IDENTITY) / username UK / password_hash / **name** / role / last_login_at / fail_count / locked_until + onLoginSuccess/onLoginFailure 헬퍼 |
| `entity/AuthRefreshToken.java` | jti PK / user_id / expires_at / revoked / created_at + isUsable() |
| `entity/AuthRsaKey.java` | kid PK / public_pem / private_pem_enc / active / expires_at + isExpired() |
| `repository/AuthUserRepository.java` | findByUsername / existsByUsername / count |
| `repository/AuthRefreshTokenRepository.java` | findByJti / **revokeAllByUserId** (비번 변경 / 본인 탈퇴 / 보안사고용) / deleteExpired |
| `repository/AuthRsaKeyRepository.java` | findByActiveTrue / findByExpiresAtAfter (JWKS 응답용) / **deactivateAll** (회전) / deleteExpired |

**검증** (PG 직접 확인 — `psql -h localhost -p 29001 -U k1m -d orchestrator`):
- ✅ `./gradlew compileJava` BUILD SUCCESSFUL (11s)
- ✅ `./gradlew bootRun` Started in 4.476s
- ✅ HikariPool-1 Start completed (DB 연결 OK)
- ✅ 자동 테이블 3개 생성:
  - `auth_users` — id PK + uk_auth_users_username + 9 컬럼
  - `auth_refresh_tokens` — jti PK + 2 인덱스 (user_id, expires_at) + 5 컬럼
  - `auth_rsa_keys` — kid PK + 2 인덱스 (active, expires_at) + 6 컬럼
- ✅ JPA repository 3개 자동 빈 등록 (`Spring Data JPA repositories in DEFAULT mode` 로그 후 3개 발견)

**주의**:
- ddl-auto=update 는 컬럼 코멘트(`@Comment`) 미반영. 필요시 별 처리 (사용자 정책 확정 후)
- Spring Security 가 default chain 자동 활성 — Step 7 SecurityConfig 작성 시 정정 (현재 모든 endpoint 가 보안 default 로 보호 중)

### Step 3 결과 ✅

**작성된 파일** (2개):
| 파일 | 내용 |
|------|------|
| `service/KeyService.java` | RSA 2048 페어 생성 (`KeyPairGenerator`) / PEM 변환 (X509+PKCS8) / jasypt 암복호화 / DB CRUD / `rotate()` (deactivateAll + 새 페어 + cleanup) / `findActiveKey()` / `findAllValidPublicKeys()` (JWKS 응답용) / `findPublicKeyByKid()` / public utility `parsePublicKey/parsePrivateKey` |
| `bootstrap/InitialKeyLoader.java` | `@EventListener(ApplicationReadyEvent)` — active 키 0건이면 `generateAndSave(true)` |

**구조 결정**:
- 메모리 캐시 미사용 — 매 호출마다 DB 조회 (RSA 사용 빈도 낮아 부담 적음, 회전 시 일관성 ↑)
- `record ActiveKey(kid, publicKey, privateKey)` / `record PublicKeyInfo(kid, publicKey)` — Java 17 record 활용
- jasypt `StringEncryptor` bean — `jasyptStringEncryptor` Qualifier 로 자동 주입

**검증** (실제 DB 확인):
- ✅ `compileJava` 성공 (10s)
- ✅ 첫 부팅: `[InitialKeyLoader] no active RSA key — generating new one` → `[KeyService] new RSA key generated: kid=K-2026-05-04-021f8631-..., active=true, expiresAt=2026-05-12T14:51:18`
- ✅ DB INSERT 검증:
  ```
  kid=K-2026-05-04-021f8631-... / active=t / created=5/4 / expires=5/12 (정확히 8일 후)
  pub_pem 450자 (RSA 2048 PEM 표준) / private_pem_enc 2328자 (jasypt ENC 적용)
  ```
- ✅ **재부팅 검증** — `[InitialKeyLoader] active RSA key already exists: kid=K-2026-05-04-021f8631-...`
- ✅ DB row 그대로 1건 (재생성 X) — jasypt 복호화 + DB SELECT 정상

**다음 검증 항목 (후속 Step)**:
- 회전 (`KeyService.rotate()`) — Step 8 (KeyRotationJob 작성 후 강제 트리거)
- JWKS endpoint (`findAllValidPublicKeys()` → JSON 응답) — Step 7

### Step 4 결과 ✅

**작성된 파일** (2개):
| 파일 | 내용 |
|------|------|
| `service/TokenService.java` | RS256 발급 (`Jwts.builder().signWith(privateKey, RS256)`) / 검증 (`SigningKeyResolverAdapter` 로 kid → publicKey 자동 lookup) / `requireIssuer` + `requireAudience` 자동 검증 / Access(15min) + Refresh(7day, type=refresh) 분리 / `record RefreshToken(jti, jwt, expiresAt)` |
| `test/.../TokenServiceTest.java` | `@SpringBootTest` + `@Transactional` 롤백 / 7 테스트 |

**구조 결정**:
- `SigningKeyResolverAdapter` 패턴 — extractKid 별도 함수 안 만들고 jjwt 가 알아서 header.kid 추출 + 동적 publicKey 주입
- DB INSERT 는 TokenService 가 책임지지 않음 — AuthService 가 receive `RefreshToken` 후 INSERT
- access vs refresh 구분 = `type=refresh` claim — verify 시 type 검사

**검증 — 7/7 PASS**:
| # | 테스트 | 의도 |
|:--:|---|---|
| 1 | Access 발급 → 검증 정상 (sub/role/iss/aud/jti) | 기본 발급/검증 동작 |
| 2 | Refresh 발급 → 검증 정상 (jti / type=refresh) | refresh 토큰 메타 OK |
| 3 | Access 토큰을 refresh 검증에 쓰면 거부 | 타입 혼용 차단 |
| 4 | Refresh 토큰을 access 검증에 쓰면 거부 | 타입 혼용 차단 |
| 5 | payload 변조 시 거부 | 변조 검증 (RuntimeException 광범위) |
| 6 | signature 변조 시 SignatureException | 서명 검증 핵심 |
| 7 | kid 매칭 실패 시 거부 (§12.6 #10) | JWKS 에 없는 kid 거부 |

**구현 발견**:
- jjwt 0.11.5 의 payload 변조 → exception type 은 변조 형태 따라 다양 (SignatureException / IllegalArgumentException / MalformedJwtException). `RuntimeException` 으로 넓게 잡음 — 의미는 "변조 시 거부" 살림.

**검증 시나리오 매트릭스 (§12.6) 진행**:
- ✅ #8 JWT 서명 검증 (정상)
- ✅ #9 JWT 서명 검증 (위조 — signature 변조)
- ✅ #10 JWT header kid 매칭 실패
- ✅ #12 iss/aud 검증 (jjwt requireIssuer/requireAudience 자동)
- ⏳ #11 JWT exp 만료 — Step 6 (AuthService) 통합 또는 시간 mock 필요

### Step 5 결과 ✅

**작성된 파일** (4개):
| 파일 | 내용 |
|------|------|
| `config/PasswordEncoderConfig.java` | BCryptPasswordEncoder bean (round 12, yml 토글) |
| `dto/UserDto.java` | record (id/username/name/createdAt) — password_hash/role/내부메타 제외 |
| `service/UserService.java` | addUser / listUsers / deleteMe / changeMyPassword + 검증 헬퍼 (username 50자 / PW 8자 / name blank) |
| `test/.../UserServiceTest.java` | `@SpringBootTest` + `@Transactional` 롤백, 10 테스트 |

**구현 결정**:
- IllegalStateException = 비즈니스 충돌 (USERNAME_DUPLICATE / LAST_USER_CANNOT_DELETE)
- IllegalArgumentException = 입력 검증 / 사용자 못 찾음 (PASSWORD_TOO_SHORT / NAME_REQUIRED / USER_NOT_FOUND / CURRENT_PASSWORD_MISMATCH)
- 메시지 = enum 같은 코드 (Step 7 Controller 에서 HTTP 코드 매핑 시 활용)
- 비번 변경 / 본인 탈퇴 → `revokeAllByUserId` 일괄 revoke (다른 디바이스 강제 로그아웃)

**발견 / 정정**:
- ❌ JPA `@Modifying` UPDATE 후 영속성 컨텍스트 stale → assertTrue 가 false
- ✅ fix — `@Modifying(clearAutomatically=true, flushAutomatically=true)` 4개 query 모두 적용
  - AuthRefreshTokenRepository.revokeAllByUserId / deleteExpired
  - AuthRsaKeyRepository.deactivateAll / deleteExpired

**검증 — UserServiceTest 10/10 PASS**:
| # | 테스트 | 시간 |
|:--:|---|:--:|
| 1 | 사용자 추가 + 목록 노출 (password_hash 미노출) | — |
| 2 | username 중복 → USERNAME_DUPLICATE | 0.23s |
| 3 | PW 8자 미만 → PASSWORD_TOO_SHORT | 0.007s |
| 4 | name 비어있음 → NAME_REQUIRED | 0.006s |
| 5 | 마지막 1명 본인 탈퇴 → LAST_USER_CANNOT_DELETE | 0.24s |
| 6 | 2명 이상이면 탈퇴 + refresh 일괄 revoke | 0.74s |
| 7 | 존재하지 않는 user 삭제 → USER_NOT_FOUND | 0.24s |
| 8 | 비번 변경 — current 일치 + refresh revoke (다른 디바이스 강제 로그아웃) | 1.13s |
| 9 | 비번 변경 — current 불일치 → CURRENT_PASSWORD_MISMATCH | 0.67s |
| 10 | 새 비번 8자 미만 → PASSWORD_TOO_SHORT | 0.46s |

**검증 시나리오 매트릭스 (§12.6) 진행**:
- ✅ #21 비번 변경 → refresh 전체 revoke (다른 디바이스 강제 로그아웃)
- ✅ #23 마지막 1명 본인 탈퇴 차단 (count == 1 → 409)

**누적 테스트 — 총 17/17 PASS** (TokenServiceTest 7 + UserServiceTest 10)

### Step 6 결과 ✅

**작성된 파일** (2개):
| 파일 | 내용 |
|------|------|
| `service/AuthService.java` | login (BCrypt + locked 체크 + fail_count + 5회 잠금 30min + timing-safe dummy hash) / refresh (jjwt 검증 + DB jti 검증 + 1회용 회전) / logout (idempotent — 위조/만료 silent) / record `LoginResult` + `RefreshResult` |
| `test/.../AuthServiceTest.java` | 10 테스트 (login/잠금/refresh/회전/위조/로그아웃) |

**구현 결정**:
- **timing-safe** — 사용자 없을 때도 BCrypt 1회 dummy 매칭 (`@PostConstruct` 로 초기화한 dummyHash)
- 잠금 임계치 5회 / 30min (상수)
- ACCOUNT_LOCKED = IllegalStateException (잠긴 정상 케이스)
- INVALID_CREDENTIALS = IllegalArgumentException (사용자 없음 + 비번 틀림 동일 — username 노출 leak 방지)
- REFRESH_REVOKED_OR_EXPIRED / REFRESH_NOT_FOUND / USER_NOT_FOUND = IllegalArgumentException
- 로그아웃 = silent — 어떤 토큰이든 idempotent (cookie 만료가 본질)

**검증 — AuthServiceTest 10/10 PASS**:
| # | 테스트 | 시간 |
|:--:|---|:--:|
| 1 | 로그인 성공 — access/refresh + last_login_at + DB INSERT | 0.51s |
| 2 | 잘못된 비번 → fail_count++ + INVALID_CREDENTIALS | 0.46s |
| 3 | 5회 연속 실패 → 30분 잠금 | 1.34s |
| 4 | 잠긴 계정 → ACCOUNT_LOCKED (정상 비번이라도) | — |
| 5 | 존재하지 않는 사용자 → INVALID_CREDENTIALS (timing-safe) | 0.23s |
| 6 | refresh 정상 회전 — 새 토큰 + 기존 jti revoked | 0.50s |
| 7 | 같은 refresh 두 번 사용 → REFRESH_REVOKED_OR_EXPIRED (1회용) | 0.50s |
| 8 | 위조 refresh → SignatureException 등 거부 | 0.79s |
| 9 | 로그아웃 → jti revoked → refresh 갱신 불가 | 0.48s |
| 10 | 로그아웃 idempotent — 위조 토큰 silent | 0.009s |

**검증 시나리오 매트릭스 (§12.6) 진행**:
- ✅ #6 로그인 BCrypt 검증 (정확)
- ✅ #7 로그인 BCrypt 검증 (틀림 + fail_count + 5회시 423)
- ✅ #15 refresh jti 검증 (정상 회전)
- ✅ #16 refresh revoked → 거부
- ✅ #22 로그아웃 → jti revoked
- ✅ #24 잠긴 사용자 로그인 시도 거부

**누적 테스트 — 총 27/27 PASS** (TokenService 7 + UserService 10 + AuthService 10)

### Step 7 결과 ✅

**작성된 파일** (10개):
| 파일 | 내용 |
|------|------|
| `dto/LoginRequest.java` | record (username, password) |
| `dto/AddUserRequest.java` | record (username, password, name) |
| `dto/ChangePasswordRequest.java` | record (currentPassword, newPassword) |
| `config/AuthCookieHelper.java` | ResponseCookie 헬퍼 (HttpOnly+SameSite=Strict+Secure(yml)+TTL+Path) — access/refresh/expire 4 메서드 |
| `config/AuthCookieFilter.java` | OncePerRequestFilter — accessToken cookie → TokenService 직접 검증 → SecurityContext set |
| `config/SecurityConfig.java` | Spring Security 5 (CSRF disable / Stateless / antMatchers permit / AuthCookieFilter 등록 / **AuthenticationEntryPoint** = 401 AUTH_REQUIRED) |
| `controller/AuthController.java` | login (cookie set + body user) / refresh (cookie set + 204) / logout (cookie 만료 + 204 idempotent) / me |
| `controller/UserController.java` | POST /users (peer multi) / GET /users / DELETE /users/me (cookie 만료) / PATCH /users/me/password |
| `controller/JwksController.java` | `/.well-known/jwks.json` — Nimbus `RSAKey.Builder` 로 JWK 변환 + `JWKSet.toJSONObject()` |
| `controller/GlobalExceptionHandler.java` | IllegalArgument → 401/404/400 / IllegalState → 423/409 / JwtException → 401 (AUTH_DESIGN §13.8 매트릭스 정합) |

**구현 결정**:
- **AuthCookieFilter** = auth 모듈 자체 인증 (TokenService 직접 사용, JWKS 호출 X — 자기 발급기). sync-agent-common 의 JwtCookieAuthFilter (검증자 모듈 용 — JwksClient) 와는 별도.
- **ResponseCookie** 사용 — Spring 의 SameSite 지원 (servlet `Cookie` 는 SameSite 직접 못 박음)
- **AuthenticationEntryPoint** — Spring Security 디폴트 (403) 가 아닌 **401 + AUTH_REQUIRED** 박음 (응답 정책 §13.8)
- **antMatchers** — Spring Boot 2.7 의 Spring Security 5.7.x 에서는 requestMatchers 미지원 (5.8+)

**검증 — curl 시나리오**:
| 시나리오 | 결과 | 정합? |
|---|---|:--:|
| `/actuator/health` | 200 | ✅ |
| `/.well-known/jwks.json` | 200 + `keys: 1, kid=K-2026-05-04-021f8631-...`, `kty=RSA` | ✅ (현재 active 1쌍) |
| `/api/auth/me` 인증 없이 | **401 + `{"error":"AUTH_REQUIRED"}`** | ✅ §13.8 매트릭스 정합 |
| `/api/auth/users` POST 인증 없이 | **401 + `{"error":"AUTH_REQUIRED"}`** | ✅ |
| `/api/auth/login` (없는 사용자) | 401 + `{"error":"INVALID_CREDENTIALS"}` | ✅ §13.8 정합 |

**발견 / 정정**:
- ❌ Spring Security 디폴트 = 인증 없는 요청에 403. AUTH_DESIGN §13.8 = 401 AUTH_REQUIRED.
- ✅ fix — `.exceptionHandling(eh -> eh.authenticationEntryPoint(...))` 추가 — 401 + JSON 박음
- AccessDeniedHandler 도 같이 — 인가 실패는 403 + FORBIDDEN

**검증 시나리오 매트릭스 (§12.6) 진행**:
- ✅ #13 JWKS 응답 (활성+만료안된 비활성 키 노출 — 현재 active 1쌍만 있어 1개 응답)
- ✅ #25 인증 없이 보호 endpoint → 401 (AUTH_REQUIRED)

**다음 검증 (Step 11)**:
- 사용자 발급 후 (Step 9 CLI) 전체 흐름 — 로그인 → cookie 받기 → /me → 사용자 추가 → 로그아웃 등

### Step 8 결과 ✅

**작성된 파일** (2개):
| 파일 | 내용 |
|------|------|
| `scheduler/KeyRotationJob.java` | `@Scheduled(cron = "${auth.rsa.rotation-cron:0 0 0 * * ?}")` → `KeyService.rotate()` wrapper. `@EnableScheduling` 은 AuthApplication 에 박혀있음 |
| `test/.../KeyRotationTest.java` | 4 테스트 — KeyService.rotate() 직접 호출 검증 (cron 자체는 Spring 신뢰) |

**구현 결정**:
- KeyRotationJob = 단순 wrapper. 실제 회전 로직은 KeyService.rotate() (Step 3 작성)
- 테스트는 KeyService.rotate() 직접 호출 — 시간 경과 mock 없이 검증

**검증 — KeyRotationTest 4/4 PASS**:
| # | 테스트 | 시간 |
|:--:|---|:--:|
| 1 | 새 active 키 생성 + 기존 active 비활성화 + active=true 정확히 1개 | 0.1s |
| 2 | 만료된 키 cleanup DELETE (회전 시 통합 처리) | 0.28s |
| 3 | 회전 후 이전 active 키도 JWKS 응답에 포함 (만료 안 됐으면) — 이전 토큰 검증 보장 | 0.96s |
| 4 | 회전 직후 findActiveKey() = 새 kid | 0.13s |

**검증 시나리오 매트릭스 (§12.6) 진행**:
- ✅ #18 RSA 자정 회전 (deactivate + new active + cleanup)
- ✅ #19 회전 후 이전 kid 토큰 검증 가능 (JWKS 응답에 포함)

**누적 테스트 — 총 31/31 PASS** (TokenService 7 + UserService 10 + AuthService 10 + KeyRotation 4)

### Step 9 결과 ✅

**작성된 파일** (1개):
| 파일 | 내용 |
|------|------|
| `tools/UserGeneratorCli.java` | Spring context 안 띄움 (가벼운 단독 실행). SnakeYAML 로 application.yml 파싱 → jasypt ENC 복호화 → BCrypt(12) 해시 → JDBC INSERT. UNIQUE 충돌 → exit 2 / 입력 검증 fail → exit 3 |

**구현 결정**:
- Spring 안 띄우는 방식 — 부팅 시간 0 (CLI 1회용 실행)
- yml 직접 파싱 — datasource ENC 자동 복호화
- 입력 검증 = UserService 와 동일 정책 (PW 8자 / NAME 50자 / USERNAME 50자)
- 의존: `org.yaml:snakeyaml` (Spring Boot 자동 의존), `org.jasypt:jasypt-1.9.3` (jasypt-spring-boot 가 끌어옴), `BCryptPasswordEncoder` (Spring Security crypto)

**검증** (실제 PG):
```
=== 1. 첫 발급 ===
$ ./gradlew createUser --args="admin pass1234 운영자"
User created: id=27, username=admin, name=운영자
BUILD SUCCESSFUL

=== 2. 재실행 (UNIQUE 충돌 기대) ===
$ ./gradlew createUser --args="admin anotherPw 다른사람"
ERROR: USERNAME_DUPLICATE — 'admin' already exists
BUILD FAILED (exit 2)

=== 3. DB 확인 ===
id=27, username=admin, name=운영자, role=user
hash_len=60 (BCrypt 표준), fail_count=0
```

> id=27 인 이유 = 이전 단위 테스트 (UserServiceTest, AuthServiceTest) 가 sequence 진척시킴 (트랜잭션 롤백 후에도 sequence 는 안 되돌아감). 동작 정상.

**검증 시나리오 매트릭스 (§12.6) 진행**:
- ✅ #5 UserGeneratorCli 사용자 0명 → 1명 발급 (BCrypt hash 60자) + 재실행 UNIQUE 충돌 (exit 2)

**시스템 상태**:
- DB 에 첫 운영자 admin (pass1234, 운영자) 살아있음 — Step 11 통합 검증 (curl) 에서 활용

### Step 10 결과 ✅ — Phase 1.5 (sync-agent-common 검증자 자산)

**작성된 파일** (5개 + build.gradle 수정):
| 파일 | 내용 |
|------|------|
| `sync-agent-common/build.gradle` (수정) | jjwt 0.11.5 (api+impl+jackson) / Nimbus JOSE 9.37 (api) / Spring Security 5.7.10 (compileOnly) / Mockito (test) 추가 |
| `auth/AuthErrorCode.java` | enum 6종 — AUTH_REQUIRED / TOKEN_EXPIRED / INVALID_SIGNATURE / INVALID_TOKEN / UNKNOWN_KEY_ID / AUTH_SERVICE_UNAVAILABLE (httpStatus + message) |
| `auth/AuthErrorResponse.java` | write 헬퍼 — 401/503 JSON + Retry-After: 30 (503만) |
| `auth/AuthFetchFailedException.java` | RuntimeException — JWKS fetch retry 2회 후 실패 시 |
| `client/JwksClient.java` | RestTemplate + 5min 캐시 + retry 2회 (200ms/500ms) — Nimbus `JWKSet.parse` 로 파싱 / `@ConditionalOnProperty(auth.jwks-url)` 토글 / synchronized refresh (race 방어) / 회전 직후 1회 더 fetch (kid 미스 시) |
| `config/JwtCookieAuthFilter.java` | OncePerRequestFilter — cookie 추출 → kid 추출 → JwksClient lookup → jjwt verify → SecurityContext set / 6 종류 예외 → AuthErrorResponse 매핑 / `@ConditionalOnProperty(jwt.cookie.enabled=true)` 토글 |
| `test/.../JwksClientTest.java` | 5 테스트 — Mockito + 실 RSA 페어 + 가짜 JWKS 응답 |

**구현 결정**:
- ApiKeyFilter (기존) 와 별도 패키지 (`auth/`, `client/`, `config/`) — 충돌 0
- 양쪽 토글 패턴 일관 — `@ConditionalOnProperty(jwt.cookie.enabled)` (sync-agent-bojo 등 비활성 = 영향 0)
- JwksClient 의 record `JwkParseException` 은 사용처 없는 보조 — 추후 정리 가능
- `synchronized refresh()` — 동시 fetch race 방어 (이미 갱신했으면 skip)

**검증** (단위 테스트 5/5 PASS + 회귀):
| 테스트 | 시간 | 의도 |
|---|:--:|---|
| 정상 fetch — kid 매칭 공개키 반환 | 0.11s | 기본 동작 |
| kid 매칭 실패 → empty | 2.65s | 회전 직후 1회 더 fetch 후에도 없으면 empty (호출자 401 처리) |
| retry 후 성공 — 1차 fail / 2차 success | 0.46s | 일시 끊김 자동 회복 |
| retry 2회 모두 실패 → AuthFetchFailedException | 0.74s | §13.8 옵션 C / 503 신호 |
| 캐시 hit — 두 번째 호출은 RestTemplate 안 부름 | 0.19s | 5min 캐시 동작 |

**JAR 복사 + 회귀 검증**:
```
✅ copied to infolink-api-collector/libs/
✅ copied to sync-agent-bojo/libs/
✅ copied to sync-agent-bojo-int/libs/
✅ copied to sync-agent-others/libs/
✅ copied to sync-agent-provide/libs/
✅ copied to sync-orchestrator/backend/libs/
✅ copied to sync-proxy-dmz/libs/
✅ copied to sync-proxy-internal/libs/

회귀: sync-agent-bojo clean compile → BUILD SUCCESSFUL (14s)
```

**검증 시나리오 매트릭스 (§12.6) 진행**:
- ✅ #14 검증자 모듈 JWKS 캐시 (첫 호출 fetch / 캐시 hit / retry / 503)

**누적 테스트 — 총 36/36 PASS** (TokenService 7 + UserService 10 + AuthService 10 + KeyRotation 4 + JwksClient 5)

**기존 모듈 영향 0**:
- `jwt.cookie.enabled` 미설정 → JwtCookieAuthFilter 비활성
- `auth.jwks-url` 미설정 → JwksClient bean 자체 미생성
- ApiKeyFilter / 다른 기존 코드 변경 0
- sync-agent-bojo clean rebuild 성공 = 기존 빌드 영향 0

### Step 11 결과 ✅ — 통합 검증 (curl)

**시나리오 17/17 PASS**:

| # | 시나리오 | 응답 | 정합? |
|:--:|---|---|:--:|
| 1 | `/actuator/health` | 200 | ✅ |
| 2 | `/.well-known/jwks.json` | 1개 키 (kid=K-2026-05-04-...) | ✅ |
| 3 | `/api/auth/me` 인증 없이 | 401 AUTH_REQUIRED | ✅ §13.8 |
| 4 | `POST /api/auth/login` (admin/pass1234) | 200 + user JSON + cookies set | ✅ |
| 5 | `GET /api/auth/me` with cookies | 200 + user (id=27, admin, 운영자) | ✅ |
| 6 | `POST /api/auth/users` (peer multi — alice 추가) | 201 + user (id=29, alice, 앨리스) | ✅ peer multiplication |
| 7 | `GET /api/auth/users` 목록 | admin + alice 2명 | ✅ |
| 8 | `POST /api/auth/refresh` | 204 + 새 cookies | ✅ 1회용 회전 |
| 9 | 새 cookies 로 `/me` | 200 | ✅ |
| 10 | `POST /api/auth/logout` | 204 + cookies 만료 | ✅ |
| 11 | 만료 cookie 로 `/me` | 401 AUTH_REQUIRED | ✅ |
| 12 | 잘못된 비번 로그인 | 401 INVALID_CREDENTIALS | ✅ |
| 13 | alice 로그인 (별 cookie jar) | 200 | ✅ |
| 14 | alice 본인 탈퇴 (2명 → 1명) | 204 | ✅ |
| 15 | admin 재로그인 | 200 | ✅ |
| 16 | admin 본인 탈퇴 시도 (마지막 1명) | **409 LAST_USER_CANNOT_DELETE** | ✅ §12.6 #23 |
| 17 | 위조 cookie 로 `/me` | 401 AUTH_REQUIRED | ✅ |

**검증 시나리오 매트릭스 (§12.6) E2E 진행**:
- ✅ #5 첫 사용자 발급 (Step 9 CLI)
- ✅ #6 로그인 BCrypt 검증 (정확)
- ✅ #7 로그인 BCrypt 검증 (틀림)
- ✅ #11 access exp — refresh 자동 갱신 패턴 (cookie 만료 후 401)
- ✅ #13 JWKS 응답 (현재 active 1쌍 노출)
- ✅ #15 refresh 정상 회전
- ✅ #16 refresh revoked 거부
- ✅ #17 logout 후 access 만료 → 401
- ✅ #22 로그아웃 → jti revoked
- ✅ #23 마지막 1명 본인 탈퇴 → 409
- ✅ #25 인증 없이 보호 endpoint → 401
- ✅ #27 위조 토큰 → 401

**시스템 최종 상태** (Step 11 완료 후):
- DB `auth_users` — admin 1명 (id=27, alice 는 본인 탈퇴로 사라짐)
- DB `auth_rsa_keys` — 1개 (active=true, kid=K-2026-05-04-021f8631-...)
- DB `auth_refresh_tokens` — 다수 (revoked 처리된 것들)

---

## Phase 1 + Phase 1.5 — 최종 요약

### 완료
- **신규 모듈 sync-orchestrator-auth (8096)** — 16 Java 파일 + yml + build.gradle
- **sync-agent-common 검증자 자산** — 5 파일 추가, 8개 의존 모듈에 JAR 복사
- **단위/통합 테스트 36/36 PASS**
- **E2E curl 17/17 PASS**

### 기존 시스템 영향
- ✅ **0** — 기존 ApiKeyFilter / 다른 코드 / Frontend / 운영 화면 변경 없음
- ✅ JwtCookieAuthFilter / JwksClient 토글 비활성 (default) — 검증자 모듈 부팅 영향 0
- ✅ sync-agent-bojo clean rebuild 성공

### 검증 시나리오 매트릭스 (§12.6) — 28개 중 본 세션 커버
- ✅ #3, #4, #5, #6, #7, #8, #9, #10, #11(부분), #12, #13, #14, #15, #16, #17, #18, #19, #21, #22, #23, #24, #25, #27 — **23개**
- ⏳ #1, #2 (yml ENC 복호화 운영) / #20 (회전 후 검증자 5min 캐시 갭) / #26 (api-provider permitAll) / #28 (X-API-Key 회귀) — Phase 2~4 후속 또는 운영 환경 검증

### Phase 2~5 미진행 (별 세션)
- Phase 2: Backend (8080) 통합
- Phase 3: api-provider (8095) 통합 + MockApiKeyController 토글
- Phase 4: api-collector (8084/8094) 통합 + MockApiController 토글
- Phase 5: Frontend (3000) — 로그인/헤더/사용자 관리 화면

### Phase 2~5 dev_plan 작성 (커밋 후 추가)

본 세션 시간 부족 + Phase 2~4 적용 시점부터 모든 운영 화면 401 (다른 테스트/운영 작업 영향) 으로 별 세션 진행 결정. 다음 세션 즉시 시작 가능하도록 plan 4개 사전 작성:

| 파일 | 라인 | 추정 시간 |
|------|:----:|:--:|
| `dev_plan/2026_05/04/auth-system-phase2-backend.md` | 148 | ~1.5h |
| `dev_plan/2026_05/04/auth-system-phase3-api-provider.md` | 149 | ~1.5~2h |
| `dev_plan/2026_05/04/auth-system-phase4-api-collector.md` | 152 | ~1.5h |
| `dev_plan/2026_05/04/auth-system-phase5-frontend.md` | 233 | ~5~6h |
| **합계** | 682 | **~10~12h (1.5~2일)** |

**핵심 — Phase 2~5 한 묶음 배포 필요**:
- Phase 2~4 토글 활성 시점 = 운영 화면 401 즉시 발생
- Phase 5 (로그인 화면) 미준비 상태에서 Phase 2~4 단독 적용 = 운영 마비
- 권장 = 한 묶음 배포 + 운영자 사전 안내 + auth CLI 로 첫 사용자 발급

**Phase 별 주의 사항**:
- Phase 2: `/api/callback/**` permitAll (Agent → Backend 시스템 간 콜백)
- Phase 3: provide/manage/mock 3-way path 분리, Type A/B 외부 호출 회귀 검증 필수
- Phase 4: NGW_0118 LOOKUP 영향 검증 + DMZ/Internal 양쪽 yml
- Phase 5: 가장 큰 작업 (~5~6h), 운영자 사전 안내 필수

---

## 다음 할 일

- Step 2: DB 스키마 + Entity + Repository
- Step 3 이후 순차

---

## 수정 파일 / 신규 파일

### 문서 (오전)
| 파일 | 내용 |
|------|------|
| `docs/AUTH_DESIGN.md` | 정정 (881 → 1424줄) |
| `docs/AUTH_FLOW.md` | **신규** 856줄 |
| `dev_plan/2026_05/04/auth-system-phase1.md` | **신규** 687줄 |

### 코드 (오후 진행 중)
| 모듈 | 파일 | 내용 |
|------|------|------|
| (작업 후 기록) | | |
