# 2026-05-13 작업 일지

> 세션 중단 사유: PC 렉 심해 재부팅 필요 → 이 시점까지 문서화. 코드 변경분 미커밋(검증 전), 서비스는 재부팅으로 다 내려감.

## 요약 (보고용)

| 파트 | 상태 | 비고 |
|------|:----:|------|
| 서비스 7종 기동 (backend/proxy-dmz/proxy-internal/others-dmz/bojo-internal/auth/frontend) | 완료 | 초기 cwd 헷갈려 1차 실패(절대경로로 재시도). Docker(29001~) 4분 전 기동 — pg_isready 확인 후 OK. bojo-dmz(8082)는 04-others/trace 검증에 불요라 미기동. |
| **04-others §4-후속: `internal-jeju-loader`(agent 32) 디버깅** — Oracle IDENTITY 시퀀스 미동기화 | **수정 완료(테스트DB)** | `TM_GD970001.BRNCH_ID` / `TM_GD120001.GWEL_NO` 가 `GENERATED BY DEFAULT AS IDENTITY` 인데 시드 덤프를 값 명시 INSERT 해서 시퀀스가 안 올라감 → 로더 INSERT 시 `nextval`(41/21)이 시드 row 와 충돌 ORA-00001. `ALTER TABLE ... MODIFY (col GENERATED BY DEFAULT AS IDENTITY START WITH LIMIT VALUE)` 로 시퀀스 점프(→1207 / →90002). 코드 무변경 — 시드 셋업 누락. |
| **`JejuFacilityLoadStep` 테이블명 오타 fix** — `TC_GD00002`→`TC_GD000002`, `TC_GD00100`→`TC_GD000100` | **완료** | 코드가 레거시 코드테이블 이름을 0 하나 빠뜨려 적음 (ORA-00942). 실제 우리 Oracle = `TC_GD000002`(NGW_0003 4건/NGW_0013 34건), `TC_GD000100`(226건), 컬럼 일치. 표준화 매핑 없는 코드테이블이라 그게 정식 이름. bojo-internal 재빌드·재기동. |
| **source_ref 생성 공통화** — Internal Loader 커스텀 step 6개 + 양식 통일 | **코드·빌드 완료 / 재실행 검증 대기** | survey 결과 양식은 `SourceRefUtils.build`(`zone:dsId:tbId:pk`, 숫자) 하나인데 Internal Loader 6개만 `String.format("[\"I:%s:%s:%s\"]", sourceDsId(문자열), ifTable(이름), ifId)` 손코딩 → 비표준. `SourceRefUtils.build` 를 `Object... pkValues` varargs 로 통합(+`buildComposite` 제거), `buildJson` 헬퍼 추가, 6개 step + DMZ `LoaderStepHelper` + `SourceToTargetStep`/`SaeolLinkPlanSndStep` 전부 `buildJson` 으로. common build → JAR 9모듈 복사 → bojo-internal/bojo-dmz 재빌드(둘 다 SUCCESS) → bojo-internal 재기동. **제주 `...635a22ca` 회차 데이터 정리(8테이블 execution_id 필터 삭제 + IF 3테이블 PENDING)까지 완료. 재실행→검증은 다음 세션.** |
| trace fix(5/12분, tableId 기반) 검증·커밋 | **미완** | proxy 재기동 후 검증 예정이었으나 source_ref 작업 우선순위로 미룸. 코드는 5/12 에 이미 됨, common JAR 에도 포함됨(오늘 또 common 재빌드했으니 같이 들어있음). |

## 진행 상세

### ① 서비스 기동
- 7종 `./gradlew bootRun` (frontend 는 `npm run dev`) 백그라운드. backend(8080)/auth(8096) health 는 401(인증보호 — 정상), proxy/agent 는 200, frontend 307(리다이렉트). 전부 정상.
- gradle 데몬 정리: `infolink-agent-common && ./gradlew --stop` → "No Gradle daemons running" (전날 다 kill 됨). java 프로세스 1개 = IntelliJ 자체.

### ② internal-jeju-loader — IDENTITY 시퀀스 미동기화 (ORA-00001)
- 사용자가 프론트에서 실행 → `MERGE INTO TM_GD970001 ... WHEN NOT MATCHED THEN INSERT (BRNCH_ID 미포함) ... ORA-00001 PK_TM_GD970001`.
- 진단: `TM_GD970001` PK = `BRNCH_ID`(OBSVTR_ID 아님), `GENERATED BY DEFAULT AS IDENTITY` (시스템 시퀀스 `ISEQ$$_77305`). 시드 1206건이 `BRNCH_ID` 1~1206 값 명시로 들어가 시퀀스 미상승 → `last_number=41` → 로더 INSERT 시 `nextval=41` → 시드 row 41 과 충돌. `TM_GD120001.GWEL_NO` 도 동일(last=21, MAX=90001). 나머지(TM_GD970101 last 18141>MAX 18138, TM_GD111010, PM_GD970201/202)는 이미 위로 밀려 있어 OK.
- fix: `ALTER TABLE K1M.TM_GD970001 MODIFY (BRNCH_ID GENERATED BY DEFAULT AS IDENTITY START WITH LIMIT VALUE)` + `TM_GD120001.GWEL_NO` 동일 → 시퀀스 1207 / 90002 로 점프. IF_RSV_TB_JEJU_JEWON FAILED 12건 → PENDING 복구.
- (참고) Internal Loader 기본 link_status 필터 = `PENDING/RESYNC` (FAILED 미포함 — DMZ 로더는 FAILED 포함). 비대칭 — 메모만.
- 사용자 재실행 → `jeju-jewon-load`✓(TM_GD970001 12 BRNCH 1207~1218 / TM_GD120001 +12 / TM_GD970130 +12 / TM_GD970002 +12 / TM_GD970101 +36), `jeju-obsvdata-load`✓(IF_RSV_TB_JEJU 12 SUCCESS / PM_GD970201 +27 / PM_GD970202 +9). `jeju-facility-load`에서 다음 에러 ↓.

### ③ JejuFacilityLoadStep — 코드테이블명 오타 (ORA-00942)
- `SELECT TRIM(UGWTR_COM_CD) AS CD, CD_CN FROM TC_GD00002 WHERE GROUP_CD_SN='NGW_0003'` → 테이블 없음.
- 실제 = `TC_GD000002` (0 셋), `TC_GD000100` (REGN 룩업). 코드가 `TC_GD00002`/`TC_GD00100` 으로 오기. 컬럼(`UGWTR_COM_CD`/`CD_CN`/`GROUP_CD_SN`, `STDG_CD`/`CTPV_NM`/...) 다 일치, 데이터 존재(NGW_0003 4 / NGW_0013 34 / TC_GD000100 226).
- fix: `JejuFacilityLoadStep.java` 의 SQL 2곳 + 주석 4곳 `TC_GD00002`→`TC_GD000002`, `TC_GD00100`→`TC_GD000100`. bojo-internal `clean build` → kill+jar 재기동(health 200).
- 사용자 재실행(이후) → facility step 도 처리됨, TM_GD111010 에 `["I:internal:IF_RSV_RGETSTGMS01:21"]` 형식 source_refs 생김 → 사용자 지적으로 다음 작업(④) 발생.

### ④ source_ref 생성 공통화 (계획서 `dev_plan/2026_05/13/jeju-loader-source-refs-propagate.md`)
- 사용자: "이 양식이 에이전트마다 다른 게 말이 안 된다, 공통화해라." → 전 에이전트 source_ref 생성 survey:
  - **양식 = `SourceRefUtils.build(context, tableName, pk)`** → `"{zone}:{dsDbId}:{tbId}:{pk}"` (전부 숫자 식별자). RCV(`SourceToTargetStep`) / DMZ Loader(`LoaderStepHelper`, pk=IF행 서러게이트 id) / SND(`SaeolLinkPlanSndStep`) 가 이걸 씀.
  - **Internal Loader 6개만 비표준**: `String.format("[\"I:%s:%s:%s\"]", sourceDsId, ifTable, ifId)` — dsId 자리에 문자열("internal"), tbId 자리에 테이블 이름, zone 하드코딩 "I". 시맨틱(pk=IF행 서러게이트 id)은 DMZ Loader 와 동일, 형식만 다름. 대상: `InternalBojoLoadStep`(보조), `SimpleLoadStep`(약수터 등), `UseLoadStep`(이용량 ×2), `JejuJewonLoadStep`/`JejuObsvdataLoadStep`/`JejuFacilityLoadStep`(제주).
  - orchestrator `ExecutionService`(L433~504)가 agent 유형 무관하게 `sourceDatasourceDbId`/`sourceZoneShortCode`/`sourceTableIds` 를 params 에 넣어줌 → internal loader 의 context 에도 숫자 메타 들어옴 → `build()` 가 internal 에서도 정상.
- 수정(코드, 미커밋):
  - `common SourceRefUtils.java` — `build(StepContext, String, Object pk)` + `buildComposite(StepContext, String, Object...)` → **`build(StepContext, String, Object... pkValues)` 하나로 통합** (1개=단일 PK, N개=`|` 조인). `buildComposite` 제거. `buildJson(StepContext, String, Object...)` = `toJsonSingle(build(...))` 추가. `buildFallback` 시그니처에서 미사용 tableName 제거. `import java.util.Arrays`.
  - `common SourceToTargetStep.java` — `build`/`buildComposite` 분기 + `toJsonSingle` → `SourceRefUtils.buildJson(context, sourceTable, pkValues)` 한 줄.
  - `others-dmz SaeolLinkPlanSndStep.java` — 동일.
  - `bojo-dmz LoaderStepHelper.java` — `build`+`toJsonSingle` 2줄 → `buildJson` (2곳: jewon/obsvdata). 동작 무변경.
  - `bojo-internal` 6개 step — `String.format("[\"I:...\"]")` → `SourceRefUtils.buildJson(context, ifTable, ifId)` + `import ...util.SourceRefUtils`. `UseLoadStep` 은 helper 메서드 `processLegacyData`/`processStatusData` 인자 `String sourceDsId` → `StepContext context` 로 교체(호출처 2곳 포함). `sourceDsId` 지역변수는 다른 step 들에선 `getJdbcTemplate`/`getDbType` 에 계속 쓰여 그대로 유지.
- 빌드: `infolink-agent-common clean build` → JAR(229440 bytes) 9모듈 `libs/` 복사 → `infolink-agent-bojo-internal` / `infolink-agent-bojo-dmz` `clean build` (둘 다 BUILD SUCCESSFUL) → bojo-internal kill(8092) + `java -jar build/libs/...` 재기동(health 200). bojo-dmz 는 미가동 — JAR 갱신만(다음 기동 때 반영).
- 데이터 정리(완료): Oracle 29004 K1M 에서 `EXECUTION_ID='internal-jeju-loader_635a22ca-b05a-486e-ba27-2032c9dc35f4'` 행 삭제 — TM_GD970001(12) / TM_GD120001(12) / TM_GD970130(12) / TM_GD970002(12) / TM_GD970101(36) / PM_GD970201(27) / PM_GD970202(9) (TM_GD111010 은 그 회차엔 0 — facility 는 직전 별도 실행이었음). IF 3테이블(IF_RSV_TB_JEJU_JEWON / IF_RSV_TB_JEJU / IF_RSV_RGETSTGMS01) link_status → 전부 PENDING(12/12/12). 공유테이블 잔존 = TM_GD970101 3618 / PM_GD970201 74628 / TM_GD970001 1206 (보조·시드분 보존 확인).
- **미완(다음 세션)**: `internal-jeju-loader` 재실행 → 새 source_refs 형식(`["{zone}:{dsId숫자}:{tbId숫자}:{ifId}"]`, internal Oracle zone 약어) + fan-out 정합성(BRNCH/GWEL 채번 일관) + trace-source(TM_GD111010→IF_RSV_RGETSTGMS01→DMZ if_snd_rgetstgms01) 검증. DMZ Loader 회귀(secJewon/secObsvdata 동작 무변경) 확인. 통과 시 dev_log 마저 + 커밋.

## 산출물

- `dev_plan/2026_05/13/jeju-loader-source-refs-propagate.md` — source_ref 공통화 계획(개정판: 처음 "제주 3개 IF source_refs 전파"로 시작 → survey 후 "Internal Loader 6개 SourceRefUtils 통일 + build varargs 통합"으로 확장).
- 코드 변경(전부 **미커밋**, 검증 후 커밋 예정):
  - `infolink-agent-common/.../util/SourceRefUtils.java` (build varargs 통합 / buildComposite 제거 / buildJson 추가)
  - `infolink-agent-common/.../step/SourceToTargetStep.java` (buildJson 사용)
  - `infolink-agent-others-dmz/.../snd/step/SaeolLinkPlanSndStep.java` (buildJson 사용)
  - `infolink-agent-bojo-dmz/.../loader/step/LoaderStepHelper.java` (buildJson 사용)
  - `infolink-agent-bojo-internal/.../loader/step/{InternalBojoLoadStep,SimpleLoadStep,UseLoadStep,JejuJewonLoadStep,JejuObsvdataLoadStep,JejuFacilityLoadStep}.java` (buildJson 사용 + TC 테이블명 fix in JejuFacilityLoadStep)
  - common JAR → 9모듈 `libs/` 복사. bojo-internal / bojo-dmz 재빌드.
  - **[미커밋, 5/12분]** `infolink-agent-common/.../controller/ExecutionDataController.java` + `ExecutionDataReader.java` (trace tableId 기반 해석)
- DB 변경(테스트 Oracle 29004): `TM_GD970001`/`TM_GD120001` IDENTITY START WITH LIMIT VALUE. `...635a22ca` 회차 행 삭제 + IF 3테이블 PENDING 복구.

## 다음 세션 시작 시

> 0) 재부팅 후 — gradle 데몬은 재부팅으로 다 내려감, 신경 안 써도 됨. Docker 컨테이너(29001~29010)도 재부팅 시 내려갈 수 있으니 `docker ps` 확인, 없으면 docker compose up. PG 부팅 대기(`docker exec gims_orchestrator_dmz pg_isready`) 후 서비스 기동.
> 1) 서비스 기동: backend(8080)/proxy-dmz(8083)/proxy-internal(8093)/others-dmz(8085)/bojo-internal(8092)/auth(8096)/frontend(3000). bojo-dmz(8082)는 DMZ Loader 회귀 확인하려면 필요. **재빌드 불요 — 이미 새 common JAR 로 다 빌드돼 있음** (bojo-internal jar / bojo-dmz jar 둘 다). bootRun 또는 `java -jar build/libs/...`.
> 2) auth 키 = K-2026-05-12 (만료 5/20), InitialKeyLoader 자가복구 — 신경 안 써도 됨.
- **A. source_ref 공통화 검증·커밋** (최우선 — 코드 다 됐고 데이터도 깔끔, 재실행만 남음): 프론트에서 `internal-jeju-loader` 실행 → 각 타겟 SOURCE_REFS = `["{zone}:{dsId숫자}:{tbId숫자}:{ifId}"]` (zone = IF_RSV 테이블 datasource zone 약어) 확인. fan-out 그룹(IF행→7행) 같은 source_refs + BRNCH/GWEL 채번 일관. trace-source: TM_GD111010 행 → `sourceTable=IF_RSV_RGETSTGMS01` FOUND → Proxy 경유 DMZ `if_snd_rgetstgms01` 까지. 회귀: DMZ Loader(secJewon/secObsvdata 적재 — bojo-dmz 기동해서), 보조 internal loader(돌려보거나 코드 검토). 통과 시 → trace fix(5/12) 검증도 같이(internal-jeju-rcv IF_RSV_TB_JEJU 행 trace-source) → **둘 다 같이 커밋** (또는 trace fix / source_ref 공통화 별 커밋).
- **B. 04-others 본 테스트 이어서** — 제주 §4 PASS, internal-jeju-rcv PASS, internal-jeju-loader (A 검증으로 자연히 PASS 처리). 다음: §5 이용량(agent 24 dmz-others-snd-use → internal-use-rcv → UseLoadStep) → 새올(agent 25, SaeolLinkPlanSndStep) → yaksoter(agent 43, SimpleLoadStep) → api-collect(agent 34). 끝나면 각 agent where-filters/retention. (이용량/약수터/보조 internal 은 ④에서 source_ref 형식 바뀌었으니 — 기존 옛 형식 타겟 데이터는 임의 클린 안 함, 각 테스트 때 정리 + trace 재검증)
- **C. 03-bojo §5~** Conditions 본 검증 → §6 Trace / §7 Schedule / §9 Frontend UI / §10 문서 + stable tag + Release.
- (별도) yml-where-filters Phase 5(백엔드 화이트리스트 가드) + 나머지 로더 where-filters yml 선언 + 기능 검증(GN-% 케이스). retention `RetentionCandidate` 주석에 "dateColumn date/timestamp 타입 필수" 명시 + string 날짜 지원 enhancement 계획.

## 메모

- 메모리 후보(승인 후 추가): "단건 source_refs JSON 은 `SourceRefUtils.buildJson(context, table, pk)` 만 사용 — `String.format("[\"...\"]")` 손코딩 금지. 복합키는 같은 메서드에 인자 여러 개. 형식 = `zone:dsId:tbId:pk` (전부 숫자). Loader 의 pk = IF행 서러게이트 id."
- `TM_GD970130`/`TM_GD970002` 시드 3건씩 있음(jeju 12 더하면 15) — jeju 외 다른 소스(보조?) 거나 초기 시드. 삭제는 execution_id 필터로 jeju 분만.
- Internal Loader link_status 기본 필터가 `PENDING/RESYNC` 라 부분실패(FAILED) 건 자동 재처리 안 됨 (DMZ 로더는 FAILED 포함). 통일할지 검토 후보.
- `TC_GD000002`/`TC_GD000100` = 표준화 매핑 없는 레거시 코드테이블 (이름 그대로 유지). 다른 코드테이블 룩업 쓰는 커스텀 step 있으면 같은 부류 오타 점검 필요할 수 있음.
- 세션 종료 시 전 서비스 OFF(재부팅). Docker 도 재부팅 시 같이 내려갈 수 있음.

---

# 2026-05-13 (재부팅 후 이어서 — 오후)

## 요약

| 파트 | 상태 | 비고 |
|------|:----:|------|
| 서비스 8종 재기동 (backend/proxy-dmz/proxy-internal/others-dmz/bojo-internal/bojo-dmz/auth/frontend) + Docker(29001~29010) | 완료 | 재부팅으로 다 내려가 있던 거 재기동. cwd persistence + `cmd && nohup ... &` 가 (cmd && nohup) & 로 파싱돼 subshell 에서 도는 점 때문에 재기동 명령 몇 번 헤맴 — 결국 다 정상. |
| **source_ref 공통화 검증** (`internal-jeju-loader` 5회차) | **PASS** | 사용자 재실행 → 새 source_refs 형식 + fan-out 채번 일관 확인. 제주 Loader 로직 복잡(제원 IF→5테이블 7행 / 수위 IF→PM, 관측소테이블은 제원 경로가 소유·수위 경로는 읽기만 → 역추적이 TM_GD970001 ← 제원 IF 만 표시하는 게 정상)도 사용자에게 설명·확인. |
| **처리현황 per-target 건수 오표시 fix + SyncLog 저장 공통화** | **완료·검증** | 발견: `PM_GD970202` 성공건수 36(=step write) vs 상세조회 9(실 데이터). 원인: 멀티타겟 step 이 `sync_log.target_tables` 를 단순배열로 저장 → 프론트가 step writeCount 를 모든 타겟에 동일 표시. + `JejuJewonLoadStep` writeCount = IF건수(12)로 underreport. fix: common `TableCountTracker`(테이블별 누적) + `SyncLogWriter`(sync_log 저장 단일 진입점, 단순배열 / `[{name,count}]` 2형식, sourcePkColumn 받음) 신규 → 8개 호출처(`SourceToTargetStep`/`LoaderStepHelper`/jeju 3 step/`SimpleLoadStep`/`UseLoadStep`/`InternalBojoLoadStep`) 전부 위임. 멀티타겟(jeju jewon/obsvdata, UseLoadStep use-legacy, InternalBojoLoad) 은 tracker 로 per-table 카운팅. `JejuJewonLoadStep` writeCount → 실 적재행수(84). `UseLoadStep.updateLastReceive` → 건수 리턴. 사용자 재실행 → 처리현황 = 상세조회 일치 확인. |
| **정방향 추적 — 복합 PK 소스 테이블 지원** | **완료·검증** | 발견: 04-others 이용량 클린 테스트 — `use_jeju_day`(PK `obsr_de,obsrvt_id` 복합) 소스 행 정방향 추적 0건(역추적은 정상). 원인: SND 가 쓴 source_refs = `["D:1018:107:20260325\|SC001"]`(복합키 `\|` 조인, 순서=DB 제약조건 KEY_SEQ), 근데 프론트가 `columns[0]` 한 컬럼만 보내고 백엔드는 `:tableId:pkValue"]` LIKE 패턴 만듦 → 안 맞음. fix(케이스 2 — 토큰 독립매칭): (백엔드 `ExecutionDataController.traceBySourcePk`) `pkColumn`/`pkValue` 콤마 다중값 허용 → 복합이면 각 값을 `|`-토큰으로 매칭(`%:v"]`/`%:v\|%`/`%\|v"]`/`%\|v\|%`, 순서 무관, tableId prefix 안 씀 — proxy 의 mgmtJdbc 가 `datasource_table` 없는 dev DB 라 `findTableIdByName` null 나는 문제 회피). Step2(Loader 복사)·Step3(직접 PK) 도 복합 지원. `getSourceData`/`executeBatchSourceQuery` 응답에 `pkColumns`(=`findPkColumns`, DB 제약조건 순) 추가. (프론트) `tableData.pkColumns` 로 PK 컬럼 알아내 값 전부 콤마 전송, 없으면 `columns[0]` fallback. `types/index.ts`/`lib/api.ts` `pkColumns?` 추가. **중요 교훈: `ExecutionDataController` 는 agent 모듈에서 excludeFilter — Proxy(proxy-dmz/proxy-internal)에서만 동작. trace/execution-data 관련 변경은 proxy 들을 재빌드·재기동해야 함** (agent 재기동은 무의미). |
| 이용량 클린 테스트 | **진행중** | DMZ PG 29001 dev: `use_legacy_data`/`use_status_data`/`use_jeju_day` → link_status=PENDING, `if_snd_use_*` TRUNCATE. Oracle 29004: `IF_RSV_USE_*`/`PM_GD111021`/`PM_GD111022`/`TM_GD111024`/`TM_GD111025` TRUNCATE (TM_GD111010 보존 — UseLoadStep `resolveBrnchId` 룩업용). `dmz-others-snd-use` 1회 실행(`adce112c`) 후 정방향 추적 검증에 사용. 다음: `internal-use-rcv` → 이용량 Internal Loader(UseLoadStep) → 처리현황 per-target + 양방향 추적. |

## 변경 파일 (전부 미커밋)

- **common**: `sync/TableCountTracker.java`(신규), `sync/SyncLogWriter.java`(신규), `step/SourceToTargetStep.java`(SyncLogWriter 위임), `controller/ExecutionDataController.java`(traceBySourcePk 복합 PK 토큰매칭, Step2/3 복합, getSourceData·executeBatchSourceQuery 에 pkColumns, findPkColumns 로그 INFO)
- **bojo-dmz**: `loader/step/LoaderStepHelper.java`(SyncLogWriter 위임)
- **bojo-internal**: `loader/step/{JejuJewonLoadStep,JejuObsvdataLoadStep,JejuFacilityLoadStep,SimpleLoadStep,UseLoadStep,InternalBojoLoadStep}.java`(TableCountTracker + SyncLogWriter, jewon writeCount 정정, UseLoadStep updateLastReceive 건수 리턴)
- **frontend**: `lib/api.ts`(TableData.pkColumns), `types/index.ts`(pkColumns?), `app/(main)/executions/[id]/page.tsx`(소스행 클릭 시 pkColumns 의 컬럼들 값 전부 콤마 전송)
- common JAR → 9모듈 `libs/` 복사. bojo-dmz/bojo-internal/others-dmz/proxy-dmz/proxy-internal 재빌드.
- **[미커밋, 5/12분]** `ExecutionDataController.java`/`ExecutionDataReader.java` (trace tableId 기반 해석) — 이번 common 변경에 같이 들어있음.
- **계획서**: `dev_plan/2026_05/13/sync-log-per-target-count.md`, `dev_plan/2026_05/13/forward-trace-composite-pk.md`
- **DB 변경(테스트)**: 29001 dev — 이용량 SND 소스 PENDING / if_snd_use_* TRUNCATE. 29004 — IF_RSV_USE_* + PM/TM_GD1110* (TM_GD111010 제외) TRUNCATE. 제주 `...3d067a41` 회차 행 삭제 + IF 3테이블 PENDING.

## 다음

- 이용량 체인 마저: `internal-use-rcv` → UseLoadStep → 처리현황 per-target + 양방향 추적(use_legacy_data 단일 PK 정방향 회귀 포함).
- 새올(`dmz-others-snd-saeol`) 클린 테스트.
- 미커밋 누적분 정리·커밋 (SyncLog 공통화 / source_ref 표준화 / 5/12 trace fix / composite forward trace fix — 분리 커밋 권장). + `findPkColumns` 로그 INFO→debug 원복할지 (디버깅용이었음).
- 03-bojo §5~ (Conditions 검증 / Trace / Schedule / Frontend / 문서 + stable tag).

## 메모

- 메모리 후보(승인 후): (1) "SyncLog 저장 = `SyncLogWriter.save(...)` 만 — `SyncLog.builder()` 직접 + target_tables JSON 손코딩 금지. 멀티타겟 step 은 `TableCountTracker` 로 per-table 카운팅(프론트 처리현황 분해 표시용)." (2) "`ExecutionDataController` 는 agent 에서 excludeFilter — Proxy(proxy-dmz/proxy-internal) 에서만 동작. trace/execution-data 변경 시 proxy 재빌드·재기동 필수." (3) "복합 PK 소스 정방향 추적 = PK 값 전부를 `|`-토큰 독립매칭(순서 무관). source_refs 의 복합 pk 조인 순서 = DB 제약조건 KEY_SEQ 순(yml `primary-key` 순서 아님)."
- java 프로세스 20+개 누적(재기동 헤매며 죽은 gradle 데몬들) — PC 렉 원인. 정리 권장(필요시 IntelliJ 외 java 다 kill 후 재기동).

---

## 추가 (오후 늦게) — 추적 정의기반 전면 정리

### 요약

| 파트 | 상태 | 비고 |
|------|:----:|------|
| 04-others 이용량 클린 테스트 (3-step 체인 RCV·Loader 마저) — 진행중 | RCV 까지 ✅ | `dmz-others-snd-use_adce112c` 1회 (전세션) → `internal-use-rcv_7fab8314` SUCCESS 20/20 (10/5/5). 다음 단계 = 이용량 Internal Loader(UseLoadStep) → 처리현황 per-target + 양방향 추적. |
| **역추적 — 휴리스틱 일소 (분기 1·2·3 통합)** | ✅ 완료·검증 | 사용자가 Internal RCV TARGET `IF_RSV_USE_JEJU_DAY` 역추적 400 ("Source 테이블 존재 X: ...") 발견. 원인: 분기 1·2(if_rsv_/if_snd_) 의 baseName 차감 fallback 이 Internal RCV(source `if_snd_*`/target `IF_RSV_*` — 양쪽 prefix)에서 깨짐. 4/22(`67a0b3f`)가 분기 3만 정확매칭 강화, 1·2는 "기존 기능 유지"로 둔 보수적 결정의 부작용. fix: `traceToSource` 단일 `resolveTraceSourceTable` 헬퍼로 통합 — sync_log target_tables 정확매칭(equalsIgnoreCase) → 매핑의 source_tables[0]. `isSndRelay`/`isLoaderTarget`/`originalIfTable` + 그 fallback(`L1241~/L1276~`) + `traceSourceBySndBusinessKey`/`getUniqueKeyColumns` 제거. `SyncLog.containsSourceTable/containsTargetTable`(JSON substring) 제거 → `ExecutionDataController` 헬퍼(parseJsonArray + equalsIgnoreCase)로 통일. |
| **정방향 추적 — LIKE 일체 폐기, `=` 정확 동등성만** | ✅ 완료·검증 | 사용자 원칙 강화: "정의된 걸 LIKE 로 찾으면 다른 거 잡힐 수 있다". backend(`ExecutionService.traceBySourcePk`)가 agent.agentTables 에서 sourceTable SOURCE 정확매칭 → datasource → zone shortCode + dsId + tableId 해석 → **exactSourceRefs = `["zone:dsId:tableId:pk"]` 빌드** → proxy 로 전달. proxy 는 sync_log source_tables 정확매칭으로 candidates 좁힘 → `WHERE source_refs = ? AND execution_id = ?` 단일 SQL. **삭제**: Pattern A0/A/B(LIKE), `queryByCompositePkTokens`(토큰 LIKE 4패턴 OR), Step 2 Loader passthrough, Step 3 직접 PK 폴백, `splitCsv` 헬퍼. 검증: `internal-use-rcv` 단일 PK 정방향 ✓, 복합 PK 정방향 회귀(`use_jeju_day`) ✓, 역방향 ✓. |
| 메모리 룰 `feedback_trace_definition_only` 강화 | ✅ | LIKE 일체 금지 명시 추가, `=` 정확 동등성만 명시. docs/claude-memory 동기화. |

### 핵심 교훈 (사용자 확정 원칙)
- 추적 코드는 **정의 진실원** 만 본다 — `sync_log.target_tables`/`source_tables` 정확매칭, `source_refs` 박힌 값, datasource_table.id.
- **LIKE/contains/substring/prefix/baseName 휴리스틱 일체 금지.**
- 매칭에 쓸 값은 backend 가 **정의 함수(`SourceRefUtils.buildJson`)와 동일 로직으로 재현**해서 `=` 로 찾는다 — 정확 동등성.
- 정확매칭 실패 = 정의에 매핑 없음 = **명확한 에러** (입력값 + 시도한 candidates 노출). 휴리스틱 fallback 으로 메꾸지 않음.
- 휴리스틱은 그 시점 케이스엔 맞아도 새 패턴(예: Internal RCV — source/target 양쪽 prefix)에서 깨짐. 4/22 가 분기 3 만 손대고 1·2 유지한 결정의 부작용이 이번에 드러남.

### 변경 파일
- common `ExecutionDataController.java`
  - `traceToSource`: `resolveTraceSourceTable` 단일 헬퍼로 통합. 분기 1·2·3 + `isSndRelay`/`isLoaderTarget`/`originalIfTable` 변수 + fallback(L1241~, L1276~) + `traceSourceBySndBusinessKey`/`getUniqueKeyColumns` 제거. 실패 시 명확한 에러 메시지.
  - `traceBySourcePk`: `exactSourceRefs` 파라미터 받음. Pattern A0/A/B / `queryByCompositePkTokens` / `splitCsv` 제거. candidate 좁힘 = sync_log source_tables 정확매칭 → 매핑의 target_tables. SQL 한 줄(`source_refs = ?`).
  - `containsSourceTable(SyncLog,String)`/`containsTargetTable(SyncLog,String)` 헬퍼 추가 (parseJsonArray + equalsIgnoreCase).
- common `SyncLog.java`: `containsSourceTable`/`containsTargetTable` 메서드 제거 (JSON substring 휴리스틱).
- orchestrator `ExecutionService.java`: `traceBySourcePk` 가 backend 단계에서 agent SOURCE → exactSourceRefs 빌드 + proxy 호출 시 query param 전달. `_view` 차감 + contains 자동해석 잔재 제거.
- memory `feedback_trace_definition_only.md` 본문 강화 + docs/claude-memory 동기화.
- 계획서 2건: `dev_plan/2026_05/13/trace-definition-only.md`(역방향 정리 — 일부 적용 후 강화로 대체), `dev_plan/2026_05/13/trace-equality-only.md`(LIKE 폐기 강화 — 적용·검증 완료).

### 다음
- 이용량 체인 마저 — Internal Loader(UseLoadStep) 돌려 처리현황 per-target + 양방향 추적 검증.
- 새올(`dmz-others-snd-saeol`) 클린 테스트.
- 03-bojo §5~.

---

## 추가 (저녁) — 이용량 체인 본 실행 + SN 보존 fix + 비추적 분류 + 내일 계획

### 요약

| 파트 | 상태 | 비고 |
|------|:----:|------|
| 이용량 SND/RCV/Loader 본 실행 | ✅ 1차 진단 | `dmz-others-snd-use_adce112c` → `internal-use-rcv_7fab8314` → `internal-use-loader` 실행. Loader 가 `TM_GD111025` INSERT 시 `ORA-01400: NULL을 SN 안에 삽입할 수 없습니다`. 1차 원인 = TM_GD111010.TELNO 가 전부 NULL → resolveBrnchId 실패 가정 → 사실은 SN 자체가 NULL 이라는 더 깊은 문제. |
| 시설(TM_GD111010) TELNO 정합 manual 처리 | ✅ | 새올 RGETSTGMS01 (90 컬럼) 에 TELNO 류 컬럼 자체가 없음 — 시스템 갭. "찾아서 처리" 가 한계라 BRNCH 53~59 에 manual UPDATE 로 7 TELNO 박음. legacy 10/10 + status 5/5 매칭 가능. (별개 todo: 시설 ↔ 이용량 연결키 시스템 정합 — 새올 어디서 가져올지 정의 필요) |
| **SN 보존 — exclude-insert-columns 4 step yml override** | ✅ 완료·검증 | UseLoadStep `INSERT INTO TM_GD111025 (SN, ...)` 가 IF_RSV.SN=NULL 으로 실패. 추적 결과 → `SourceToTargetStep` 의 default `excludeInsertColumns = ["id","sn"]` 가 SND·RCV 양 단계에서 비즈니스 sn 을 INSERT 에서 제외. 4/23(`fd077f1`) 가 만든 yml-override 기능 — `exclude-insert-columns: [id]` 명시로 풀어야 함. **use SND/RCV 4 step yml override + 데이터 클린 + 체인 재실행 → SN 끝까지 보존**. ※ 이용량 외 잠재적 같은 갭 = `td_gd010310`/`tm_gd010310`/`tm_gd014000`/`tm_gd014001` 4 테이블 + provide-tm-gd* 5 step (별개 점검 todo). |
| **비추적 테이블 분류 결정** | ✅ | `PM_GD111022`(일집계) — 파생 집계, (BRNCH_ID,OBSRVN_YMD) 자연키 fan-in. multi-ref JSON 박는 건 컬럼 4000자 한도 위험 + 정보 중복 (자연키로 도출 가능). 비추적. `TM_GD111024`(최근수신) — Link/스냅샷, BRNCH별 MAX(OBSRVN_DT) 1행 덮어씀. 비추적. UseLoadStep 의 두 MERGE 가 이미 SOURCE_REFS 컬럼 미박음 = 비추적 정책과 코드 일치, 코드 변경 불요. ※ **추적/SyncLog 매핑은 직교 차원** — 비추적이라도 처리현황 표시는 운영자에게 가시화 필요시 별개 결정. PM_GD111022/TM_GD111024 는 둘 다 SyncLog 매핑 그대로 유지(per-target tracker `30f393e` 그대로). |
| docs `non-traceable-tables.md` | ✅ | 과거 Link 룰(`link_ngwis`/`tm_gd980002`/`tm_gd970101`)통합 + 이번 결정. `테이블 / 이유 / 어디서 적재` 3 컬럼 표. tm_gd970101 양면성(Link 컨텍스트 vs jeju EAV 결과정의 추적용) footnote. |
| 메모리 룰 `feedback_table_alignment` | ✅ 신규 | 마크다운 표는 raw 줄맞춤 필수 + 셀 컴팩트 + 디테일은 footnote (CJK=2칸 폭 계산). 양쪽 동기화. |
| 이용량 retention/where-filters yml 계획서 | ✅ 작성, **수행 내일** | 6 retention 후보 + 5 where-filters (Loader 만). 보류 3 (string YYYYMMDD), 비대상 1 (TM_GD111024). 계획서: `dev_plan/2026_05/13/use-retention-where-filters.md`. |

### 핵심 교훈 / 결정

1. **`SourceToTargetStep` default `excludeInsertColumns = ["id","sn"]` 는 over-broad** — id+sn 둘 다 IDENTITY surrogate 라 가정. use 도메인은 `id`(surrogate) + `sn`(business) 가 둘 다 있는 IF 테이블이라 default 가 비즈니스 sn 도 잘림. 4/23(`fd077f1`) 의 yml override 가 정공법. **잠재적 동일 갭**: source 에 `sn` 비즈니스 컬럼 가진 6 테이블 (use 2개 + tm_gd010310/tm_gd010310/tm_gd014000/tm_gd014001 4개) — 후속 점검 todo.

2. **추적 vs SyncLog 매핑 = 직교 차원**:
   - 추적 (source_refs 기반 행단위 매칭) vs SyncLog 매핑 (운영자 가시화) 따로 결정.
   - 옛 Link 룰 "Link → SyncLog/모니터링/매핑에서 제외" 는 link 테이블에서 우연히 둘이 일치했을 뿐 — 일반화 X.
   - PM_GD111022/TM_GD111024 는 비추적이지만 SyncLog 매핑은 유지 (운영자 N건 적재 가시화).

3. **파생 집계 / Link 테이블 source_refs 비박음 결정 기준**:
   - 자연키로 도출 가능 (PM_GD111022: brnch+ymd) → multi-ref JSON 중복 + 컬럼 4000 한도 위험.
   - Link/스냅샷 (TM_GD111024) → 다음 회차에 덮어써 ref 의미 약함.

4. **마크다운 표 raw 줄맞춤** 룰 — 메모리 박음. 앞으로 표 쓸 때 적용.

### 미커밋 변경

- yml override (오늘 오후 분 — exclude-insert-columns):
  - `infolink-agent-others-dmz/.../dmz-others-snd-use.yml` (use-legacy-snd / use-status-snd)
  - `infolink-agent-bojo-internal/.../internal-use-rcv.yml` (use-legacy-rcv / use-status-rcv)
- 신규 docs:
  - `docs/non-traceable-tables.md`
  - `docs/claude-memory/feedback_table_alignment.md` (memory 양쪽 동기화)
- 신규 계획서:
  - `dev_plan/2026_05/13/use-retention-where-filters.md` (내일 수행)
- 메모리: `feedback_table_alignment.md` (양쪽 동기화 + MEMORY.md 인덱스)

### DB 변경 (테스트 Oracle 29004 / DMZ PG 29001)

- `TM_GD111010` BRNCH 53~59 에 TELNO manual UPDATE (7건). 시설 ↔ 이용량 연결키 정합 시스템 갭의 임시 해결.
- 이용량 체인 클린 후 SND/RCV/Loader 본 실행 → `dmz-others-snd-use_2f0b…` (또는 새 회차) / `internal-use-rcv_…` / `internal-use-loader_…` 회차 적재됨.
- `IF_RSV_USE_*` SN 보존 확인 (이번 yml override 의 결과).

## 내일 시작 시

> 0) 서비스 상태 확인 — bojo-internal(8092) / others-dmz(8085) / proxy-dmz(8083) / proxy-internal(8093) / backend(8080) / bojo-dmz(8082) / auth(8096) / frontend(3000). 재부팅 됐으면 다 띄우기. yml 변경분이 이미 메모리에 떠있는 상태였으니 재기동 시 그대로 재로드됨.

> 1) **이용량 retention/where-filters yml 박기** (계획서 `dev_plan/2026_05/13/use-retention-where-filters.md` 그대로):
>    - `dmz-others-snd-use.yml`: retention 2 후보 (`if_snd_use_legacy_data`/`if_snd_use_status_data`)
>    - `internal-use-rcv.yml`: retention 2 후보 (`IF_RSV_USE_LEGACY_DATA`/`IF_RSV_USE_STATUS_DATA`)
>    - `internal-use-loader.yml`: retention 2 후보 (`PM_GD111021`/`TM_GD111025`) + where-filters 5개
>    - yml 만 수정, 코드 변경 X.
>    - others-dmz / bojo-internal 재기동 (yml 재로드).
>    - 검증: 프론트 → 이용량 agent 수동실행 화면 → 조건실행 select 에 5 필터 큐레이션 노출 / Retention 설정 화면(있으면) 6 후보.
>    - 통과 시 dev_log + 커밋.

> 2) 이용량 후속 검증 (시간 남으면): 처리현황 per-target 카운트 (PM_GD111021 / PM_GD111022 / TM_GD111024 / TM_GD111025 각 분리) + 양방향 추적 (단일 PK·sn 보존 확인) — `internal-use-loader` 결과 확인.

> 3) 04-others 본 테스트 이어서:
>    - 새올(`dmz-others-snd-saeol`) 클린 테스트
>    - yaksoter / api-collect

> 4) 03-bojo §5~ (Conditions 본 검증 / Trace / Schedule / Frontend / 문서).

### 별개 todo (관련 발견사항)

- **시설 ↔ 이용량 연결키 시스템 정합** — 새올 시설등록(RGETSTGMS01) 에 TELNO 없음. 이용량 측은 TELNO 로 시설 식별. 마중물 연락처(RGETNYCSG01.PLAN_OP_UPCH_TELNO) 와 시설등록의 매핑 필요? 또는 시스템 설계상 별도 매핑 테이블? 도메인 결정 필요.
- **exclude-insert-columns 잠재 같은 갭 점검** — source 에 `sn` 비즈니스 컬럼 가진 4 테이블 (`td_gd010310`/`tm_gd010310`/`tm_gd014000`/`tm_gd014001`) + provide-tm-gd* 5 step. 그쪽 agent 테스트 시 같은 패턴 적용 검토.
- **string YYYYMMDD retention enhancement** — `if_snd_use_jeju_day`/`IF_RSV_USE_JEJU_DAY`/`PM_GD111022` 등 varchar 날짜 컬럼도 retention 처리 가능하게 `RetentionCandidate` 보강. 별개 트랙.
- **WhereFilterDef `*` 와일드카드 활용** — `column: "*"` / `table: "*"` 단축형 — 단순 카피 step 에서 메타 컬럼 빼고 모든 비즈니스 컬럼 노출하는 식 패턴 — 다른 yml 적용 검토.
- **자연키 기반 비추적 테이블 trace UI enhancement** — PM_GD111022/TM_GD111024 같은 비추적 테이블도 BRNCH+YMD 같은 자연키로 contributing IF 조회 가능. 추적 UI 의 별도 경로로 노출 (선택).
