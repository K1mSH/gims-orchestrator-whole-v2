# 2026-04-30 작업 일지

## 요약 (보고용)

### 파트별 진행도

| 파트 | 상태 | 비고 |
|------|:----:|------|
| **약수터 파이프라인 E2E** (SND/RCV/Loader) | **완료** | 제원 1026 + 수질 1000 row 손실 0, LINK_STATUS=SUCCESS 정합 |
| 계획서 — `dev_plan/2026_04/30/yaksoter-yaml-e2e.md` | **완료** | 4/29 §B 이월분, 6 step 적재 완료 |
| YAML 3건 (SND/RCV/Loader) — yaksoter | **완료** | 코드 신규 0, factory 재사용. source-table 추측 함정 정정 (`yaksoter_*` → `tm/td_gd010310`) |
| **SimpleLoadStep merge-key List 확장** | **완료** | 자연키 복수 컬럼 지원. 기존 5 파이프라인 후방 호환 |
| **IF entity 자동채번 표준 정합** | **완료** | 8 entity `@GeneratedValue(IDENTITY)` 추가 + PG 6 테이블 sequence 박음 |
| 계획서 — `if-entity-auto-increment-standardization.md` | **완료** | 룰 재정비 + 모듈별 진단표 |
| **frontend 추적 매칭 false positive fix** | **완료** | `Object.values.some` (컬럼 무시) → backend API (`traceBySourcePk`) 호출. 50줄 → 7줄 |
| **DatasourceService introspection fallback** | **완료** | `registerTable` request.columns 비면 자체 introspection / `refreshTableColumns` 항상 자체 introspection |
| **WTSMP_YMD length 8→10** | **완료** | bojo-int IF_RSV_TD / TD entity + Oracle DROP/recreate |
| datasource_table 소문자 중복 정리 + Loader zone 정정 | **완료** | api-collect/yaksoter RCV 매핑 대문자로 redirect / `INTERNAL → INTERNAL_COMMON` |
| InfoTab ✓/✗ 분기 컬럼 수 표시 | **완료** | "처음 렌더 시 보였다가 덮임" 패턴 fix |
| bojo-int 4 entity `@org.hibernate.annotations.Table` 제거 | **완료** | DynamicEntityManagerService NamingStrategy 충돌. 다른 IF_RSV entity 와 일관 패턴 |
| /trace-source 추적 검증 | **완료** | source.sn=652 클릭 → target sn=656 정확 매칭 |
| **별개 트랙 (본 세션 외)** | **대기** | 5 row datasource_column 회복 / bojo IF Integer→Long 통일 / IF_RSV.SN 잉여 컬럼 / 메모리 룰 갱신 |

---

## 오전 — 약수터 SND/RCV/Loader 진입

### 4/29 EOD 이월분
- §A (수집 단계 + 엔티티 정정) 완료된 상태에서 §B (코드 작업) 진입
- plan 작성 — `dev_plan/2026_04/30/yaksoter-yaml-e2e.md` (12 단계 정리)

### 사전 토론 — 토론 영역 3건
1. **Agent 등록 방식** = 사용자 화면 직접 (Claude 서포트)
2. **Internal Oracle 약수터 4 테이블 사전 상태 확인** = 기동 직전
3. **VER-004 패턴 (execution_id 인덱스)** = 다른 에이전트 패턴 따라가기

### SimpleLoadStep merge-key 단일→복수 한계 발견
- 기존 5 파이프라인 (api-collect/use/jeju/saeol/news) 모두 단일 키 (`ORGNL_URL`/`BID_PBANC_NO` 등)
- 약수터 자연키 = 복수 (`BRNCH_NO+BRNCH_STD_CD` / `BRNCH_NO+YR+QTR+WTSMP_YMD`)
- 수정 — common 코드 1군데 (`SimpleLoadStep` + `SimpleLoadStepFactory`), `mergeKey String → List<String>` 확장
- 기존 단일 키 `toList()` 정규화로 후방 호환 (회귀 0)

### Internal Oracle 약수터 4 테이블 사전 점검
- 4/15 시점 자동 생성 잔존 발견 — row 0 / nullable=N / length=400 (entity=500)
- DROP 후 bojo-int 재기동 → 자동 재생성 (length=2000 / nullable=Y / index 박힘)

---

## 오후 — 다수 발견 + 정합화 작업

### 1) Internal Oracle 4 entity 코멘트 누락 발견
- 사용자 화면 등록 시 다른 코멘트 없는 테이블 다수 표시 (TM_GD010910/110350/110351 등 B13 source — 별개 영역)
- 약수터 4 entity 의 entity 코멘트 적용 — 4 entity 풀 rewrite + `@org.hibernate.annotations.Table(comment=...)` 박음
- DROP 후 자동 재생성 — Oracle USER_TAB_COMMENTS / USER_COL_COMMENTS 정상 박힘 검증

### 2) Loader 시작 후 SND fail — `null value in column "sn"`
- 약수터 SND 1차 시도 → 1000건 모두 fail
- 진단 — `SourceToTargetStep` default `["id","sn"]` 가 sn INSERT 제외 → if_snd_*.sn (NOT NULL, sequence 없음) → null 위반
- **광범위 검증** — 다른 모든 IF entity 비교:
  - bojo IF (`@Id Integer id`) — DB sequence 박힘 (PG SERIAL auto-detect 추정) → 동작
  - jeju (`@GeneratedValue(IDENTITY) Long id`) — 정합 (정석)
  - **others IF_SND 6개 (`@Id Long sn`, IDENTITY 명시 없음, sequence 없음)** — 깨짐 / 4/03 시점 옛 default `["id"]` 로 동작했으나 4/23 변경 이후 새로 돌리면 fail

### 3) 4/23 plan 의 사각지대 + 룰 재정비
- 4/23 plan (`auto-increment-pk-hardcoding-fix.md`) 채택 = 방안 C 하이브리드
  - 의도 = JDBC 메타 IDENTITY 자동 감지 + fallback `["id","sn"]`
  - 실제 구현 = fallback 만, **메타 감지 미구현**
- 사용자 룰 정정 명문화:
  > **"Target IF entity 의 PK 는 자동채번 명시 (`@GeneratedValue(IDENTITY)`) 가 기본. source/target 겹치는 비-PK 비즈니스 컬럼이 있을 때만 YAML `exclude-insert-columns` 로 조정"**
- plan 작성 — `dev_plan/2026_04/30/if-entity-auto-increment-standardization.md`

### 4) 8 entity 자동채번 표준 정합화 + DB 마이그
- **others IF_SND 6 entity** (`@GeneratedValue(IDENTITY)` 추가):
  - IfSndTmGd010310 (yaksoter), IfSndTdGd010310 (yaksoter)
  - IfSndTmGd014000 (nara), IfSndTmGd014001 (news)
  - IfSndUseLegacyData, IfSndUseStatusData
- **bojo IF_SND 2 entity 정합화** (Integer id, IDENTITY 명시 추가 + 거짓 주석 정정):
  - IfSndSecJewon, IfSndSecObsvdata
  - 주석 "Source ID 그대로 사용 (PK)" → "자동증분 PK"
- DB 마이그 SQL — 6 PG 테이블 sn 컬럼 IDENTITY 추가 (`ADD GENERATED BY DEFAULT AS IDENTITY` + `setval(GREATEST(MAX+1, 1))`)
- 약수터 SND 재실행 → **1026 + 1000 정상 적재** ✅

### 5) frontend 추적 매칭 false positive 발견
- 사용자 발견: source row sn=652 (PUB_2666) 클릭 → 매칭 target = sn=12 (PUB_2778, source_refs="...3")
- **다른 brnch_no 의 row 가 짝으로 표시** = false positive
- 코드 추적 — `app/executions/[id]/page.tsx:229-239`:
  ```javascript
  return pkParts.every(part =>
    Object.values(row).some(v => String(v) === part)  // ← 컬럼 무시, 값만 매칭
  );
  ```
- source row 안에 "3" 값 있는 컬럼 (탁도/일반항목 등) 존재 → false positive
- **이전 추적 수정 history 검증** (Explore agent):
  - 추적 관련 commits 20+ — **모두 backend (buildSourceFilter, SourceToIfStep, Loader 등) 영역**
  - frontend SOURCE 클릭 매칭 = `c145775` (4/1) 도입 후 **8주간 미수정** = 사각지대
  - 회귀 테스트 = backend API 단위만, frontend 화면 행 클릭 시나리오 누락
- fix — frontend 자체 매칭 (1000건 fetch + Object.values.some) 통째 제거 + backend API (`traceBySourcePk`) 만 호출. **50줄 → 7줄**
- 검증 — source.sn=652 클릭 → target sn=656 정확 매칭 ✅

### 6) RCV fail — `ORA-12899: WTSMP_YMD 열에 대한 값이 너무 큼 (실제: 10, 최대값: 8)`
- 진단 — bojo-int `IfRsvTdGd010310.WTSMP_YMD` length=8 / source 값 = `YYYY-MM-DD` 10자
- fix — entity 2 파일 length 8 → 10 + Oracle DROP/recreate
- 검증 — Oracle data_length=40 (VARCHAR2(10 CHAR) UTF-8) ✅

### 7) datasource_table 소문자 중복 + Loader zone 정정
- 발견:
  - 약수터/api-collect IF_RSV 의 **소문자/대문자 두 row 동시 등록** (사용자 화면에서 잘못 등록)
  - Loader Agent zone = `INTERNAL` (다른 Loader 모두 `INTERNAL_COMMON`)
- fix:
  - agent_table 매핑 대문자 row 로 redirect (id=234→229, 235→228, 264→262, 265→261)
  - 소문자 datasource_column / datasource_table DELETE
  - Loader zone `INTERNAL → INTERNAL_COMMON`

### 8) DatasourceService introspection fallback 보강
- 사용자 발견 — 5 row datasource_column 0건 (api-provider 3 + internal 2). 화면 "(N컬럼)" 표시 안 됨
- 진단 — `registerTable` 이 frontend 가 보낸 request.columns 만 INSERT, **자체 introspection 없음**. 자동화 스크립트 / 우회 흐름에서 누락 발생
- 사용자 룰 = "별도 endpoint 만들지 말고 등록 흐름 자체에서 누락 안 되도록"
- fix (소스 최소 수정 — `DatasourceService.java` 1 파일):
  - 헬퍼 추가 — `introspectColumns(datasourceId, tableName)` (기존 `searchColumns` 재사용)
  - `registerTable` — request.columns 비면 fallback (auto introspection)
  - `refreshTableColumns` — request.columns 무시, 항상 자체 introspection (재수집의 본질)
- 효과: 5 row 회복 = 화면 재수집 버튼 한번 누르면 자동 채워짐

### 9) Loader 시작 후 `@org.hibernate.annotations.Table references an unknown table`
- DynamicEntityManagerService 의 `CaseAwareNamingStrategy` 와 `appliesTo` 매칭 충돌
- 약수터 4 entity 만 `@org.hibernate.annotations.Table` 박혀있음 — 다른 IF_RSV 17+개 entity 는 미사용
- fix — 약수터 4 entity 의 `@org.hibernate.annotations.Table` 어노테이션 제거 + IfRsv 2 entity javadoc 추가 (self-doc 보존)
- DB 코멘트 = 이미 박혀있음 (DROP/recreate 시 박힌 거) + ddl-auto=update 가 변경 안 시도 → 그대로 유지
- Loader 재실행 → **1026 + 1000 적재 완료** ✅

### 10) InfoTab ✓/✗ 분기 컬럼 수 표시
- 사용자 발견 — Agent 상세 화면에서 컬럼 수가 처음 보였다가 덮어써져 사라짐
- 진단 — InfoTab 의 `viewSourceVerify` 분기 (line 880-886) 에 컬럼 수 표시 코드 누락
  - 첫 렌더: sourceTables 분기 → "(N컬럼)" 표시
  - `loadPipelineVerify()` 비동기 종료 후 viewSourceVerify 채워짐 → ✓/✗ 분기 우선 → 컬럼 수 사라짐
- fix — Source/Target ✓/✗ 분기 두 곳에 컬럼 수 표시 추가

---

## 추적 검증 (E2E)

| 단계 | row 수 | LINK_STATUS |
|---|:-:|:-:|
| Source PG `tm_gd010310` (제원) | 1026 | - |
| Source PG `td_gd010310` (수질) | 1000 | - |
| IF_SND PG (SND 결과) | 1026 / 1000 | PENDING |
| IF_RSV Oracle (RCV 결과) | 1026 / 1000 | **SUCCESS** ✅ |
| Target Oracle (Loader 결과) | **1026 / 1000** | - |

**손실 0 / 정합 완료**.

화면 추적: source sn=652 (PUB_2666) → target sn=656 (source_refs="...652") 정확 매칭 ✅

---

## 다음 할 일 (5/1~)

### 즉시 가능 (사용자 화면 작업)
- **5 row datasource_column 회복** — 화면 재수집 버튼 한번 누르기 (api-provider 3 + internal 2)
  - api_prv_tm_gd120001
  - api_prv_tmp_megokr_api
  - api_prv_wt_dream_permwell_public_21033
  - TMP_MEGOKR_API
  - SDE_NGWS.WT_DREAM_PERMWELL_PUBLIC_21033

### 별개 트랙 (VER 이슈 / 별개 plan 등록 후보)
- bojo-int Oracle IF_RSV 의 잉여 SN 컬럼 정책 (nullable=Y, IDENTITY 없음 — silent 손실 영역)
- bojo IF_SND Integer id → Long id 타입 통일 (다른 IF entity 와 일관)
- frontend 추적 화면 회귀 테스트 시나리오 추가 (도입 후 8주 사각지대 재발 방지)
- 메모리 룰 `feedback_trace_definition.md` 갱신 — "끝자리 PK == source PK 정합 비교까지 검증 포함"
- bojo-int 의 `@org.hibernate.annotations.Table` 일관 도입 — DynamicEntityManagerService NamingStrategy 보강 필요 (`@Comment` 클래스 레벨 호환 / 또는 별도 SQL DDL 표준화)
- SyncLog `error_summary` Oracle 4000자 한계 (4/30 RCV 실패 시 발견) — message truncation 추가 또는 컬럼 길이 확장

### Type B Phase 3 (B13) — 4/30 미진행
- 4/29 dev_log 의 "다음 할 일" 그대로 이월
- v3 SQL `opn.waterQualityMfdsInfo` + helper 2개 + DDL 3종

---

## 수정 파일

### 코드
| 모듈 | 파일 | 내용 |
|------|------|------|
| sync-agent-bojo-int | `loader/step/SimpleLoadStep.java` | mergeKey String → List<String> 확장 |
| sync-agent-bojo-int | `loader/factory/SimpleLoadStepFactory.java` | toList() 적용 |
| sync-agent-bojo-int | `entity/iftable/IfRsvTmGd010310.java` | `@Index(execution_id)` + 코멘트 + javadoc + length 정정. `@org.hibernate.annotations.Table` 제거 |
| sync-agent-bojo-int | `entity/iftable/IfRsvTdGd010310.java` | 동 + WTSMP_YMD length 8→10 |
| sync-agent-bojo-int | `entity/target/TmGd010310.java` | `@Index` + 코멘트. `@org.hibernate.annotations.Table` 제거 |
| sync-agent-bojo-int | `entity/target/TdGd010310.java` | 동 + WTSMP_YMD length 8→10 |
| sync-agent-others | `entity/iftable/snd/IfSndTmGd010310.java` | `@GeneratedValue(IDENTITY)` 추가 |
| sync-agent-others | `entity/iftable/snd/IfSndTdGd010310.java` | 동 |
| sync-agent-others | `entity/iftable/snd/IfSndTmGd014000.java` | 동 |
| sync-agent-others | `entity/iftable/snd/IfSndTmGd014001.java` | 동 |
| sync-agent-others | `entity/iftable/snd/IfSndUseLegacyData.java` | 동 |
| sync-agent-others | `entity/iftable/snd/IfSndUseStatusData.java` | 동 |
| sync-agent-bojo | `entity/iftable/snd/IfSndSecJewon.java` | `@GeneratedValue(IDENTITY)` + 거짓 주석 정정 |
| sync-agent-bojo | `entity/iftable/snd/IfSndSecObsvdata.java` | 동 |
| sync-agent-others | `config/agents/dmz-others-snd-yaksoter.yml` | **신규** (source-table 정정 후) |
| sync-agent-bojo-int | `config/agents/internal-yaksoter-rcv.yml` | **신규** |
| sync-agent-bojo-int | `config/agents/internal-yaksoter-loader.yml` | **신규** (merge-key 자연키 list) |
| sync-orchestrator/backend | `service/DatasourceService.java` | introspection fallback (registerTable + refreshTableColumns) + 헬퍼 |
| sync-orchestrator/frontend | `app/executions/[id]/page.tsx` | SOURCE 클릭 매칭 false positive fix (1000건 fetch + frontend find 제거) |
| sync-orchestrator/frontend | `components/agent/InfoTab.tsx` | ✓/✗ 분기 컬럼 수 표시 추가 |

### DDL / DB 마이그
| 파일 | 내용 |
|------|------|
| `scripts/ddl/dmz-pg/2026-04-30-if-snd-sn-identity.sql` | **신규** — 6 테이블 sn 컬럼 IDENTITY 추가 (옛 데이터 max+1 시작) |
| Internal Oracle | DROP 4 테이블 (TM/TD/IF_RSV_TM/TD_GD010310) — 자동 재생성 (3차) |
| PG dev DB | 6 IF_SND 테이블 sn IDENTITY 박음 |
| Orchestrator DB | agent_table redirect (4 row) + datasource_table 소문자 4 row 삭제 + Loader zone 정정 |

### 계획 / 일지
| 파일 | 내용 |
|------|------|
| `dev_plan/2026_04/30/yaksoter-yaml-e2e.md` | **신규** (이미 4/30 오전 작성) |
| `dev_plan/2026_04/30/if-entity-auto-increment-standardization.md` | **신규** — 룰 재정비 + 모듈별 진단표 |
| `dev_logs/2026_04/2026-04-30.md` | **신규** (본 문서) |

---

## 회귀 / 검증 결과

| 항목 | 결과 |
|---|---|
| 약수터 SND/RCV/Loader E2E | ✅ 1026 + 1000 row 손실 0 |
| LINK_STATUS 전환 | ✅ PENDING → SUCCESS (RCV 후 Loader 가 set) |
| /trace-source 추적 | ✅ source sn=652 → target sn=656 정확 매칭 |
| SimpleLoadStep 회귀 (다른 5 파이프라인 단일 키) | ✅ toList() 정규화로 후방 호환 (실행 회귀 안 했지만 코드 일관) |
| IF entity 자동채번 정합 | ✅ 8 entity + DB sequence 박힘 |
| frontend 매칭 fix | ✅ false positive 제거. 1000건 fetch 제거 = 효율 ↑ |
| introspection fallback | ✅ 등록/재수집 양 endpoint 모두 자체 introspection. 5 row 회복 화면 재수집으로 가능 |
| InfoTab ✓/✗ 분기 컬럼 수 | ✅ "처음 보였다가 사라짐" 패턴 fix |

---

## 핵심 결정 / 룰 재정비

### IF entity 자동채번 표준 (사용자 결정)
> **Target IF entity 의 PK 는 자동채번 명시 (`@GeneratedValue(IDENTITY)`) 가 기본. source/target 겹치는 비-PK 비즈니스 컬럼이 있을 때만 YAML `exclude-insert-columns` 로 조정.**

### datasource_column 진실원 (사용자 결정)
> **컬럼 메타 진실원 = DB 자체. Orchestrator backend 는 등록/재수집 시 항상 DB 직접 introspection 으로 동기화. frontend payload 에 의존 X.**
> **별도 sync-columns endpoint 만들지 말고 등록 흐름 자체에서 누락 안 되도록.**

### 추적 검증 정의 (메모리 룰 갱신 후보)
> 기존: "추적 검증 = 건수 + 단건 역추적까지 포함" (= 응답 1건 받기까지)
> **갱신 후보**: "추적 검증 = 건수 + 표시된 매칭 row 의 source_refs PK 끝자리 == 클릭한 source row 의 PK 정합 비교까지 포함"

---

> **본 세션의 큰 그림**: 약수터 파이프라인 코드 작업 + E2E 가 메인 의도였으나, 진행 중 발견된 **8 entity 자동채번 비일관 / frontend 추적 false positive / introspection 누락 / Hibernate NamingStrategy 충돌 / WTSMP_YMD 길이 mismatch** 등 다수 정합 이슈를 같이 해결. 룰 재정비 (자동채번 표준 + datasource_column 진실원) + 코드 fix + DB 마이그 + frontend 정합 모두 묶음.
