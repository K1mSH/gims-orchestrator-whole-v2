package com.sync.orchestrator.domain.agent;

import org.springframework.data.jpa.repository.JpaRepository;

public interface AgentExecutionModeRepository extends JpaRepository<AgentExecutionMode, Long> {
}
