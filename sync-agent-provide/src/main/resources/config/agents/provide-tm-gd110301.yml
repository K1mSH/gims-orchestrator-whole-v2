# ──────────────────────────────────────
# 패턴: 단순 복사 (Oracle 원본 → PG 제공 테이블 1:1)
# 소스: TM_GD110301 (수질측정망검사개요)
# 타겟: api_prv_tm_gd110301
# 레거시: MEGOKR selectNgw03 (수질측정망검사 개요)
# Note: Type B 전처리 대상이나 현재는 단순 카피 설정. 전처리 Step 구현 시 별도 factory-key로 전환 예정.
# ──────────────────────────────────────
agent-code: provide-tm-gd110301
type: LOADER

steps:
  - id: provide-tm-gd110301
    name: 수질측정망검사개요 복사
    factory-key: source-to-if
    source-table: TM_GD110301
    target-table: api_prv_tm_gd110301
    primary-key: WQ_INSP_SN
    conflict-key: source_refs
    target-meta-columns:
      - source_refs
      - execution_id
      - updated_at
